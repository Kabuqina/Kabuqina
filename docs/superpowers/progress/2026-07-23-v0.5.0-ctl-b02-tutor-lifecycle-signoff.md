# CTL-B02 · Tutor checkpoint/activity lifecycle Gate sign-off

> **结论：APPROVED / `CTL-B02 DONE`。** L-1 checkpoint/activity lifecycle、
> cross-process fence、crash recovery、BundleV2/runtime-only restore、terminal outbox
> 与 24 MiB bridge 已完成实现、修复复审和当前 bundle smoke。
>
> **Independent Gate reviewer：Codex（B 轨非作者 reviewer）**
>
> **签字授权：用户于 2026-07-23 明确授权**
>
> **签字时间：2026-07-23，Asia/Shanghai**
>
> **Reviewed target：** `ab685481411a24903eb3461622bbaeb7ae51c319`
>
> **Implementation base：** `2c34a8f282b3266f825ff1b0013220071d890383`

## Gate 结论

| 复审项 | 结论 |
|---|---|
| Lifecycle / DB truth | 独立 `tutor_runtime.db` v1、revision CAS、checkpoint、attempt reservation、terminal summary 与 outbox 合同均已落地；`learning.db` schema 保持 v1。 |
| Cross-process safety | shared coordinator 覆盖 LearningStore 与 TutorRuntimeStore；durable operation 由 writer liveness lock 保护，desktop 在 handshake 前及周期性恢复 dead writer。 |
| Delete/import/restore | composite operation phase journal 可恢复；projection 全程持有 owner-wide guard；runtime merge 在同一 transaction 校验实际合并后的全套配额并保持 zero-write failure。 |
| Terminal history | outbox identity 由完整 activity identity 唯一派生；import 必须匹配 terminal run、固定 type/time 和完整 run-derived payload，pending/delivered replay 不复制 history。 |
| Portability / bundle | Python/Rust canonical JSON 对 NFC、key collision、sorted compact UTF-8 与 integer-only 行为一致；当前 1397.5 MB bundle 已包含修复源码并通过标准 smoke、STT 与 B02 runtime import。 |
| Scope boundary | `student-tutor-runtime` 继续为 candidate/not-ready；B02 没有接入 provider、L-2 graph、Web Tutor scene 或 Gateway Tutor。 |

## Independent verification

- Core remediation evidence：**350 passed / 0 failed**；reviewer 最终复跑 BundleV2
  **34 passed**、TutorRuntimeStore **17 passed**。
- Desktop Python focused gate：**17 passed**；reviewer 复审阶段另运行相关桌面 Python
  **26 passed**。
- Rust study gate：**13 passed**，包含 Python/Rust shared Unicode canonical fixture。
- R5 原始复现已变为 `runtime_outbox_event_id_mismatch`，目标 runtime 完全不变，
  projector 最终只产生一条 terminal history。
- 用户完成当前 bundle 验证：标准 smoke、STT binaries、B02 Tutor runtime 成品导入全部通过。
- `BUNDLE_INFO.json`：CPython 3.11.15，`bundleSizeMb=1397.5`，
  `builtAt=2026-07-23T15:33:08.4508686+08:00`，文件更新时间 15:33:16。
- Reviewer 复核 bundle 中 `learning_recovery.py` 与
  `kabuqina/learning/tutor_runtime_store.py` 的 SHA-256 分别与当前源码一致；嵌入式
  `python.exe` 导入 recovery、runtime store 与 composite data service 通过。
- `git diff --check`：通过。

## Findings closure

| Finding | 结论 |
|---|---|
| `B02-R1` P1 | CLOSED：只恢复 dead writer；production startup-before-handshake 与 periodic runner 已接线。 |
| `B02-R2` P1 | CLOSED：pending read → learning insert → runtime ack 共用 owner-wide guard。 |
| `B02-R3` P1 | CLOSED：runtime merge 按 existing + actual-new 执行组合配额并整批 rollback。 |
| `B02-R4` P2 | CLOSED：Python/Rust 使用同一 canonical hash 合同和共享 fixture。 |
| `B02-R5` P1 | CLOSED：stable terminal event identity 与 run/type/time/payload provenance fail closed。 |

无未关闭的 B02 actionable finding。

## Accepted deferrals

- 全量 Python repository gate 的既有 C0 generated-ledger baseline 项归 C 轨 shared-spine
  owner；B02 focused tests 与当前成品验证均通过，因此不作为本卡 blocker。
- L-2 Tutor graph/provider、start/resume 真正激活及对应 E2E 属于 `CTL-B03`。
- 正式 MSI、安装/升级/降级、provenance 与最终发布许可仍属于
  `CTL-G01/Q-3 → Q-4`。

**Gate effect：** `CTL-B03` 的 `CTL-B02 DONE` 前置条件已满足，可转为 `READY`。这不等于
自动启动 B03；仍须用户明确授权并建立独立 JIT plan、base、允许路径、测试和回滚边界。
本签字不是 v0.5 release approval。
