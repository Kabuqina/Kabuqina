# CTL-C06 + CTL-C07 · retained readiness and legacy upgrade combined sign-off

> **结论：APPROVED / `CTL-C06 DONE` / `CTL-C07 DONE`。**
>
> C06 retained-platform readiness 与 C07 legacy upgrade 已按一个 C-track
> 工作包完成合并复审，不再拆独立 Gate。
>
> **Reviewer：Codex（本 task reviewer）**
>
> **签字时间：2026-07-26，Asia/Shanghai**
>
> **复审基线：** `5ef27dbf987d0fc301725d8364e184649f92ca83`
> （review fixes `27afc1d8`；DingTalk bundle fix `5ef27dbf`）。

## 合并 Review 结论

| 复审项 | 结论 |
|---|---|
| Retained runtime | Mainland/SEA profile allowlists 保持 exact；DingTalk SDK/Tea closure、bundled Node 24.18.0、WhatsApp bridge/session/cache、QQ voice degradation 均满足实现契约。 |
| Legacy safety | Twilio retained credentials 不被清理；export snapshot 与 cleanup target 哈希一致后才允许 mutation；无 silent reroute、目录 walk 或 recursive delete。 |
| Stateful compatibility | sole legacy WhatsApp session 原子迁移，coexistence 时 canonical-only write；unknown session platform 保留原文；old cron target 保持 visible/editable/deletable 并 fail as `unsupported_delivery`。 |
| UX contract | Settings 使用真实组件测试覆盖 export、confirm/cancel、backend failure 与 stale-export invalidation；旧 Feishu/WeCom routes 为 tombstone。 |
| Review blocker closure | 首轮 5 项 finding——Twilio ownership、export TOCTOU、WhatsApp writable legacy path、QQ partial WAV、source-regex Web test——全部关闭。 |

## Reviewer verification

- owner `main` bundle：`verified: true`，built at
  `2026-07-26T15:23:15.1526990+08:00`。
- bundle input/inventory SHA-256 与 success marker 一致。
- bundled Node：`v24.18.0`；Node executable/license hashes 已入 inventory。
- bundled DingTalk：`dingtalk_stream-0.24.3.dist-info` 与 Alibaba SDK/Tea
  modules 存在。
- reviewer 定向 Python bundle/import/inventory contracts：**26 passed**。
- reviewer Web legacy-upgrade component tests：**3 passed**。
- review delta `git diff --check`：通过。
- Core 大型/并发测试未在本次 reviewer session 重跑；采用 evidence 中已记录
  的分组单进程结果，避免重复触发已知 Codex 稳定性问题。

## 延迟 smoke 决定

六个 retained platforms 的真实账号、设备与 profile-isolation smoke 需要
A-track 完成后的真实产品 surface。owner 决定将该矩阵延迟到 A-track 完成后：

- 当前状态是 **DEFERRED**，不是测试 PASS；
- 不阻塞 C06+C07 implementation/bundle sign-off；
- 必须在 v0.5 release approval 前补录 timestamp、profile 与 sanitized result；
- 若 smoke 暴露实现缺陷，按普通 regression 修复，不重新恢复逐 leaf Gate。

## 管理效果

1. `CTL-C06` 与 `CTL-C07` 同时标记 `DONE`。
2. C-track 不再为两者保留 REVIEW 状态或重复 bundle 循环。
3. post-A real-account smoke 明确转交 `CTL-G01/Q-2 → Q-3`：Q-2 跑完整矩阵，
   Q-3 做最终制品级确认。
4. CTL-G01/Q 继续负责 installer/updater provenance、installed-app upgrade、
   clean/install/uninstall、advisory/license reconciliation 与最终 release
   decision；本签字不是 v0.5 release approval。
