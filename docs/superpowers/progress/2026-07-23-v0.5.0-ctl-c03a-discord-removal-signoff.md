# CTL-C03a · Discord removal Gate sign-off

> **结论：APPROVED / `CTL-C03a DONE`。** Discord owned leaf 已完成物理删除，
> 首次 Gate review 的 fresh-install、autostart、retained voice coverage 与
> closeout evidence 阻断均已关闭。
>
> **Independent Gate reviewer：Codex（本 task reviewer）**
>
> **签字时间：2026-07-23，Asia/Shanghai**
>
> **复审基线：** `3292a7af85755fcbc1028d3141627f57d6bf519f`
> （implementation base；sign-off 文档提交不改变本次受审实现）。

## Gate 结论

| 复审项 | 结论 |
|---|---|
| Owned leaf 与依赖 | Discord adapter、direct-send/admin tool、voice doctor、Discord-only tests/stubs 与 active messaging route 已删除；`discord.py[voice]` 及其 orphan lock/runtime packages 不再存在。 |
| Fresh-install / autostart | `cli-config.yaml.example` 不再生成 Discord platform/toolset state，active Core README 不再宣传该 adapter；Windows/POSIX installer 不再把旧 `DISCORD_BOT_TOKEN` 计入 gateway start eligibility。 |
| Retained shared behavior | generic media、TTS、voice-mode、session/thread 与 retained platform contracts 保留；voice config/default sync、Base per-chat gate、runner streaming/dedup、voice routing/filename/cleanup 均有 Telegram 或 neutral behavior tests。 |
| Compatibility / data safety | `Platform.DISCORD`、removed-target denial、stale-config filtering 和旧持久化文本仅用于兼容；不会注册 adapter、联网或静默改投。既有 credential、profile、session、job、channel directory 与 home target 未删除。 |
| Integration boundary | 分支已合并当前 `main`，merge-base 为 `3a241d43fbfd8ff58c0ed5d360f7bb0e7bbf1aa6`；C03a 没有宣称 C03b/C03c/C04/C05、integrated bundle 或 release gate 完成。 |

## Independent verification

- Gate-focused Core contracts：**25 passed**。
- canonical retained media/voice suite：**248 passed, 2 skipped**。
- runtime-pruned verifier unit suite：**5 passed**。
- `uv lock --check`：resolved **265 packages**，通过。
- checked runtime pruning：通过。
- PowerShell installer parse 与 committed POSIX installer `bash -n`：通过。
- C0 no-refresh platform manifest audit：25 surfaces、23 classified platforms、
  owned-source unknown 0、`gate_ready=true`。
- `git diff --check main..HEAD`：通过；复审结束时 worktree 洁净。

沙箱内首次测试尝试因 Windows 临时目录 ACL 在 fixture/断言前失败；reviewer
随后在同一源码与 canonical runner 下于沙箱外重跑，得到上述成功结果。ACL
失败不计作产品测试失败，也未被伪记为成功。

## Accepted deferrals

- `scripts/hermes-gateway` help/systemd 示例与网站散落说明仍可能包含 Discord
  文案。它们不恢复 adapter、import、active messaging route 或 autostart，
  Core help/registry 与全站 active-doc negative scan 按控制板归 **CTL-C05**
  统一收口。
- **CTL-C03b/C03c/C04** 继续独立处理其他 removed platform leaves。
- **CTL-C07** 负责旧 credential、profile、session、job、channel-directory、
  home target 的显式清理与升级矩阵。
- 当前 fresh bundle 证明 C03a package/runtime absence，但不是 merged HEAD 的
  integrated release artifact；integrated build provenance、installer/updater、
  stability window 与 release approval 仍属于 **CTL-G01/Q-3 → Q-4**。

**Gate effect：** `CTL-C03a` 可合并并标记 `DONE`。本签字仅批准 Discord
removal leaf，不是 v0.5 release approval，也不解除后续 C-track controls。
