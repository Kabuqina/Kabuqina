# CTL-C03c + CTL-C04 · removed-platform combined Review sign-off

> **结论：APPROVED / `CTL-C03c DONE` / `CTL-C04 DONE`。**
>
> WeCom bot/callback 与 C04 long-tail families 已作为同一组“removed
> platform convergence”变更完成合并 Review。owner 已取消逐 leaf 独立
> Gate 与重复 bundle 循环；本签字不再拆成两份 Gate 文档。
>
> **Reviewer：Codex（本 task reviewer）**
>
> **签字时间：2026-07-25，Asia/Shanghai**
>
> **复审基线：** `f7c72c04e4e4f61c6d24ca56f8c5492908ed359c`
> （C03c merge `f7064124`；C04 merge `f9ed9507`；review fix
> `4fcdab29`）。

## 合并 Review 结论

| 复审项 | 结论 |
|---|---|
| C03c owned surface | WeCom bot/callback adapters、callback crypto、QR worker、Rust child/commands、Web route/settings/onboarding producers、direct-send/toolset/config branches与专属测试/active docs 已删除。Weixin QR/account/media/crypto 与共享依赖保持。 |
| C04 owned surface | SMS/Twilio、Slack、Signal、Matrix、Mattermost、BlueBubbles、Home Assistant、Yuanbao、Webhook、API Server 及 bundled IRC/Teams platform plugins 已物理删除；平台专属 tools/extras/dependencies 与 active docs 已收口。 |
| Fixed retained set | adapter construction、config、toolsets、cron delivery、direct send、channel discovery 与 desktop profile boundary 只允许 Weixin、QQBot、DingTalk、Telegram、WhatsApp、Email。动态 platform registration/override 失败关闭。 |
| Compatibility / data safety | removed enum/config tokens 仅用于读取旧状态并拒绝，不注册 adapter、不扩网、不静默改投。旧 credentials、profiles、sessions、jobs、channel-directory 与 home targets 未被 destructive cleanup；显式清理仍属于 C07。 |
| Review blocker closure | stale channel directory 改为 exact retained allowlist；Email advanced settings 改用 `EMAIL_HOME_ADDRESS`，Rust upsert/remove 均执行相同 profile write guard；过期 Home Assistant/Teams manifest tests 改为 removal-negative contracts。 |

## Reviewer verification

- canonical Core focused regression：**253 passed**。
- Python bundle/runtime verifier + fixed manifest contracts：**18 passed**。
- Rust gateway/profile/env-key focused regression：**21 passed**。
  测试进程使用临时 `TAURI_CONFIG={"bundle":{"resources":[]}}`，只绕过
  bundled Python runtime 的 8,000+ resource enumeration；Rust source、
  features 与测试二进制均正常编译链接。
- Web gateway UX contract：通过。
- C0 no-refresh platform manifest audit：**25 surfaces**、**23 classified
  platforms**、**owned-source unknown 0**、`gate_ready=true`。
- review diff `git diff --check`：通过。

## 后续证据归属

- **CTL-C05** 统一处理 shared registry/profile/dependency/license/active-doc
  closeout，并消费一次 fresh integrated runtime/bundle 证明；不再为 C03c
  或 C04 重跑 leaf bundle。
- **CTL-C06** 证明六个 retained adapters 的 mainland/SEA release readiness；
  removal sign-off 不冒充 retained transport readiness。
- **CTL-C07** 处理旧 credential/profile/session/job/channel-directory/home
  的显式清理、可解释拒绝与 v0.4 upgrade matrix。
- **CTL-G01/Q-3 → Q-4** 处理最终 installer/updater provenance、稳定窗口与
  release decision。

## 管理效果

1. `CTL-C03c` 与 `CTL-C04` 同时标记 `DONE`。
2. `CTL-C05` 前置满足，转为 `READY`，成为 C 流下一张唯一可拉取卡。
3. C 流后续使用“阶段工作包 + 合并 Review”：
   - 不再为子 leaf 建独立 Gate/sign-off/bundle 循环；
   - C05/C06/C07 各自按工作包交付一份证据与一次 reviewer 结论；
   - fresh integrated bundle 只在 C05/G01 边界执行并复用。
4. 本签字批准 removed-platform source convergence，不是 v0.5 release
   approval。
