# CTL-C06 retained platform readiness evidence

**Status:** APPROVED · OWNER BUNDLE VERIFIED · REAL-ACCOUNT SMOKE DEFERRED POST-A
**Base:** `366e337c`
**Branch:** `codex/v050-c06-retained-readiness`
**Owner:** Codex / C integrator, authorized 2026-07-26

## Closed deterministic contracts

- The retained set is unchanged and profile-exact:
  - `mainland_cn`: Weixin, QQ Bot, DingTalk
  - `sea`: Telegram, WhatsApp, Email
- DingTalk is a real desktop dependency rather than an optional-import false
  green. Core, desktop requirements and the lock select
  `dingtalk-stream==0.24.3` plus
  `alibabacloud-dingtalk==2.2.42`; the retained runtime verifier requires
  their modules and DingTalk dist-info. Official 0.24.3 metadata has
  `websockets>=11.0.2` and no obsolete `<13` cap, so the existing locked
  `websockets==15.0.1` is compatible. The adapter reports connected only after
  the SDK exposes a live WebSocket, not merely after scheduling `start()`.
- WhatsApp no longer depends on an unversioned system Node in the desktop
  product. The build input pins the official Node.js 24.18.0 Windows x64
  archive and SHA-256, stages only `node.exe`, `LICENSE` and `VERSION`, and
  verifies the executable. Runtime inventory hashes the executable/license;
  npm remains build-time-only. The locked bridge install and `npm ls`
  validation use the same pinned archive's `npm.cmd`, and the reusable cache
  key/marker includes the Node version so native packages cannot be reused
  across incompatible Node ABIs.
- WhatsApp adapter, identity reader and bridge agree on
  `KABUQINA_HOME/platforms/whatsapp/session` and
  `KABUQINA_HOME/cache/{images,documents,audio}`. A sole legacy
  `whatsapp/session` tree is atomically renamed into the canonical location;
  when both trees exist, canonical is the only write target and legacy remains
  a read-only identity fallback.
- QQ voice conversion no longer wraps arbitrary compressed bytes as raw PCM.
  It uses QQ ASR, a real SILK/ffmpeg conversion, or the visible
  `[Voice] [语音识别失败]` degraded result. The gateway child resolves bundled
  `stt-bin/ffmpeg.exe` before an optional developer system fallback.
- The C0 environment audit now derives accesses, declarations and computed
  templates in one Python AST pass per tracked file. A behavioral regression
  prevents the three ledgers from silently returning to repeated full-tree
  parsing.

## Deterministic verification and integrated bundle

Codex did not start a second bundle during review. The owner ran the integrated
bundle once from `main`, and the reviewer inspected the resulting success
marker and inventory directly.

The first owner bundle attempt on `main` reached dependency installation but
failed before smoke verification. Alibaba's locked DingTalk closure contains
five pure-Python releases published only as sdists, while the cross-target
runtime install correctly enforces `--only-binary`. The build now creates
those exact wheels in a temporary directory, validates the complete set, and
atomically publishes it to the machine-wide cache for reuse by all worktrees.
The main pip step consumes that wheelhouse and exits immediately on failure,
so a resolver error can no longer be obscured by the secondary `yaml` import
error seen against the incomplete runtime. The subsequent owner bundle passed
all `-Verify` checks.

| Scope | Result |
|---|---:|
| Core retained mainland + Email adapter contracts (`dingtalk`, `email`, `qqbot`, `weixin`) | **252 passed** |
| Core WhatsApp contracts | **76 passed** |
| Core Telegram contracts | **309 passed** |
| WhatsApp canonical identity/session focus | **29 passed**, 46 deselected |
| Python runtime import/inventory contracts | **18 passed** |
| Post-failure DingTalk wheel-cache/fail-fast contracts | **26 passed**; Windows CPython 3.11 wheels-only full requirements dry-run **exit 0** |
| WhatsApp bridge allowlist/path contracts | **6 passed** |
| C0 manifest negative/unit contracts | **29 passed** |
| PowerShell parser, changed-Python compile, `git diff --check` | **passed** |

The retained adapter total is **637 passed** across three explicit
single-process groups. Tests were intentionally split and run with `-n 0`;
the earlier combined collection produced excessive terminal output and is not
used as evidence.

The refreshed C0 source audit reports `surfaces=25`,
`classified_platforms=23`, `owned_source_unknown=0`, `gate_ready=true`.

## Owner integrated bundle result

The successful `main` artifact records:

- `builtAt`: `2026-07-26T15:23:15.1526990+08:00`;
- `verified: true`, Python `3.11.15`, Node `24.18.0`;
- desktop requirements SHA-256
  `dc61a5d973b0dcd1e6ec28734c89b767dfb27ffbcb3e7fbb9fa18c130a4ac187`;
- dependency inventory SHA-256
  `baa18f88bd1e4add50609ae91e7dc620414735a509c82d9533fc1846451d463f`.

Reviewer inspection confirmed:

- `site-packages/dingtalk_stream` and the Alibaba DingTalk/Tea imports pass;
- exact `dingtalk_stream-0.24.3.dist-info` is present;
- `node/node.exe --version` returns `v24.18.0`;
- `node/LICENSE`, `node/VERSION` and bridge `runtime_paths.js` are present;
- `DEPENDENCY_INVENTORY.json.runtime_components` contains the Node executable
  and license hashes;
- `BUNDLE_INFO.json` records the Node version/archive SHA and
  `verified: true`.

The previously accepted rolling ffmpeg download warning is not closed by C06;
digest-pinned ffmpeg provenance remains G01/Q.

## Real-account smoke matrix

Missing credentials or devices are not a pass. By owner decision, these
external-account checks require the completed A-track product surface and are
therefore deferred until A-track completion. They do not block the C06+C07
implementation sign-off, but remain mandatory before v0.5 release approval.

| Profile/platform | Minimum smoke | Current status |
|---|---|---|
| mainland / Weixin | QR/account start; DM + group text; inbound/outbound image/file; restart; profile isolation | **DEFERRED — run after A-track completion** |
| mainland / QQ Bot | production or sandbox auth; DM/group/channel; media; voice success or visible degraded result; restart | **DEFERRED — run after A-track completion** |
| mainland / DingTalk | Stream credential handshake; receive + session-webhook reply; media/card path; failed credential must not report connected | **DEFERRED — run after A-track completion** |
| SEA / Telegram | polling or authenticated webhook; topic/DM; media; restart; no mainland child/network start | **DEFERRED — run after A-track completion** |
| SEA / WhatsApp | first pairing; restart without re-pair; receive/send text and media; canonical session/cache paths; no system Node dependency | **DEFERRED — run after A-track completion** |
| SEA / Email | password and OAuth2 variants as available; IMAP receive; SMTP reply/send; attachment; custom host; no mainland child/network start | **DEFERRED — run after A-track completion** |

## Gate boundary

C06 is `DONE` for implementation and integrated-bundle readiness under the
combined C06+C07 reviewer sign-off. The six-row real-account matrix is an
explicit deferred verification dependency on A-track completion; it must be
recorded by `CTL-G01/Q-2 → Q-3` before v0.5 release approval but does not
reopen this C-track code gate. G01/Q still owns installer provenance, external advisory review,
redistributed-license reconciliation and the final release decision.
