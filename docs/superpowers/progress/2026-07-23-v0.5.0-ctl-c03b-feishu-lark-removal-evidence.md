# CTL-C03b Feishu/Lark removal implementation evidence

**Control:** CTL-C03b / C-2 Feishu/Lark leaf

**Status:** `DONE` — the first release-operator bundle exposed stale in-place
runtime artifacts and failed pruning. The build cleanup/success-marker fix
`ce1218e4` and the release operator's successful second `-Verify` run were
independently reviewed at `b941c81e` and approved by the
[Gate sign-off](2026-07-23-v0.5.0-ctl-c03b-feishu-lark-removal-signoff.md).

**Implementation base:** `7d2c2e3fa81eb8dd1bc29aeccfdcb6973270b950`

**Current integrated base:** B02-signed `main` merged by `cedb78e9`

**Gate-fix implementation:** `a090ab1e`, `5086252e`, `ce1218e4`

## Current conclusion

The owned Feishu/Lark leaf is physically absent from Core, desktop Python,
Rust and Web. The adapter, comment automation, document/drive tools, QR
worker, Rust commands/child state, Web route/settings/onboarding producers,
Feishu-only tests and active messaging documentation route were removed.
Shared registries, direct send, CLI/toolset configuration and current product
copy no longer select or advertise the adapter.

The first Gate review found three implementation blockers. The fresh bundle
copy of the deleted QR worker is now gone. `lark-oapi` is removed from desktop
requirements, the Core extra, the `all` extra and the mechanically regenerated
lock. Runtime pruning rejects the worker, three adapter/comment modules, two
tool modules, `lark_oapi` and its dist-info. Source sync also removes those
artifacts from an existing developer runtime before running the verifier.
Legacy `feishu_doc` and `feishu_drive` toolset names are in the removed set, so
hand-edited or pre-C03b configs cannot re-enable them.

The first release-operator build then proved that an in-place rebuild could
silently retain old Core files when its best-effort directory removal did not
fully clear the runtime, and that root-level workers were outside the replaced
Core tree. It also wrote `BUNDLE_INFO.json` and printed `Bundle ready` before
smoke verification. `ce1218e4` makes these boundaries fail closed: bundled
Core removal is strict and retried, retired Core/root paths are explicitly
removed, old success metadata is invalidated before mutation, `-Verify` only
reuses a `verified: true` marker, and new success metadata is written only
after every requested verifier and STT check passes.

The release operator's second run exercised that corrected path successfully.
Runtime pruning, retained imports, profile imports, legacy identity, the
standard smoke test and STT binary verification all passed. The resulting
success marker records `verified=true`, and a post-build read-only scan found
none of the retired worker, adapter, comment, tool or `lark_oapi` artifacts.

Compatibility remains non-destructive. `Platform.FEISHU`, removed-target
delivery denial, stale config/env filtering and old persisted target text stay
only where required to read, list, edit, delete or fail closed. No
`FEISHU_*` credential, profile, QR/comment file, session, job, channel
directory or home target is deleted or rerouted; CTL-C07 owns that cleanup.

## Commit slices

| slice | commit(s) | result |
|---|---|---|
| JIT + retained boundary | `aea11a20`, `7305f656` | boundary frozen; shared tests no longer depend on the Feishu adapter |
| owned leaf deletion | `284ff7f3` | Core adapter/comment/tools, desktop worker/Rust/Web leaf and Feishu-only tests removed |
| shared spine + active docs | `2ace0dee`, `2d0362f8`, `cf1dac9b`, `292fb246`..`9b5418b3` | imports, registries, routes, producers and current product copy retired |
| signed B02/main integration | `cedb78e9` | B02 `DONE` main merged; control board preserves B02 truth and C03b `ACTIVE` |
| first Gate blocker closure | `a090ab1e`, `5086252e` | bundle copy, dependencies/lock, runtime cleanup/verifier, legacy toolsets, negative source contracts and C0 ledger closed |
| first operator bundle failure closure | `ce1218e4` | strict stale-tree/root cleanup; pruning defense for Discord/Feishu; verified-only, post-smoke success marker |

## First Gate findings and closure

| finding | closure | state |
|---|---|---|
| P1 fresh bundle unconditionally copied deleted `feishu_qr_worker.py` | removed the `Copy-Item`; strict cleanup now also retires pre-existing root worker state | **closed** by successful second operator bundle and explicit absence scan |
| P1 Lark dependency/runtime removal incomplete | removed desktop/Core declarations, regenerated lock, added source-sync cleanup and fail-closed runtime paths/packages | **closed**; lock proof and fresh verified runtime absence both passed |
| P2 old `feishu_doc` / `feishu_drive` config remained enabled | both names added to `_REMOVED_TOOLSETS`; negative platform and pre-C03b config tests added | passed |
| Gate material incomplete / B02 integration pending | signed B02 `main` merged; C0 generated ledgers refreshed; manifest dependency records and this evidence updated | implementation and bundle evidence complete; independent reviewer sign-off pending |
| failed verifier could leave a reusable success marker | invalidate marker before rebuild; write `verified: true` only after pruning/import/profile/legacy/STT verification | **closed**; second run emitted the marker only after all requested verification passed |

## Verification completed

| check | result |
|---|---|
| `python -m unittest python.tests.test_runtime_pruned_verifier -v` | **8 passed** |
| build console/cleanup contracts | **6 passed**; combined build + pruning suite **14 passed** after `ce1218e4` |
| Core `tests/kabuqina_cli/test_tools_config.py` with the canonical runner's hermetic env/flags and four workers | **53 passed** |
| integrated retained shared-spine suite (15 files) | **466 passed, 5 skipped**; one Windows EOF child exceeded its 5-second deadline |
| EOF child follow-up | first isolated C03b rerun also timed out; canonical `main` passed 1/1 and the final isolated C03b rerun passed 1/1. Related C03b diffs are comment-only; the non-clean aggregate run is disclosed rather than counted as fully green |
| canonical Core wrapper availability | worktree-local `.venv` absent, so the wrapper correctly refused; the repository's existing Core venv was invoked with the wrapper's exact env isolation, ignores, marker, basetemp and `-n 4` flags |
| `uv lock` / `uv lock --check` | resolved **264 packages**; `lark-oapi 1.5.3` removed; check passed |
| PowerShell parser over `python/build_bundle.ps1` and `scripts/sync-runtime-sources.ps1` | passed |
| `scripts/sync-runtime-sources.ps1` | pruning, runtime imports, profile imports and legacy identity passed |
| existing runtime artifact scan after source sync | no Feishu worker/adapter path, `lark_oapi` package or dist-info |
| C0 generated-ledger refresh + contract audit | references 617; 25 surfaces; 23 classified platforms; owned-source unknown 0; `gate_ready=true` |
| `git diff --check` before the Gate-fix commit | passed; only checkout LF/CRLF warnings |
| release-operator `.\python\build_bundle.ps1 -Verify` rerun | **passed**: runtime pruning, retained imports, profile imports, legacy identity, smoke test and STT binaries |
| successful bundled-runtime identity | Python 3.11.15; PBS 20260414; frozen Core `90b304b7c`; `verified=true`; `builtAt=2026-07-23T21:47:16.1093249+08:00`; 1,372.9 MB |
| post-build retired-artifact scan | worker, adapter/comment/tool paths, `lark_oapi` package and `lark_oapi-*.dist-info` all absent |

The assistant started a fresh bundle before the operator reserved that step,
then stopped it immediately on instruction while it was still preparing the
bundled Python/pip layer. It produced no completed verification result and is
not counted as evidence.

## First release-operator bundle result

The operator ran `.\python\build_bundle.ps1 -Verify` in the correct C03b
worktree. Dependency installation completed and the script produced a
1,373.2 MB runtime with `builtAt=2026-07-23T18:31:27.7507863+08:00`, but the
pruning verifier correctly failed on:

- `kabuqina/gateway/platforms/discord.py`;
- `feishu_qr_worker.py`;
- `kabuqina/gateway/platforms/feishu.py`;
- `kabuqina/gateway/platforms/feishu_comment.py`;
- `kabuqina/gateway/platforms/feishu_comment_rules.py`.

This is a failed Gate artifact, not fresh-runtime absence evidence. The files
had old July 14–16 timestamps while current copied files matched the C03b
source, proving an in-place stale-tree merge rather than source resurrection.
The pre-fix script's `Bundle ready` line and `BUNDLE_INFO.json` are likewise
not success evidence. The marker has no `verified: true` field, so the fixed
script will reject it and rebuild normally.

## Second release-operator bundle result

The operator reran `.\python\build_bundle.ps1 -Verify` in the same C03b
worktree after `ce1218e4`. The terminal result reported:

```text
runtime pruning ok
runtime imports ok
profile platform imports ok
legacy runtime identity ok
smoke test passed
STT binaries OK
Bundle ready ... (1372.9 MB)
```

The post-verification `python/dist/runtime/BUNDLE_INFO.json` is:

```json
{
  "pythonVersion": "3.11.15",
  "pbsRelease": "20260414",
  "frozenCommit": "90b304b7c (v2026.4.23 — frozen upstream snapshot)",
  "verified": true,
  "builtAt": "2026-07-23T21:47:16.1093249+08:00",
  "bundleSizeMb": 1372.9
}
```

A separate read-only check of the completed runtime confirmed absence of:

- `feishu_qr_worker.py`;
- `kabuqina/gateway/platforms/feishu.py`;
- `kabuqina/gateway/platforms/feishu_comment.py`;
- `kabuqina/gateway/platforms/feishu_comment_rules.py`;
- `kabuqina/tools/feishu_doc_tool.py`;
- `kabuqina/tools/feishu_drive_tool.py`;
- `site-packages/lark_oapi`;
- `site-packages/lark_oapi-*.dist-info`.

The release-operator bundle requirement is therefore satisfied. CTL-C03b
received independent Gate approval at `b941c81e`.

## Review/sign-off

Independent reviewer verification confirmed the `verified=true` bundle marker,
the correct root-level `site-packages` absence boundary, runtime pruning,
retained imports, profile imports, legacy identity, C0 contract audit and the
fail-closed bundle contracts. No actionable finding remains.

[Gate sign-off](2026-07-23-v0.5.0-ctl-c03b-feishu-lark-removal-signoff.md)
records the approved baseline, independent checks and accepted deferrals.

## Deferred controls

- CTL-C03c/C04 own WeCom and other long-tail leaves.
- CTL-C05 consumes the dependency, license, advisory and active-doc removal
  delta.
- CTL-C07 owns explicit legacy credential/profile/job/session/home cleanup and
  the v0.4 upgrade matrix.
- CTL-G01/Q-3 → Q-4 own integrated installer/updater provenance, stability and
  release approval.
