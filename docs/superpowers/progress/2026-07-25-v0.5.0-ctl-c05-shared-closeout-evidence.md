# CTL-C05 shared single-truth closeout evidence

**Status:** DONE · OWNER BUNDLE PASSED · REVIEWER SIGN-OFF 2026-07-26
**Base:** `4005b2c3`
**Branch:** `codex/v050-c05-shared-closeout`
**Owner:** Codex / C integrator, authorized 2026-07-25

## Closed source contracts

- Exact retained set remains:
  - `mainland_cn`: Weixin, QQBot, DingTalk
  - `sea`: Telegram, WhatsApp, Email
- Desktop requirements now directly declare retained imports:
  `aiohttp`, `certifi`, `cryptography`, `qrcode`, and
  `python-telegram-bot[webhooks]`.
- DingTalk SDKs remain intentionally outside the desktop payload until C06
  resolves their modern-`websockets` compatibility. The adapter import does
  not count as release-readiness evidence.
- WhatsApp bridge source and lock-resolved `node_modules` are staged at build
  time. Runtime `npm install` is prohibited. Git inputs use pinned HTTPS
  commits rather than SSH URLs.
- Pinned CPython/Whisper downloads and the WhatsApp install use a machine-wide
  cache keyed by asset or lock identity, so a new worktree does not download
  the same inputs again. WhatsApp cache publication uses a unique temporary directory, full
  `npm ls --omit=dev --all` validation and a completion marker included in the
  final directory rename; an interrupted or marker-less install is invalid.
- `tauri/Cargo.lock` is tracked. Every bundle emits
  `DEPENDENCY_INVENTORY.json`, and `BUNDLE_INFO.json` records hashes for the
  inventory, desktop requirements, WhatsApp lock, and Cargo lock.
- Runtime verification requires all retained Python distributions and the
  complete direct WhatsApp bridge payload. Optional-import fallthrough can no
  longer turn a missing SDK into a passing smoke test.
- Active product documentation is checked against the C0 manifest and exposes
  only the six retained platform pages/keys/toolsets/delivery targets. Removed
  platform display aliases include English and Chinese names; behavior tests
  prove that `Feishu`/`飞书`, WeCom aliases and `Webhook` are rejected. Every
  manifest-classified `docs/test-cases/**` active document is discovered
  automatically; the audit has no second per-test-file allowlist.

## Non-bundle verification

- Focused C05 Python contracts: **31 passed**
  - runtime/profile verifier
  - dependency inventory and tracked lock
  - active documentation audit
  - build console/success-marker contract
  - gateway environment/profile host policy
- Earlier C05 baseline:
  - Python profile/runtime/manifest focus: **64 passed**
  - Core delivery/config/channel-directory focus: **65 passed**
- PowerShell parser: `python/build_bundle.ps1` and
  `scripts/sync-runtime-sources.ps1` passed.
- `cargo metadata --locked --offline --no-deps`: passed.
- Active platform documentation audit: retained `6`, removed advertisements
  `0`.
- Refreshed C0 manifest audit: `surfaces=25`, `classified_platforms=23`,
  `owned_source_unknown=0`, `gate_ready=true`.

No bundle was executed by Codex. A parallel C0 scan plus `pytest -n 4` was
observed to destabilize Codex Desktop; remaining validation is intentionally
serialized and single-process.

## Owner integrated bundle and sign-off

The owner ran `python/build_bundle.ps1 -Verify` from merged `main`. The
completed artifact at `python/dist/runtime` reports:

- `builtAt`: `2026-07-25T21:59:23.7152596+08:00`;
- `verified`: `true`;
- size: `1424.0 MB`;
- dependency inventory: Python `165`, Node `167`;
- runtime pruning, retained imports, profile isolation, legacy identity and
  STT binary checks: passed.

The owner completed reviewer sign-off on 2026-07-26 after the active-doc and
WhatsApp cache review blockers were closed. C05 is `DONE`; the verified runtime
may be reused by G01 while its hashed bundle inputs remain unchanged.

The pre-existing rolling ffmpeg URL is not digest-pinned and emitted a warning
during the successful build. This does not invalidate the accepted C05
functional/runtime gate. Digest-pinned ffmpeg provenance remains an explicit
G01/Q supply-chain item and must be closed before final release approval.

## Explicit deferrals

- **C06:** DingTalk SDK/`websockets` selection, system/bundled Node ownership,
  real credentials and transport smoke for all six retained adapters, QQ audio
  degradation, and WhatsApp canonical session/profile path repair.
- **C07:** legacy credentials, jobs, home-channel values, sessions and profile
  cleanup/upgrade; no destructive migration is performed here.
- **G01/Q:** ffmpeg digest-pinned provenance, fresh external Python/npm/Cargo
  advisory scan, redistributed-file notice reconciliation,
  installer/upgrade/provenance and release decision.

## Owner-run integrated bundle

After this source stack is reviewed and merged to `main`, run once:

```powershell
cd D:\project\Kabuqina
.\python\build_bundle.ps1 -Verify
```

Acceptance requires:

- `smoke test passed` and `STT binaries OK`;
- no removed source/package residual;
- retained Python modules and dist-info present;
- `kabuqina/scripts/whatsapp-bridge` contains the locked source plus all direct
  `node_modules` packages;
- `DEPENDENCY_INVENTORY.json` exists;
- `BUNDLE_INFO.json` has `verified: true` and all four C05 dependency hashes.

Record that one runtime for C05 and reuse it in G01 unless a dependency or
bundle input changes.
