# CTL-C03b · Feishu/Lark removal Gate sign-off

> **结论：APPROVED / `CTL-C03b DONE`。** Feishu/Lark owned leaf 已完成物理
> 删除；首次 Gate review 的 bundle copy、dependency/runtime、legacy toolset
> 与 closeout evidence 阻断，以及首次 operator bundle 暴露的 stale-tree /
> success-marker 问题均已关闭。
>
> **Independent Gate reviewer：Codex（本 task reviewer）**
>
> **签字时间：2026-07-23，Asia/Shanghai**
>
> **复审基线：** `b941c81e07c6fffe50bc8aa6208e9d26d5937193`
> （implementation + operator evidence base；sign-off 文档提交不改变本次
> 受审实现）。

## Gate 结论

| 复审项 | 结论 |
|---|---|
| Owned leaf 与依赖 | Feishu adapter、comment automation、document/drive tools、QR worker、Rust commands/child state、Web route/settings/onboarding producers 与 Feishu-only tests 已删除；desktop/Core declarations、`all` extra、lock 与 fresh runtime 均不再包含 `lark-oapi/lark_oapi`。 |
| Fresh bundle / fail-closed | `ce1218e4` 严格替换 bundled Core、退休 root worker 与明确 removed paths；旧 success marker 在 mutation 前失效，`-Verify` 只复用 `verified=true` marker，新 marker 仅在 pruning/import/profile/legacy/STT verification 全过后写入。 |
| Runtime absence | operator 第二次 `build_bundle.ps1 -Verify` 产生 Python 3.11.15、1,372.9 MB runtime，`builtAt=2026-07-23T21:47:16.1093249+08:00`、`verified=true`；worker、adapter/comment/tool paths、根级 `site-packages/lark_oapi` 与 dist-info 全部不存在。 |
| Compatibility / data safety | `Platform.FEISHU`、removed-target denial、stale config/env stripping、cron/session/prompt legacy text 仅用于兼容和 fail-closed；不会注册 adapter、联网或静默改投。既有 credentials、profiles、QR/comment state、sessions、jobs、channel directory 与 home targets 未删除。 |
| Integration boundary | signed B02 `main` 已通过 `cedb78e9` 合并；C03b 没有宣称 C03c/C04/C05/C07、installer/updater provenance、stability window 或 v0.5 release gate 完成。 |

## Independent verification

- bundle console/cleanup 与 runtime-pruned contracts：**14 passed**。
- source/profile/gateway/runtime focused Python contracts：**40 passed**。
- fresh runtime pruning：通过；正确的根级 `site-packages` absence 已独立检查。
- bundled retained imports、profile platform imports、legacy runtime identity：
  全部通过。
- Core evidence：tools-config **53 passed**；retained shared-spine suite
  **466 passed, 5 skipped**。已披露的 Windows EOF timing case 在 isolated
  C03b rerun通过，相关 C03b diff 仅为 comment。
- `uv lock --check`：resolved **264 packages**，无 `lark-oapi/lark_oapi`。
- C0 no-refresh platform manifest audit：25 surfaces、23 classified platforms、
  owned-source unknown 0、`gate_ready=true`。
- `git diff --check`：通过；签字前 worktree 洁净。

成功证据最初把手工扫描位置写成 embedded Python 的
`python/Lib/site-packages`；reviewer 已同时检查实际依赖位置
`runtime/site-packages` 并在签字提交中校正文档。自动 pruning verifier
本来就检查正确的根级位置，因此这是一处证据路径笔误，不是 runtime
coverage 缺口。

## Accepted deferrals

- compatibility-only `Platform.FEISHU`、cron/session/prompt text 与旧 persisted
  targets 保留到需要读取、编辑、删除或明确拒绝的边界，不恢复 adapter 或
  network path。
- **CTL-C03c/C04** 继续独立处理 WeCom 与其他 long-tail leaves。
- **CTL-C05** 统一收口 dependency/license/advisory 与 remaining active-doc
  removal delta。
- **CTL-C07** 负责旧 credentials、profiles、QR/comment state、sessions、
  jobs、channel-directory 与 home-target 清理及 v0.4 upgrade matrix。
- 当前 fresh bundle 证明 C03b package/runtime absence，但不是最终 merged
  release artifact；installer/updater provenance、integrated stability 与
  release approval 仍属于 **CTL-G01/Q-3 → Q-4**。

**Gate effect：** `CTL-C03b` 可合并并标记 `DONE`。本签字仅批准
Feishu/Lark removal leaf，不是 v0.5 release approval，也不解除后续
C-track controls。
