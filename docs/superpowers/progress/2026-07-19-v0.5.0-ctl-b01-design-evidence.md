# CTL-B01 · Tutor runtime design evidence

> **状态：** submitted for non-author review
>
> **Base：** `e5c020f885dc041f73cd00bd841cf3c9aaa747e5`
>
> **Branch / worktree：** `codex/v050-b01-tutor-design` /
> `D:\project\Kabuqina\.worktrees\b-track-b01`

## 1. 交付物

- [L-0 Tutor graph 实施设计](../plans/2026-07-19-v0.5.0-ctl-b01-tutor-runtime-design.md)
- `DECISIONS.md` 的 v0.5 Tutor runtime L-0 锁定项
- 执行总控板 `CTL-B01` 的 REVIEW 状态与 owner/handoff

本切片没有修改 runtime、schema、bundle、Web 或 Tutor hot path。

## 2. Observed-source evidence

| 事实 | 核验来源 | 设计影响 |
|---|---|---|
| 普通 Agent graph 编译时无 checkpointer | `hermes_core/agent/graph_engine/engine.py`、`builder.py` | 新 Tutor graph 独立；不扩展 `TurnState` |
| provider 路由已有集中 `call_llm` | `hermes_core/providers/chat_completions.py` | 新增窄 `TutorModelPort`，不再造 client/router |
| `learning.db` 已有 WAL、busy retry、secure delete、owner/space scope | `hermes_core/learning/learning_store.py` | v2 adapter 延续同一 DB/transaction 纪律 |
| `learning_activities` 是 append-only event | 同上 schema 与 `insert_activity()` | 新建 run/checkpoint 表，不改旧读者语义 |
| owner 由 desktop runtime 注入 | `python/src/learning_owner.py`、Study routes | public wire 不接受 owner id |
| Study Rust bridge 已独立 | `tauri/src/study.rs` | L-1 增加同族 command，不在 Web 直连 Python |
| 现有 `/study/activities` 是历史摘要 | `python/src/desk_server/routes/study_routes.py` | 新 API 使用 `/activity-runs`，不破坏旧 endpoint |

## 3. 合同覆盖对照

| CTL-B01 退出项 | 设计位置 | 状态 |
|---|---|---|
| start/explain/check/interrupt/resume/cancel/terminal | §3–§5 | frozen |
| owner/space checkpoint、revision/CAS | §3、§4、§6 | frozen |
| activity lifecycle、delete/restart | §4、§6.3 | frozen |
| ordinary Chat / explicit Tutor 边界 | §0、§2、§5 | frozen |
| deterministic truth / LLM draft trust | §5.3、§8 | frozen |
| prompt injection、retention、redaction | §5.3、§6.3、§8 | frozen |
| compression/cost/failure degradation | §5.1、§7 | frozen for L-2 boundary; H4 stays L-5 |
| L-1/L-2 JIT split、tests、hot-file owner | §10–§11 | frozen |

## 4. 验证

本切片的验证只检查文档、链接、状态与 whitespace；runtime tests 留给 L-1/L-2。

```powershell
git diff --check
rg -n "CTL-B01|TutorGraph|LearningCheckpointStore|waiting_for_learner|interrupted" `
  docs/superpowers/plans/2026-07-19-v0.5.0-ctl-b01-tutor-runtime-design.md `
  docs/superpowers/plans/2026-07-18-v0.5.0-execution-control-board.md `
  DECISIONS.md
```

## 5. Gate 与后续动作

- 当前只能标记 `REVIEW`，作者不能自报 `DONE`。
- 非作者 reviewer 依据设计 §12 签字；如有合同变更，先修订设计与 decision。
- reviewer 通过后，`CTL-B01 DONE`；随后单独建立 `CTL-B02 / L-1` JIT plan。
- `CTL-B02` 不得从主工作区 C 轨 dirty tree 复制文件；从记录的固定 commit/rebase 起步。
