# CTL-C04 long-tail platform removal evidence

**Control:** CTL-C04 / C-3
**Status:** REVIEW
**Date:** 2026-07-24
**Base:** `f7064124`

## Implemented boundary

The C04a-e families were closed as one integration batch:

- deleted 14 adapter/source modules for SMS, Slack, Signal, Matrix,
  Mattermost, BlueBubbles, Home Assistant, Yuanbao, Webhook and API Server;
- deleted the Home Assistant and Yuanbao platform tools;
- deleted bundled IRC and Teams platform plugins;
- removed Slack/Matrix/Home Assistant/SMS platform extras and regenerated
  `hermes_core/uv.lock`;
- reduced adapter construction, CLI/platform lists, channel discovery,
  direct send, cron home delivery and display defaults to retained adapters;
- rejected unknown platform enum values, dynamic plugin registration and
  plugin override;
- made legacy removed config/toolset inputs fail closed;
- added runtime-pruning checks for C04 source, tools and exclusive packages.

Compatibility-only enum tokens and old persisted data are not treated as live
platform support. No credential, session, job or home-channel data is deleted.

## Verification results

| Check | Result |
|---|---|
| affected Core regression (37 files) | **1294 passed, 6 skipped**; one pre-existing MCP mock coroutine warning |
| focused post-fix rerun | **3 passed** |
| Python build/runtime/profile/manifest focus | **49 passed** |
| C0 generated-ledger refresh | active references **521**, environment keys **660**, namespaces **26**, dynamic templates **2** |
| C0 no-refresh audit | **passed**; surfaces **25**, classified platforms **23**, owned source unknown **0**, `gate_ready=true` |
| syntax/source hygiene | `compileall` passed; `git diff --check` passed; removed tracked source absent |

An exploratory full Core run was stopped after roughly 78% because the
remaining unrelated slow suite was not a C04 release gate. Its C04-relevant
failures were all obsolete positive samples for removed platforms; the
complete affected-file regression above was then rerun green. A Python
all-tests exploration was likewise stopped when it entered repeated C0
repository scans; the official C0 audit and focused Python contract suite were
run after ledger refresh.

## Deferred gates

- No branch bundle was run, per owner instruction. C05/G01 owns fresh
  integrated runtime and distribution evidence.
- C05 owns final shared dependency/license/SBOM and single-truth closeout.
- C06 owns retained mainland/SEA release readiness.
- C07 owns explicit legacy credential/job/home cleanup and upgrade behavior.

C04 is ready for independent review; it does not claim sign-off or permission
to merge to `main`.
