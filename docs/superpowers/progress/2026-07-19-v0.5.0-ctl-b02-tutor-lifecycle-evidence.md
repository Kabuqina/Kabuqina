# CTL-B02 · Tutor checkpoint/activity lifecycle implementation evidence

> **状态：** APPROVED / `CTL-B02 DONE`（2026-07-23）
>
> **固定 base：** `2c34a8f282b3266f825ff1b0013220071d890383`
>
> **Review remediation targets：** `27908527`（B02-R1/R2/R3）、
> `e64921b2`（B02-R4）、`731ae2ed`（R1 startup-order test）、
> `d125a4c8`（B02-R5）
>
> **Branch / worktree：** `codex/v050-b02-review-fixes` /
> `D:\project\Kabuqina`
>
> **Independent Gate sign-off：**
> [2026-07-23-v0.5.0-ctl-b02-tutor-lifecycle-signoff.md](2026-07-23-v0.5.0-ctl-b02-tutor-lifecycle-signoff.md)

非作者 Gate reviewer 已关闭 `B02-R1…R5`。用户于 2026-07-23 重建当前 Python bundle；
标准 smoke、STT binaries 与 B02 Tutor runtime 成品导入均通过。Reviewer 另行核对
`BUNDLE_INFO.json`、关键源码 SHA-256 与嵌入式 imports 后完成签字。正式 MSI、安装/升级/
降级与发布许可仍归 `CTL-G01/Q-3 → Q-4`，本卡不冒充 release artifact approval。

## 1. Commit map

| Task | Commit | Outcome |
|---|---|---|
| JIT plan / start | `1d928d6f`, `97aa2dcc` | 固定 base、路径、owner、TDD 与 stop rules |
| contracts / ports | `7060b1c8` | 四元 identity、wire validators、fingerprint、checkpoint port/fake |
| coordinator | `9c5b4a16` | durable cross-process fence、lease、phase journal、recovery |
| guarded LearningStore | `bb19f2d9` | 所有 learning business read/write 共用 coordinator |
| runtime DB | `68a9dfdb` | 独立 `tutor_runtime.db` v1、CAS、budget reservation、terminal/outbox |
| composite data | `b1f46f25` | BundleV2、双库 delete/import、runtime-only merge、prepare downgrade |
| desk candidate API | `f6cabfb0` | start/resume 503 zero-row；get/list/cancel repository truth；candidate capability |
| Tauri bridge | `48b4cdc6` | activity commands、24 MiB v1/v2 file gate、安全备份两阶段提交 |
| B02-R1/R2/R3 remediation | `27908527` | dead-writer-only recovery、desktop lifecycle owner、projection owner guard、post-merge quotas |
| B02-R4 remediation | `e64921b2` | Python/Rust 递归 NFC canonical hash 与共享 Unicode fixture |
| desktop recovery wiring | `731ae2ed` | pin recovery one-shot before port handshake and periodic runner startup |
| B02-R5 remediation | `d125a4c8` | stable terminal event identity；run/type/time/payload provenance；forged history zero-write |

实现未修改 LLM/provider、Agent graph、Web、Gateway adapters 或 `python/overlays/**`。`learning.db`
schema 仍为 v1；新增 schema 只存在于独立 runtime/coordination DB。

## 2. Frozen implementation outcomes

| Contract | Executable evidence |
|---|---|
| identity/idempotency | owner/space/kind/activity 四元 PK；scoped idempotency；canonical request SHA-256；异 payload conflict |
| CAS/lifecycle | checkpoint/run 同 revision；answer/interrupt exact binding；cancel/worker race 单一胜者；terminal immutable |
| hard provider budget | write-ahead reservation；每个物理 attempt 单独 ordinal/status；attempt/activity caps；reserved crash 记 unknown 且不退款 |
| cross-process safety | `learning_coordination.db` fence 位于 core；每个 operation 有 kernel-released liveness sidecar；desktop 是唯一 recovery owner；LearningStore 与 TutorRuntimeStore 所有 business access 共同参加 |
| deterministic terminal truth | completion basis、terminal reason、remediation count、六项 numeric budget summary 持久化；outbox identity 唯一派生且 import 必须匹配 terminal run/type/time/payload |
| downgrade/restore | v0.5 两阶段 verified backup 后才删 runtime；v0.4 learning 新行保留；`tutor_runtime_merge` 不覆盖 learning，并在同一 transaction 校验合并后组合配额 |
| portability limits | learning 16 MiB + runtime 6 MiB + overhead 1 MiB，outer 24 MiB；checkpoint 256 KiB / owner 3 MiB；Python/Rust 共享 NFC canonical fixture；near-cap round trip |
| B02 readiness | `student-tutor-runtime` 保持 `candidate/not_ready`；start/resume 返回 503 且零 row；未伪造 graph/provider ready |

## 3. Crash, race, and restart evidence

- spawned coordinator owner crash 留下 durable fence/journal，由新 coordinator 实例接管；
- production `learning_recovery` entrypoint 从真实 subprocess 覆盖 delete 四 phase、full import
  四 phase、runtime restore 三 phase；desktop startup 在 port handshake 前执行，并每 30 秒重试；
- spawned live writer 持有 OS liveness lock 时第二 entrypoint 只跳过、不 finish/rollback；writer
  退出后下一次 scan 接管。失败只记录 stable reason，fence/journal 原样保留；
- spawned Gateway production constructor 与独立 desk-style LearningStore writer 同时命中 owner fence，
  两者均返回 `LearningOperationInProgressError / learning_operation_in_progress`；
- delete restart matrix 覆盖 `fenced → learning_deleted → runtime_deleted → compacted` 四个 crash phase；
- full import restart rollback matrix 覆盖
  `fenced → validated_empty → learning_imported → runtime_imported` 四个 crash phase；
- stable outbox 覆盖 learning projection 已提交、ack 前 crash 的 replay，不复制 terminal event；
  owner/space delete barrier 还证明 pending-read 到 insert/ack 的 owner-wide guard 不允许 delete 后复活；
- prepare hash drift、manifest mismatch、oversize 与 runtime merge fingerprint conflict 均为 zero-delete/zero-partial-write。
- runtime merge 组合配额覆盖 owner 12、space 6、checkpoint aggregate、terminal 1,000、
  attempt 24、pending outbox 44、exact duplicate 与半批超限 rollback。
- terminal outbox import 覆盖 forged event id/type/payload/budget/time、nonterminal reference 全部
  zero-write；pending replay 与 delivered 后再次 restore 始终复用同一 projection identity，只保留
  一条 `tutor.terminal` history。

## 4. Real v0.4 source characterization

从 detached worktree 真实运行固定 source commit
`f6229d962abb420109fcf667d78807d9dd362d32` 的 `LearningStore.delete_owner_data()`，测试数据先由
B02 runtime 建立一条 learning space 和一条 Tutor created run。旧源码执行结果：

```text
V040_DELETE_COUNTS={'learning_activities': 0, 'learning_items': 0,
 'learning_artifacts': 0, 'learning_spaces': 1, 'learning_migrations': 0}
SOURCE_COMMIT=f6229d962abb420109fcf667d78807d9dd362d32
TUTOR_ROW_SURVIVED=True
LEARNING_OWNER_EMPTY=True
TUTOR_DB_SHA_UNCHANGED=True
COORD_DB_SHA_UNCHANGED=True
```

结论是预期的 **red characterization**：v0.4 只删除 learning.db 五表，完全不打开/修改
`tutor_runtime.db` 或 `learning_coordination.db`，Tutor row 因而残留。B02 不声明旧 runtime 会对
新 schema fail closed；支持路径只能是 verified prepare-downgrade，再以 runtime-only merge 恢复。
官方制品安装/升级/降级阻断仍归 `CTL-G01/Q-3`，本卡不伪造 release artifact PASS。

## 5. Verification results

### Core repository gate

```powershell
cd hermes_core
.\scripts\run_tests.ps1 tests\learning tests\gateway\test_study_commands.py `
  tests\cron\test_study_review_reminder.py -q
```

Result after R5: **350 passed / 0 failed** in 66.54 s, 4 hermetic workers；credential env 全部清空。
其中 BundleV2 focused suite 为 **34 passed / 0 failed**，包含 7 个新增 forged/replay cases。

### Python repository gate

```powershell
cd python
python -m unittest discover -s tests -p "test_*.py" -v
```

Fresh remediation run: **368 tests / 367 passed / 1 unrelated baseline failure** in 317.870 s。
唯一失败是 `test_repository_manifest_satisfies_fail_closed_contract`：既有 C-track commit
`4926959a` 新增四个 delivery-contract source/test path 后没有刷新 C0 generated ledger；官方
`--refresh-generated-ledgers` 只会把这四个 C-track path 加入 shared manifest。B02 owner 不越权
提交 C0 shared-spine 变更，故如实保留该 repository-level baseline blocker。B02 自身新增 Python
gate：

```powershell
python -m unittest tests.test_runtime_import_verifier `
  tests.test_learning_recovery `
  tests.test_study_activity_routes -v
```

Result after R5: **17 passed / 0 failed** in 43.452 s；包含六项 recovery/startup、七项
bundle/import verifier 与四项 Study route tests。新增关键 tests 使用 `unittest.TestCase`，
可被 canonical discover 捕获。

### Tauri study gate

```powershell
cd tauri
cargo test study --no-fail-fast
```

Result: **13 passed / 0 failed**。覆盖 v1 12 MiB、near-24 MiB v2、oversize、非法/相对路径、
symlink/non-file、Windows atomic replace、写失败、readback corruption、hash drift，以及与 Python
canonical JSON 相同的共享 Unicode fixture（NFC/NFD、key order、中日韩、组合音标、emoji、
normalized-key collision）。测试编译只使用未提交的空 resource/frontend
placeholder；没有构建 Python bundle 或 Web artifact。

### Bundle gate（当前结果）

```text
PASS / USER-RUN
Bundle ready at D:\project\Kabuqina\python\dist\runtime (1397.5 MB)
standard smoke passed
STT binaries OK
B02 Tutor runtime bundled import OK
```

`BUNDLE_INFO.json` 记录 bundled CPython 3.11.15、1397.5 MB 与
`2026-07-23T15:33:08.4508686+08:00` build timestamp，文件更新时间为 15:33:16。
Reviewer 复核 bundle 中 `learning_recovery.py` 和 R5 `tutor_runtime_store.py` 的 SHA-256
分别与当前 source 完全一致，并用嵌入式 `python\\python.exe` 再次导入 recovery、
runtime store 与 composite data service。官方 v0.4 artifact preflight、最终候选安装/升级/
降级与 provenance 仍由 `CTL-G01/Q-3 → Q-4` 关闭，不属于本 B02 smoke PASS。

## 6. Review findings response

| Finding | Resolution | Tests / evidence |
|---|---|---|
| `B02-R1` P1 | operation writer 持有 OS sidecar lock；desktop 为唯一 one-shot + periodic recovery owner；live writer fail closed | `test_learning_recovery.py` 五项真实 subprocess tests；core live-writer process test |
| `B02-R2` P1 | pending read → learning insert → runtime ack 共用 owner-wide write guard | owner/space 两进程 barrier、delete 后全表为空、既有 replay 幂等 |
| `B02-R3` P1 | 单 transaction 预检 actual-new groups/events 与 existing 的组合配额 | owner/space/checkpoint/terminal/attempt/outbox/exact-duplicate/batch rollback |
| `B02-R4` P2 | Python/Rust 递归 NFC、normalized-key collision、sorted compact UTF-8、integer-only 合同一致 | 同一 `tutor_canonical_json_vectors.json` 被 core 与 Rust focused tests 消费 |
| `B02-R5` P1 | outbox id 由完整 activity identity 唯一推导；只接受 terminal run、固定 type、terminal timestamp 和完整 run-derived payload | forged id/type/payload/budget/time、nonterminal event 均 zero-write；pending/delivered replay 只产生一条 history |

## 7. Independent Gate sign-off

- **Reviewer：** Codex（B 轨非作者 Gate reviewer；用户于 2026-07-23 授权签字）。
- **Decision：** `APPROVED`；`CTL-B02 DONE`。
- **Reviewed target：** `ab685481411a24903eb3461622bbaeb7ae51c319`。
- **Finding result：** `B02-R1…R5` 全部关闭；无新增 actionable finding。
- **Bundle result：** 当前 1397.5 MB bundle 的标准 smoke、STT 与 B02 runtime import 通过，
  关键 bundled source hash 与当前源码一致。
- **Scope result：** capability 仍为 candidate/not-ready；provider、L-2 graph、Web scene 与
  Gateway Tutor 未在 B02 偷跑。

完整签字见
[CTL-B02 Gate sign-off](2026-07-23-v0.5.0-ctl-b02-tutor-lifecycle-signoff.md)。
`CTL-B03` 可转为 `READY`，但须另行授权启动；正式 release artifact Gate 仍归 G01/Q-3。
