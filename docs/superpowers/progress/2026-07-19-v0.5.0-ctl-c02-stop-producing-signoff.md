# CTL-C02 · stop-producing / autostart Gate sign-off

> **结论：APPROVED / `CTL-C02 DONE`。** C01 的双 profile 合同已经落实到 C02
> 范围内的可见性、autostart、child identity/config、Tauri 写入/worker 启动以及
> cron/messaging delivery 边界。前次 Gate review 的四项阻断均已关闭。
>
> **Independent Gate reviewer：Codex（本 task reviewer）**
>
> **签字时间：2026-07-19，Asia/Shanghai**
> **复审基线：** implementation base `e5c020f885dc041f73cd00bd841cf3c9aaa747e5`
> 上的当前 CTL-C02 implementation/evidence worktree；实现提交应保持本次复审内容不变。

## Gate 结论

| 复审项 | 结论 |
|---|---|
| Gateway child 身份与隔离 | Tauri 向每个真实 child 注入 canonical/compatibility profile 与单平台 identity；生成仅含选中 adapter 的 profile config。Core 在 YAML、env override、plugin discovery 合并后再次按 profile/platform fail closed。 |
| Rust state-producing boundaries | 通用 env patch 与 Telegram、DingTalk、Email/password、Email OAuth、Weixin/QQ QR、Feishu/WeCom config/QR 等入口均在写文件、清理、网络或 worker launch 前校验 active profile。读取、显式删除、status/cancel 仍保留。 |
| Web surface | Settings/onboarding 使用受信任的 Rust profile DTO；两个 profile 的 retained catalog 精确，removed/unknown deep link 显式 tombstone；先前陈旧的 focused tests 已修正。 |
| Delivery contract | 新 removed/unknown cron 与 `send_message` target 在加载配置、adapter 或网络前返回 versioned `unsupported_delivery`；旧 target 文本保持可查看、编辑和删除，不静默改投。 |

## Independent verification

- canonical Core runner：**117 passed**。
- Python focused policy/runtime suite：**40 passed**。
- Rust 当前测试二进制（生成时间晚于本次修复源码）：
  `gateway_supervisor::tests` **15 passed**，`paths::tests` **12 passed**。
  本次 reviewer 直接运行测试 harness 全绿；`cargo test` 驱动在本机无输出挂起并超时，
  因此不把该次 Cargo invocation 伪记为成功。
- Web focused scripts：两项通过；production build 与 ESLint 通过。
- C01 platform manifest audit：`gate_ready=true`，25 surfaces、23 classified platforms、
  owned-source unknown 0。
- `git diff --check`：通过；仅报告工作树的 LF → CRLF 提示。

## Accepted deferrals

- `web/src/lib/gatewayPlatformSettingsRegistry.ts` 的 Email surface 仍展示
  `SMS_HOME_CHANNEL`。C02 后端会在 mutation 前拒绝它，所以不再产生 SMS state；
  canonical Email home target 与可用配置 UX 必须由 **CTL-C06** 关闭，不能据此宣称
  Email release-ready。
- adapter/plugin/dependency/source 的物理删除仍属于 **CTL-C03/C04**。
- legacy credential/profile/job/home cleanup 与升级矩阵仍属于 **CTL-C07**。
- installer、upgrade、稳定窗口与最终发布许可仍属于 **CTL-G01/Q-3 → Q-4**。

**Gate effect：** `CTL-C03a/b/c` 与 `CTL-C04` 的 `CTL-C02 DONE` 前置条件已满足，
可按控制板 WIP、shared-spine owner 和独立 leaf review 规则拉取。本签字不是 v0.5 发布许可。
