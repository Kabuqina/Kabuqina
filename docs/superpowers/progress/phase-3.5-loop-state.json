{
  "plan": "docs/superpowers/specs/2026-06-24-consolidate-and-langgraph-replatform-plan.md",
  "current_task": "10-review-remediation",
  "status": "approved",
  "attempt": 1,
  "worktree": null,
  "last_commit": "baefa4fa",
  "last_verification": [
    "Task 10 re-review (2026-07-01) APPROVED. Reviewed the rollback-safe engine selector + review-remediation Groups A-D (commits c05b344d -> 7cfce77d -> 58cf57b0 -> 017ff465 -> baefa4fa).",
    "engine_selector.py: precedence explicit > HERMES_AGENT_ENGINE > agent.engine (config) > loop is correct; explicit/env invalid -> ValueError (operator intent), config invalid -> warn+fallback to loop; no LangGraph import; resolved once in __init__ (self.agent_engine). test_engine_selector.py: 20 passed (the 17->20 miscount is fixed).",
    "Dispatch seam run_agent.py:9486 run_conversation -> _run_conversation_graph / _run_conversation_loop with NO cross-engine fallback; docstring documents the tool-side-effect rationale for not re-running the loop on a graph failure.",
    "P1-1/P1-2 (7cfce77d) confirmed real: graph initialize_turn/_finalize_turn now mirror the loop prefix+finalizer side effects, each loop-line-cited; _sync_external_memory_for_turn self-guards on interrupted (run_agent.py:4465, #15218), so the unconditional call is correct parity. golden lifecycle_calls is a legit parity pin re-recorded from the loop.",
    "P1-3/P1-4 (58cf57b0) confirmed: _record_usage_attempt uses a per-turn monotonic _usage_attempt_index (fixes the [0,1,1] api_call_count collision); max-iteration summary records real per-call usage; strict no-op without a usage_sink -> ZERO production behavior change (default has no sink).",
    "Deterministic equivalence gate re-run on current main this session: test_graph_differential_sequences + test_usage_event_sink + test_exit_contract = 131 passed. test_engine_selector = 20 passed."
  ],
  "failed_approaches": [
    "Initial hypothesis that the differential flake was the OpenRouter prewarm/cost cache race was wrong: a plain (no-pytest) harness loop ran 960 case-pairs clean. The real cause was the interrupt variant under a real (pytest) event loop -> graph runs async and observes the interrupt at a non-deterministic point (-> PH35-FU-008).",
    "Task 9 finalize markers were dropped by StateGraph(TurnState) until declared as NotRequired channels (finalize/final_response/turn_interrupted)."
  ],
  "blocker": null,
  "handoff_prompt": "Continue Phase 3.5 FU-009. State is fully on origin/main (2afeca4f); nothing depends on prior-session memory. FIRST read this file's next_action + the FU-009 section at the end of DECISIONS.md. Done so far: families G, E.1, E.2 fixed (18->15 remaining). NEXT CYCLE = port Family A/B/C, the empty-response ladder in the loop at run_agent.py:12360-12580, into the graph adapter's process_response (currently the plain-text path at ~:13602 finalizes empty content as final_response=''). It is ONE ~200-line shared block, not 3 independent fixes: partial-stream recovery (B), prior-turn-content shortcut, post-tool empty nudge, thinking-only prefill x2 (C), truly-empty retry x3 (A), fallback provider, '(empty)' terminal — sharing _empty_content_retries / _thinking_prefill_retries / _post_tool_empty_retried counters and a strict tool->assistant('(empty)')->user(nudge) message-sequence contract. Port sub-case by sub-case, verifying each under HERMES_AGENT_ENGINE=graph against its test in test_run_agent.py, then run the differential gate (test_graph_differential_sequences.py) and a loop regression (test_run_agent.py under default). Target tests: test_truly_empty_response_retries_3_times_then_empty, _succeeds_on_nudge, test_empty_response_triggers_fallback_provider, _fallback_also_empty_returns_empty, _emits_status_for_gateway (A); test_reasoning_only_* x3, test_kimi_tool_replay_includes_empty_reasoning_content (C); test_partial_stream_recovery_on_empty_stub, _preempts_prior_turn_fallback (B). Commit locally per sub-cluster; push only after the cluster is green on both engines. After A/B/C: D (compression trigger: test_context_compression_triggered, test_minimax_delta_overflow), F (test_ollama_glm_stop_after_tools), H (test_nous_401_refreshes_after_remint). Do NOT flip DEFAULT_ENGINE until all FU-009 + FU-008 close.",
  "next_action": "STEP 2 DONE + STEP 3 STARTED (2026-07-01, FU-009): triaged the graph slice (test_run_agent.py under HERMES_AGENT_ENGINE=graph = 18 failed / 276 passed) into 8 families sharing one root (graph reproduced the loop's happy path + major exits but not its deep retry/nudge/fallback ladders; transport node checks only 'response is None' where the loop uses validate_response). Families: A empty/malformed nudge ladder (5), B partial stream recovery (2), C reasoning-only/prefill (4), D compression trigger (2), E retry-exhaustion returns-error (2), F continuation boundary (1), G SafeWriter (1), H 401 credential remint (1). FIXED families G, E.1, E.2 (2026-07-01, 18->15 remain). G: _install_safe_stdio() at top of _run_conversation_graph. E.1: graph transport node now validates with _get_transport().validate_response() (catches malformed choices=[], not just None) -> existing _handle_invalid_response ladder; also ungated the loop's FU-007 invalid_response emission to stay symmetric. E.2: wrapped _build_api_kwargs at prepare_request :13367 in try/except -> _pending_exc + transport_error emit + route to handle_transport_error (shared classifier surfaces str(exc), failed=True). Verified: TestRetryExhaustion 3 passed both engines; FU-007 usage 12 passed. REMAINING 15: A(5) B(2) C(4) D(2) F(1) H(1). NEXT focused cycle = Family A/B/C the empty-response ladder: reading loop :12360-:12580 shows A+B+C+post-tool-nudge are ONE ~200-line shared block (partial-stream recovery, prior-turn shortcut, post-tool nudge, thinking-prefill x2, truly-empty retry x3, fallback, '(empty)' terminal) sharing _empty_content_retries/_thinking_prefill_retries/_post_tool_empty_retried counters + a strict tool->assistant('(empty)')->user(nudge) message contract. Graph process_response plain-text (:13602) finalizes empty as ''. Highest leverage (~11 tests) but highest risk (hot path) -> own cycle with per-sub-case verification. Then D (compression trigger), F (ollama continuation), H (401 remint). See DECISIONS.md FU-009 triage. STEP 1 DONE (2026-07-01, FU-007): the loop now emits transport_error / invalid_response(None-gated) / compression(413 + general context-overflow) usage events via the shared _record_usage_attempt, matching the graph's per-attempt sequence on 5 error fixtures (new parametrized test in test_usage_event_sink.py). Verified: differential 121, loop suite 294, usage/compression/exit 29; no-op without a sink so legacy result dicts untouched. Anthropic 1M-tier compress intentionally NOT emitted (graph has no long_context_tier compression handler -> PH35-FU-009). G1 usage-sink blocker cleared. NEXT = STEP 2: FU-009 triage — re-run the graph slice (now that the _snapshot test-rot is fixed) and categorize the ~18 clean test_run_agent.py loop-suite-under-graph divergences (empty/partial response, reasoning-only prefill, compression trigger, 401 remint, continuation boundary, retry exhaustion) into real graph gaps vs test-only assumptions; then STEP 3 fix per family, using FU-008's deterministic interrupt fixture for the interrupt family. STEP 0 DONE (2026-07-01): fixed a harness regression the Task 10 Group A change left — golden_harness._snapshot's lifecycle_calls made optional (7cfce77d had made it a required positional arg but only updated the canonical callers, leaving test_graph_{protocol,tool,error,budget}_parity.py red with TypeError on main since 2026-06-30, unseen because the '131 passed' gate excluded them). After the fix all 6 graph-parity files pass (39+ tests, no real divergence underneath = pure test-rot). exit_context_no_compression / exit_context_stepdown still fail on BOTH engines locally = pre-existing missing-aux-provider env failure, not a graph gap. FU-009 real signal = ~18 test_run_agent.py loop-suite-under-graph divergences. Follow-up: fold test_graph_*_parity.py into the canonical gate. Original Task 10 status below. Task 10 APPROVED (re-review 2026-07-01). NOTE ON STATE: contrary to the prior 'Do NOT push' cursor note, the Task 10 work + Groups A-D remediation are ALREADY on origin/main (merged, likely via the PR flow) alongside G0 and the STUDY PRs #4/#5. This is low-risk because DEFAULT_ENGINE='loop' and Task 11 (the default flip) is NOT done -> the graph engine is DORMANT unless HERMES_AGENT_ENGINE=graph or agent.engine=graph is set explicitly. Before Task 11 / G1 default-flip, three gates remain OPEN: PH35-FU-007 (loop-side usage emission on error paths transport_error/invalid_response/compression; only the success path is done), PH35-FU-008 (interrupt fixture: graph observes interrupt at a non-deterministic async point), PH35-FU-009 (22 graph edge-case equivalence gaps: retry/empty-response/compression-trigger/length-continuation/401-remint). Do NOT flip the default engine until FU-007/008/009 are closed."
}
