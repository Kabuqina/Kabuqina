# CTL-C07 · Legacy credential/job/home-channel upgrade evidence

> Status: **REVIEW READY · AUTHOR VERIFIED · NOT SIGNED OFF**
> Date: 2026-07-26 · branch: `codex/v050-c07-legacy-upgrade` · base: `027d3551`

## Outcome

C07 now has one user-visible, fail-closed path for v0.4 messaging data:
inventory → verified local export → explicit exact cleanup. Removed or unknown
records are not redirected to Desktop or another retained channel. This is
source/fixture upgrade evidence; CTL-G01/Q still owns the official installed
artifact upgrade matrix.

## Implemented contract

| Surface | Result |
|---|---|
| host/profile env | inventory exposes key names only; cleanup rewrites a frozen exact-key allowlist after export verification |
| QQ home alias | `QQ_HOME_CHANNEL{,_NAME}` copies to `QQBOT_` only during the explicit action and only when canonical is absent |
| config/directory | removed `platforms` entries are deleted structurally; retained and unknown sibling data survives |
| exact runtime state | finite Feishu/WeCom QR, Feishu state, removed pairing/thread-map and known profile files are removable; symlinks are refused |
| directories/history | production code has no walk or recursive delete; sessions, jobs, media, unknown files and profile directories remain |
| pairing limits | only keys whose parsed platform is in the fixed removed set are rewritten |
| cron | raw old destination stays visible and deletable, receives an unavailable badge, and still executes as `unsupported_delivery` without reroute |
| sessions | an unknown stored platform becomes `PersistedPlatformValue`, round-trips its original text, and cannot become a live `Platform` or `local` |
| Settings routes | v0.4 `/settings/feishu` and `/settings/wecom` are tombstones linking to the legacy-data control, not redirects to retained channels |
| home rename state | inventory reports canonical/legacy-home coexistence and the UI explains that this control does not merge or recursively delete either tree |

Exports are content-addressed SHA-256 JSON files below the app data
`legacy-channel-exports` directory. Each included file has its own SHA-256 and
base64 body. A file over 16 MiB, a total over 64 MiB, a changed home path, a
bad export hash, a malformed JSON record, or a missing confirmation refuses
cleanup before the first rewrite.

## Verification

| Check | Result |
|---|---|
| Rust `cargo test legacy_upgrade -- --nocapture` | **8 passed**; includes verified-export gate, incomplete export, malformed-record preflight, exact dotenv/YAML/JSON/pairing edits, no recursive deletion and the v0.4 fixture |
| Rust cron compatibility test | **1 passed**; old raw target is unchanged and marked unavailable by active profile |
| Core session/delivery/cron serial tests | **98 passed**; opaque unknown origin, removed delivery rejection, job non-delivery edit/delete contract |
| Web `npm run test:legacy-upgrade` | **passed**; three command sequence, confirmation token, oversize block, Feishu/WeCom tombstones and unavailable-job label |
| Web `npm run build` | **passed**; existing Vite dynamic/static-import and chunk-size advisories only |
| `audit_v050_platform_manifest.py` | **passed** after generator-owned typed-ledger refresh: 25 surfaces, 23 platforms, 0 unknown owned source, gate-ready true |
| `audit_active_platform_docs.py` | **passed**: retained 6, removed advertisements 0 |
| bundle | **not run** by explicit owner direction; C07 adds no runtime dependency |

The Rust test compile used empty ignored `python/dist/runtime` and `web/dist`
resource placeholders in this worktree; these are test scaffolding, not a
bundle or evidence of a packaged runtime.

## Review focus and remaining owner gates

Independent review should concentrate on the exact env/file allowlists,
preflight ordering, export disclosure wording and the distinction between
opaque history and live platform identity. C07 must not be marked DONE until
that review signs off.

CTL-G01/Q remains responsible for the official-hash v0.4 installed app → final
v0.5 candidate update, real app-data inventory/export/cleanup UI smoke,
clean/install/uninstall, signing/provenance, advisory reconciliation and final
release decision. C06's six real-account transport smoke also remains its own
review evidence and is not silently closed here.
