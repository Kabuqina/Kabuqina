# CTL-C03a Discord removal implementation evidence

**Control:** CTL-C03a / C-2 Discord leaf

**Frozen runtime base:** `4926959a0046d78f7e820e301a4c4568a26cd0b1`

**Status:** `DONE` — first Gate `CHANGES_REQUESTED` findings closed and
[independent re-review approved](2026-07-23-v0.5.0-ctl-c03a-discord-removal-signoff.md)
on 2026-07-23

## Conclusion

The owned Discord gateway surface is physically absent. The adapter, direct
send/admin tool, voice doctor, Discord-only tests/stubs, and active messaging
documentation route were deleted. Gateway startup/config/session/channel
directory, generic send-message routing, toolsets, CLI configuration, model
tool schemas, and active navigation no longer import, register, advertise, or
select Discord.

Fresh installs no longer receive a Discord platform/toolset entry from
`cli-config.yaml.example`; this closes the shared Windows, Linux and Docker
template path. Active Core README copy no longer promotes the removed adapter.
`DISCORD_BOT_TOKEN` is no longer considered by the Windows or POSIX installer
when deciding whether to start a gateway. Existing config and `.env` files
remain untouched.

The direct `discord.py[voice]` dependency was removed and `uv lock`
mechanically removed `discord-py`, `PyNaCl`, `audioop-lts`, and `davey`
as unreachable packages. A fresh verified desktop runtime contains none of the
owned Discord source paths, Discord package/dist-info, or orphaned NaCl
package/dist-info.

Compatibility remains deliberately non-destructive. `Platform.DISCORD`, C02
unsupported-delivery tombstones, stale-config filtering, and persisted old
credential/profile/session/job/home text remain so legacy state is readable,
explicitly removable, and never rerouted or executed. CTL-C03a did not delete
user data or claim cleanup/upgrade completion.

## Commit slices

| slice | commit | result |
|---|---|---|
| retained boundary | `b2892bf3` | Discord mocks removed from shared media/voice tests; retained contracts frozen |
| physical leaf deletion | `f67d019b` | 30 Discord-owned files deleted |
| shared-spine cleanup | `1afa444f` | imports, branches, config/CLI/toolset/model-tools and active route exposure removed |
| dependency/runtime closeout | `0b37d1ea` | lock, bundle/sync verifier, manifest and initial evidence closeout |
| current-main integration | `9c267915` | merged current `main`; the only content conflict was the control-board owner row, resolved by preserving B-track state and recording C03a review state |
| first Gate blocker closure | `fce8f299` | fresh-install template/README, installer start eligibility, retained voice tests and JIT allowed-path correction |

## Verification evidence

| layer | command/evidence | result |
|---|---|---|
| Gate-focused contracts | canonical Core runner over voice, fresh-install contract, POSIX TTY gate and CLI toolset compatibility | **84 passed** |
| retained Core media/voice | canonical `hermes_core/scripts/run_tests.ps1` over the nine-file retained suite from the JIT plan | **248 passed, 2 skipped** |
| shared-spine Core | canonical config/session/text-batching/stream/registry/CLI/toolset suite | **267 passed** |
| compatibility/model/channel | canonical channel-directory, config/session, C02 delivery-contract, registry, model-tools and toolset suite after the current-main merge | **353 passed** |
| installer syntax | PowerShell `[scriptblock]::Create(...)`; committed `install.sh` Git blob piped to `bash -n` | passed |
| Python syntax | `python -m py_compile` over all changed Python files in Slice 3 | passed |
| dependency lock | initial `uv lock`; current `uv lock --check` | resolved 265 packages; removed Discord/voice orphan packages; check passed |
| runtime verifier tests | `python -m unittest python.tests.test_runtime_pruned_verifier -v` | **5 passed** |
| fresh bundle | `python/build_bundle.ps1 -Verify` | pruning/import/profile/legacy verifiers passed; STT binaries OK |
| fresh bundle identity | `python/dist/runtime/BUNDLE_INFO.json` | Python 3.11.15; built 2026-07-19 14:54 +08:00; 1,397.6 MB |
| independent absence check | bundled Python ran `python/tools/verify_runtime_pruned.py`; explicit package query | pruning passed; no Discord/PyNaCl package or dist-info |
| source sync | `scripts/sync-runtime-sources.ps1` after `9c267915` + `fce8f299` | pruning, runtime imports, profile imports and legacy identity all passed |
| active Web copy | `npm run test:gateway-ux`; `npm run test:onboarding-providers`; `npm run build`; `npm run lint` | both focused tests, production build and ESLint passed |
| C0 regression | refresh-generated-ledgers, then no-refresh audit rerun | references 634; 25 surfaces, 23 classified platforms, owned-source unknown 0, gate-ready true |
| diff hygiene | `git diff --check` | passed; only LF → CRLF working-copy warnings |

The first agent-driven fresh bundle invocation was interrupted by the tool
host's 15-minute timeout while pip was still staging wheels; it produced no
`BUNDLE_INFO.json` and is not counted as evidence. The release operator then
ran the same command to completion in the worktree; the success output and
artifacts above are the authoritative run.

That full fresh bundle was produced for the C03a dependency/runtime closeout
at `0b37d1ea`. The later current-main merge adds B-track learning/desktop work,
and `fce8f299` changes install-time copy/eligibility, docs and tests; neither
changes the C03a dependency graph nor restores a removed runtime surface. The
post-merge source sync and all runtime verifiers passed. This evidence therefore
proves C03a runtime/package absence but does not claim a new integrated release
bundle at the merged HEAD; CTL-G01 owns that integrated build/provenance Gate.

## First Gate findings and closure

| finding | closure |
|---|---|
| P1 fresh install produced Discord state | removed the key, example, preset text and active README promotion; YAML/README contract tests pass; all three installers consume the sanitized shared template |
| P1 legacy token qualified gateway autostart | removed only `DISCORD_BOT_TOKEN` from `Start-GatewayIfConfigured` / `maybe_start_gateway`; existing `.env` preservation and reads are unchanged |
| P2 retained voice coverage was over-deleted | restored config/default synchronization, Base per-chat gate, runner streaming/dedup and behavioral voice send/filename/cleanup tests with Telegram or neutral doubles |
| JIT allowed paths incomplete | added `gateway/platforms/helpers.py`, `gateway/platforms/webhook.py`, `model_tools.py` and `tools/cronjob_tools.py`, plus review-fix install/docs paths |
| evidence overstated platform-source isolation | replaced the statement with the exact rule: no retained adapter/dependency/data removal; enumerated shared files contain narrow Discord-only edits |
| control-board merge conflict | merged current `main` in `9c267915`; preserved both B-track status and the truthful C03a review state |

## Classified residual references

- `Platform.DISCORD` and exact `"discord"` filtering tokens remain only for
  stored-record/config compatibility and removed-target denial.
- `tests/test_removed_platform_install_contract.py` contains the removed names
  solely as negative assertions for fresh-install and start-eligibility
  regression coverage.
- Tauri stale-key stripping and C02 delivery-contract tests remain in force.
- Historical release/upstream/legal documents are not rewritten by this leaf.
- Shared Core help and cross-site active-doc mentions such as
  `scripts/hermes-gateway` do not restore an adapter or route; their exact-set
  negative scan is deferred to CTL-C05.
- C0 keeps the `py-discord-voice` dependency record as an audited removal
  delta whose current consumers are the runtime absence verifier and its test.

## Deferred work

- **CTL-C03b/C03c/C04:** other platform leaves remain independent and incomplete.
- **CTL-C05:** consumes the lock/license/advisory removal delta and closes
  shared Core help/registry plus the full active-doc negative scan.
- **CTL-C07:** explicit legacy credentials, profiles, jobs, sessions and home
  cleanup plus v0.4 upgrade matrix.
- **CTL-G01/Q-3 → Q-4:** installer/updater/integrated stability and release
  approval.

## Review/sign-off

Implementation owner: **Codex / C integrator — Gate fixes complete,
2026-07-23**.

Independent leaf reviewer: **first review returned `CHANGES_REQUESTED`; second
review APPROVED, 2026-07-23**. The signed review confirms the closures above,
allowed-path boundary, classified compatibility references, fresh-runtime
absence, and accepted deferrals. See the
[Gate sign-off](2026-07-23-v0.5.0-ctl-c03a-discord-removal-signoff.md).
