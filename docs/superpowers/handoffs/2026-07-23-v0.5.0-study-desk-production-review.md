# v0.5.0 A 轨 · Study 书桌生产纵切片 Review Handoff

> **日期：** 2026-07-23
> **控制卡：** `CTL-A06a`
> **状态：** `REVIEW · TAURI MATRIX DEFERRED TO PRE-ART GATE · NOT DONE`
> **Review base：** `b947f297`
> **Durable commits：** contract/bridge `42a7eccf`；Web production integration `1198915b`；review evidence `887078d6`。提交范围已核对，不含无关 C 轨 manifest。
> **结论边界：** 两轮 P1/P2 findings 均已关闭；本切片已形成可引用 durable commits，因此进入 `REVIEW`。production fixture cleanup 已于 2026-07-24 在当前 worktree 实现并验证，待随本轮形成 durable commit；ignored runtime 同步与真实 Tauri 人工矩阵按 owner 决策延后到 pre-art 完整前端后的集中验收。该矩阵不阻止后续前端开发，但阻止 `DONE` 和更高 Gate 宣告。owner 仅粗看布局无明显问题。本包不等于 `E-I1`、`E-I2`、`S-1`、`P-3`、`V-5` 或 A 轨整体完成。

## 0. 首轮 Review 与修复状态

2026-07-23 独立 Review 裁决为 `CHANGES REQUIRED`。当前处理结果：

| Finding | 修复 | 回归证据 |
|---|---|---|
| P1 · check 后学习事件使 spaces 刷新、adapter 重建；已完成答案仍从 recovery draft 恢复，可重复产生活动 | `StudyDeskPage` 按 spaces 的语义快照稳定 adapter；`completed` check 在广播学习事件前清除该题 recovery answer；完成批注和末步返回不再被同值刷新重置 | `StudyRoute` 新增真实事件重验证测试；`studyDeskAdapter` 新增 completed 后不恢复草稿测试 |
| P2 · 重复 `item_ids` 被 set 静默折叠 | Core 要求 non-empty、unique、known IDs；Rust bridge 提前拒绝空/重复列表并保持默认调用不发送字段；Python 路由验证失败时 zero activity | Core `11 passed`；Python routes `12 passed`；Rust Study `15 passed` |
| P2 · debounce 在刷新/卸载时丢最后输入；离开文案声称删除但实际保留 | 输入变化同步写 recovery cache，异步 save 仍可 debounce；dirty/checking 恢复 `beforeunload`；中英文文案改为“保存恢复草稿并离开” | `DeskScene` 新增同步恢复与 unload 测试；`StudyShell` 更新确认语义测试 |
| P2 · 标准 Web gate 连续两次超时 | 将交互测试从逐字 `userEvent` 改为合同级 `fireEvent`，保留真实状态/保存/check 断言；避免与 Rust runtime build script 并发 | earlier full gate 连续三轮 `25 files / 114 tests`；第二轮修复后最新 `25 files / 116 tests` |

首轮 Review 的两项 P3 中，production fixture fallback 已于 2026-07-24 在当前 worktree 清理并验证；更完整的
bridge E2E/真实 Tauri 人工矩阵保留到 pre-art 完整前端后的集中验收。

### 0.1 第二轮 Review 修复

第二轮 Review 发现首轮 P1 修复把 recovery cleanup 放在所有 grade 上，导致 incorrect/ungraded
进入 `needs_revision` 后原答案反而被删除。现已修正：

- 只有映射结果为 `completed` 才清除该题 recovery answer；
- incorrect 与 ungraded 都保留原答案，重载后仍可继续修改；
- `needs_revision` 与 `dirty/checking` 一样触发应用内离开确认和 `beforeunload`；
- 中英文离开文案从“未提交”扩为“尚未完成”，准确覆盖已判错但仍需修改的答案；
- adapter 回归分别覆盖 correct 清除、incorrect 保留、ungraded 保留；
- DeskScene 回归覆盖 `needs_revision` 的 dirty callback 与 `beforeunload`。

本轮 Web focused 为 `4 files / 29 tests`；标准 full gate 为 `25 files / 116 tests`，lint 与
production build 通过。

真实 Tauri 人工矩阵仍未取得：最新一次启动在同步 ignored runtime 时完成 `1168 / 3658`
个文件后被中止，尚未进入应用窗口。下一次人工轮必须先让该同步完整结束，不能把本轮记为
路由、五题型、重启、原生 200% 或失败注入通过。

### 0.2 最终复审裁决

最终复审没有新增 P0/P1/P2，也无需再次代码 Review。裁决如下：

- `CTL-A06a` 已以 `42a7eccf`、`1198915b`、`887078d6` 形成排除 C 轨 manifest 的
  durable commits，当前进入 `REVIEW`；
- localStorage 合同统一为“只保存尚未完成的恢复草稿和当前位置”，其中包含
  incorrect/ungraded 已提交但仍需修订的答案；它不作为 canonical learning evidence；
- production `/__dev/desk` route、fixture adapter、fixture 课程 ID 与固定 fixture answer
  已从 production bundle 清除；`npm run build` 现在包含自动 bundle 泄漏检查；
- 真实 Tauri 路由、五题型、重启、原生 200% 与失败注入人工矩阵保持 P3；
- 该人工矩阵不阻止继续完成 pre-art 前端，但在关闭前不得将 `CTL-A06a` 标为 `DONE`，也不得据此宣告
  E-I1/E-I2/S-1/P-3/V-5、A 轨或更高 Gate 完成；
- durable commits 已按 owner 授权形成，且未纳入无关 C 轨 manifest。

## 1. Review 要回答的问题

请独立 reviewer 给出明确裁决，不要只复述测试结果：

1. 当前 `/study/:spaceId/practice` 是否已经形成可接受的首个生产 Study 纵切片；
2. frozen canonical 书桌是否被忠实迁入 production，而没有建立第二套 Study 真值；
3. “检查这一步”是否只批改当前题，并保持旧的整份 quiz 提交兼容；
4. 草稿、书签、活动记录、学习事件、失败重试、过期请求和 dirty-leave 是否一致且可恢复；
5. 书桌内的页面、课程、Chat、Activity、Settings、材料与“开新本”入口是否使用既有路由和 guard；
6. 是否存在 P0/P1，或足以阻止本切片进入 `REVIEW`/提交的 P2；
7. 哪些发现属于本包，哪些必须明确留给 `E-I2`、Tutor/Practice 后续、最终美术或 `E-I6`。

## 2. 本包实际交付

### 2.1 Phase 1 · React rescue

- 将冻结的 canonical 书桌从单文件 HTML 拆成 React 组件与 CSS；
- 提供仅开发环境可用的 `/__dev/desk` 独立预览；
- 保留书桌、笔记本、工作夹、课程书、卡片盒、咖啡杯和纸面反馈的空间关系；
- 使用仓库既有 Lucide 图标，不引入手写 SVG 图标系统；
- 覆盖 `overview → focused → dirty → checking → needs_revision/completed` 状态。

### 2.2 Phase 2 · production Study integration

- production 路径为 `StudyRoute → StudyShell → StudyDeskPage → DeskScene`；
- `/study/:spaceId/practice` 由书桌接管产品 chrome，其他 Study 页面继续使用原 shell；
- 数据继续由既有 `StudyRepository`、quiz service、activity store 与
  `STUDY_LEARNING_EVENT` 持有；
- choice、true/false、short-answer、code、derivation 五类题目进入书桌；
- `QuizService.submit_attempt` 增加可选 `item_ids`，书桌只提交当前题；不传时保持历史
  full-quiz 行为；
- localStorage 只保存尚未完成的恢复草稿和当前位置，key 为
  `kabuqina.study.desk-draft.v1:<spaceId>:<artifactId>`，不作为学习证据；
- save/check 支持 abort、stale-completion guard 与 retry-safe error；
- 页面、课程和跨面跳转复用既有导航与 dirty-leave guard。

## 3. 明确不在本包内

- 咖啡杯目前只进入真实 `/chat`，尚未携带结构化课程/题目上下文；
- Study → Chat → Study 的同一 session、pending/transcript 连续性与 exact return 尚未实现；
- Tutor graph、hint ladder 的真实 runtime 闭环属于 B 轨及后续 A 轨接入；
- Practice 非线性、offline/restart 全矩阵与 Tutor integration 尚未关闭 `P-3`；
- 最终插画、品牌资产、克制动效、真实系统读屏和 Windows 原生 200% 尚未关闭 `V-5/E-I6`；
- 本包没有关闭 `E2/E4/X4` 的正式 Gate，也不得据此宣称 A 轨或 v0.5.0 feature complete。

### 3.1 必须审查的控制债务

本轮最初是接管 Kimi 因 429 中断的前端 rescue，owner 在粗看 Phase 1 后授权继续 Phase 2。
实现因此在当前 worktree 连续展开，没有在开工前单独建立 `CTL-A06a` JIT plan、独立 branch/
worktree 和固定允许路径；`CTL-A06a` 是本次为恢复控制面而补登记的卡。它也早于正式
`E4/X4/V5` Gate。

这不是自动豁免。reviewer 必须判断：

- 当前 handoff、文件地图、测试与 scope boundary 是否足以形成可审查的 retrospective slice；
- 是否需要在提交前补一份独立 JIT boundary/evidence 文档；
- production desk、step-scoped quiz 合同和控制面文档是否应拆成多个 commit；
- 哪些实现可作为后续 `CTL-A02..A06` 的输入，哪些必须在正式 Gate 后重审；
- 当前位于 `main` 的 dirty worktree 是否存在与其他活动卡冲突的改动。

若 reviewer 认为该控制债务会使 diff 无法可靠归属，应给出 `CHANGES REQUIRED`，而不是仅因
自动化通过而接受。

## 4. Review 文件地图

### 4.1 新增 production desk

- `web/src/study/desk/DeskScene.tsx`
- `web/src/study/desk/DeskNotebook.tsx`
- `web/src/study/desk/DeskObjects.tsx`
- `web/src/study/desk/DeskCup.tsx`
- `web/src/study/desk/DeskChrome.tsx`
- `web/src/study/desk/StudyDeskPage.tsx`
- `web/src/study/desk/deskAdapter.ts`
- `web/src/study/desk/studyDeskAdapter.ts`
- `web/src/study/desk/types.ts`
- `web/src/study/desk/artAssets.ts`
- `web/src/study/desk/desk.css`
- 同目录 fixtures 与测试文件。

### 4.2 接入与合同变化

- `web/src/main.tsx`
- `web/src/study/StudyShell.tsx`
- `web/src/study/repository.ts`
- `web/src/chat/study/study-api.ts`
- `web/src/index.css`
- `tauri/src/study.rs`
- `python/src/desk_server/routes/study_routes.py`
- `hermes_core/learning/quizzes.py`

### 4.3 测试与决策证据

- `web/src/study/StudyRoute.test.tsx`
- `web/src/study/StudyShell.test.tsx`
- `web/src/study/repository.test.ts`
- `web/src/study/desk/DeskScene.test.tsx`
- `web/src/study/desk/studyDeskAdapter.test.ts`
- `hermes_core/tests/learning/test_quizzes.py`
- `python/tests/test_study_routes.py`
- `DECISIONS.md`
- `design-qa.md`
- 本 handoff 与执行总控板。

`docs/superpowers/progress/2026-07-18-v0.5.0-c0-platform-surface-manifest.json`
是本包开始前已存在的无关 dirty 文件，本包未修改；不得将它混入 A 轨提交或 review 结论。

## 5. 作者侧已有证据

| 验证 | 结果 |
|---|---|
| Web component suite | focused `4 files / 29 tests`; latest full `25 files / 116 tests` passed |
| Web lint | passed |
| Web production build | passed |
| Core quiz contract | `11 passed` |
| Desktop Study HTTP routes | `12 passed` |
| Rust Study bridge | `15 passed / 0 failed` |
| 浏览器主路径 | `overview → focused → dirty → checking → completed` passed |
| 浏览器 console | error/warning 为空 |
| Phase 1 ↔ Phase 2 同态视觉对照 | 未发现 P0/P1/P2 drift |

视觉与浏览器证据位置：

- `.test-output/fe01-desk-rescue-2026-07-23/`
- `.test-output/fe02-desk-integration-2026-07-23/`
- `design-qa.md` 的 `FE-01`、`FE-02`

这些是作者证据，不代替独立 reviewer 复跑、代码审查或 owner 的 Tauri 人工验收。

## 6. Reviewer 必跑检查

### 6.1 Diff 与合同

1. 从 `b947f297` 审查全部 tracked/untracked diff；
2. 确认旧 quiz 调用不传 `item_ids` 时语义完全不变；
3. 确认未知、重复或不属于 quiz 的 `item_ids` 失败关闭；
4. 确认当前题 check 不为未来未答题生成错误 evidence；
5. 确认 localStorage 只作 recovery cache，不成为第二份 canonical learning state；
6. 检查 abort/stale guard、retry、书签恢复与最后一步回到 overview；
7. 检查 `/__dev/desk` 不进入 production bundle。

### 6.2 真实路由与功能人工矩阵

在真实 Tauri/Study 数据上至少完成：

1. 从实际课程进入 `/study/:spaceId/practice`；
2. 分别作答 choice、true/false、short-answer、code、derivation；
3. 检查当前题、修改答案、继续下一题、完成最后一步；
4. 刷新/重启后恢复草稿与当前书签；
5. dirty 状态离开到 page/course/Chat，分别验证取消和确认；
6. 逐项打开页面 tabs、其他课程、Chat、Activity、Settings、材料与“开新本”；
7. 验证 activity/plan/evaluate 刷新，并确认未来未答题没有被批改；
8. 验证保存失败、检查失败、慢请求/切课等恢复路径；
9. 验证窄窗、键盘、焦点、200% 和 reduced motion 的基本可达性。

### 6.3 建议复跑

按仓库开发指南复跑 Web lint/build 与相关 Web、Core、Python、Rust focused tests。若 reviewer
只复跑子集，结论中必须列出跳过项及原因，不能直接继承作者数字。

## 7. Review 裁决与后续退出门

独立 Review 已给出 `ACCEPT WITH P3 FOLLOW-UPS`，且原切片 durable commits 已形成。
production fixture P3 已在当前 worktree 关闭，待随本轮提交固化。当前 `CTL-A06a` 为
`REVIEW · TAURI MATRIX DEFERRED TO PRE-ART GATE · NOT DONE`，仍需：

- 连续完成美术资源接入前的完整前端，不再为中间小切片重复开启独立 Review；
- 前端合同稳定后完成 ignored runtime 同步，再执行一次真实 Tauri 人工矩阵；
- 把后续问题分别归入 `E-I2`、B 轨 Tutor/Practice、`V-5` 或 `E-I6`。

只有指定 Gate reviewer/owner 验收和上述阻断 `DONE` 的 P3 证据齐全后，才可标为
`DONE`。

## 8. 给新 Review session 的最短指令

> 请按
> `docs/superpowers/handoffs/2026-07-23-v0.5.0-study-desk-production-review.md`
> 本包已完成独立代码复审并获 `ACCEPT WITH P3 FOLLOW-UPS`。production fixture cleanup
> 已关闭；当前为 `REVIEW · TAURI MATRIX DEFERRED TO PRE-ART GATE · NOT DONE`，
> durable commits 为 `42a7eccf`、`1198915b`、`887078d6`，且已排除 C 轨 manifest。
> 后续先完成 pre-art 完整前端，再完成 ignored runtime 同步和一次真实 Tauri 人工矩阵。
> 不要把咖啡杯 exact return、
> Tutor、最终美术或 A 轨整体完成误算进本包，也不要纳入无关的 C 轨 manifest。
