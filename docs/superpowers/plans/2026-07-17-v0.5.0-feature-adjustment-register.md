# v0.5.0 功能调整与升级登记表

> **性质：** 活的 scope intake，不是实现 plan。新增需求先在这里完成问题定义、
> 依赖与版本裁决，再进入总计划和独立 JIT plan。
>
> **状态：** Open（2026-07-17）。

UX/交互类调整的审计、状态机、验证与冻结统一见
[v0.5.0 UX 交互设计计划](2026-07-17-v0.5.0-ux-interaction-design-plan.md)；登记表只保留
scope intake 和版本裁决，不用一行表格代替交互设计。

PPT 生成结构、审稿闭环、视觉母版与真实模板路线统一见
[PPT 生成质量与视觉母版完整优化计划](2026-07-17-ppt-generation-and-visual-master-optimization-plan.md)；
v0.5.0 只接不挤压 STUDY Must 的可延期视觉薄切片。

## 1. 纳入规则

每个调整项必须回答：当前行为、用户问题/证据、目标行为、非目标、所属层、数据/
兼容影响、依赖、最小纵切、验收和是否阻塞 v0.5.0。只有“想升级一下”而没有可观察
目标的项不进入实现。

优先级：

- **Must**：不完成就无法兑现 v0.5 核心承诺或会造成数据/安全/升级错误；
- **Should**：明显改善主路径，但有安全降级，可在 scope freeze 前决定；
- **Could**：不影响核心闭环，默认移入 v0.5.x/0.6；
- **Reject**：与学生学习产品定位冲突或维护成本大于价值。

## 2. 已知调整项

| ID | 项目 | 当前问题 | v0.5 目标 | 优先级 | 归属/计划 |
|---|---|---|---|---|---|
| U-01 | Tutor loop | LG1/LG2/H2 只有 requirements，普通 agent 无结构性停机/恢复 | explicit start→interrupt→resume→terminal | Must | 总计划 L-0..L-3 |
| U-02 | 练习④⑤ | 缺独立练习、hint ladder 和学习者讲解闭环 | grader 真值 + evaluation draft + LG4 | Must | 总计划 P-0..P-3 |
| U-03 | Learning Space | v0.4 有 IA，无完整场景/白板活动 | 功能 shell、白板 artifact、场景呈现 | Must | 总计划 S + 独立 UI 计划 |
| U-04 | kq-kp | raw block 仍依赖主模型/表面门控 | bounded post-node，所有 surface 不泄漏 raw block | Must | L-4 |
| U-05 | 长活动压缩/成本 | 通用压缩可能丢学习者错误证据，额外调用缺少分账 | tutor-aware retention + 每 resume 硬预算 | Must | L-5 |
| U-06 | 渠道删除后的投递 | home channel/cron/job/secret 可能仍指向企业微信/Discord/飞书/长尾 | 无静默改投，blocked 可恢复，旧数据可解释 | Must | 裁剪计划 C-1/C-4 |
| U-07 | Provider/成本 UX | Tutor、hint、evaluation、post-node 可能增加调用，但用户难区分 | readiness、费用说明、pending/blocked 和降级可见 | Should | L-0 后另出 JIT plan |
| U-08 | Chat↔Study 上下文 | 显式 Study activity 与普通 chat 必须互不误触且可返回 | context handoff、逃生门、restart 恢复一致 | Must | L-2/P-3/S-1 |
| U-09 | Companion 合并 | CompanionWindow 与场景杯可能双状态/双提醒 | 单一状态源，逐项迁移后再退旧面 | Should | UI V-4 + S-3 |
| U-10 | Gateway 内核收束 | 多平台 shared kernel 和 auto-loaded platform plugins 包含大量被删平台特例 | exact-profile registry、neutral helper、保留通用安全测试 | Must | 裁剪 C-0..C-3 |
| U-11 | 钉钉恢复/升级 | 当前 mainland policy 禁用，旧 SDK 因 `websockets<13` 冲突未打包 | 升级到可维护 SDK/transport，进入 mainland 正式 smoke | Must | C-0 后另出 DingTalk JIT plan |
| U-12 | SEA profile 实装 | 当前 profile mapping 只有 mainland，`sea` 实际回落 mainland 列表 | Telegram/WhatsApp/Email 独立依赖、设置、autostart、bundle 与测试 | Must | C-0/C-1 后另出 SEA JIT plan |
| U-13 | 自测作答闭环 | 输入能力存在但入口、作答区、答题状态和提交检查不可发现 | 各题型明确作答、答案保留、未答检查、可解释结果与恢复 | Must | UX X-0..X-4 + P-3；另出 JIT plan |
| U-14 | 闪卡主动回忆 | 只有翻面/四档评分；hint 在答案后、快捷键隐蔽、无完成总结 | recall→hint→reveal→grade→complete，鼠标/键盘同构 | Must | UX X-0..X-4；增强项可延期 |
| U-15 | 首次进入/建课/space 管理 | “开新本”只返回 Chat，space create API 无一等 UI 入口 | 空态与 switcher 可直接建课，成功后进入明确第一步 | Must | UX X-1/X-2 + S-0/S-1 |
| U-16 | 计划执行与继续学习 | “继续上次”只聚焦计划项，不启动/恢复真实活动 | Plan 下一步映射 Tutor/Practice/Learn，动作字面与结果一致 | Must | UX X-1/X-2 + U-01/U-08 |
| U-17 | 草稿审核与信任 | 多种 draft 不展示内容却允许激活/拒绝，动作隐喻不透明 | preview/source/impact/review 状态齐全，启用/拒绝语义明确 | Must | UX X-2 + artifact/review JIT plan |
| U-18 | Learn→Practice 上下文 | 知识点旁“去复习”是静态标签，只能进入无差别练习首页 | source-scoped 复习或显式生成待审核练习草稿 | Should | UX X-2；scope freeze 前裁决 |
| U-19 | 错题/评估行动闭环 | 只见分数/标签，看不到原题、答案、原因，建议不可执行 | evidence 可解释，exact retry，建议可转计划/练习 draft | Must | UX X-2 + P-2/P-3 |
| U-20 | 跨页面交互完整性 | back/dirty/focus/keyboard/pending/blocked/copy 无统一合同 | 共享状态、导航、焦点、快捷键、恢复和术语矩阵 | Must | UX X-0..X-4；各纵切验收 |
| U-21 | PPT 生成结构与修改闭环 | 只有 goal/emphasis prompt、整段 Markdown 审核、自动保存和整套重生成；无稳定 manifest/逐页来源/版本恢复 | 仅当现有 Web metadata 足够时在 v0.5 修正重生成沿用原母版；否则连同 PptRequest、故事板、预检、真实素材、预览和局部重生成移入 v0.5.x | Should | PPT 总计划 G-0..G-6；v0.5 条件子集不得新增 schema/migration |
| U-22 | PPT 视觉母版与资产管线 | 五套母版主要是 flat fill、色块和线条；selector 是配色预览，不能代表实际输出 | v0.5 新增 2 套六角色艺术化母版、背景资产 manifest、同源真实样张和授权/兼容 QA；其余 4 套移入 v0.5.x | Should | PPT 总计划 A-0..A-5；v0.5 Slice 5A..5D |
| U-23 | 真实 PowerPoint 模板复用 | `template_path` 只提取配色/字体，不能保留原模板 Logo、版式和对象 | v0.5 不接生产路径；v0.6 候选做对象复用 MVP 和安全回退 | Could | PPT 总计划 §8 + 既有 template reuse roadmap |

## 3. 待 owner 补充/裁决

后续提出的功能调整从 U-24 起追加，不覆盖旧记录。每项先选择：

- 修正现有合同；
- 交互/可用性升级；
- 性能/成本升级；
- provider/模型能力升级；
- 文档/PPT/报告能力升级；
- reminder/cron/messaging 升级；
- 延期到 v0.5.x/0.6。

## 4. Scope freeze

- L-0/P-0/S-0/C-0/V-0 review 完成前为需求收集窗；
- schema/视觉冻结后，只接受 P0/P1 bug、数据安全、升级和 release blocker；
- 新增 Must 必须说明替换/延期哪个既有 scope，不能无成本叠加；
- 每个进入实现的 U 项必须回链独立 plan、测试证据和 release note。
- U-21/U-22 在 v0.5 均为 Should：若命中 PPT 总计划 stop rule，整体延期且不得降低
  Tutor、Practice、Learning Space、升级安全或渠道裁剪的完成标准。
