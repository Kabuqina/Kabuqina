# CTL-B01 · L-0 Tutor graph 实施设计

> **状态：** REVIEW v3（2026-07-19；第二轮 P1/P2 findings 已修订，待非作者复核）
>
> **性质：** v0.5.0 B 轨硬门；只冻结 Tutor runtime 合同、数据边界、失败语义与
> L-1/L-2 切片，不修改 Tutor hot path。
>
> **Base：** `e5c020f885dc041f73cd00bd841cf3c9aaa747e5`
>
> **Branch / worktree：** `codex/v050-b01-tutor-design` /
> `D:\project\Kabuqina\.worktrees\b-track-b01`
>
> **上游约束：** [LG1/LG2/H2 requirements](../specs/2026-07-12-learning-graph-contract-requirements.md)、
> [v0.5.0 开发计划 §6](2026-07-17-v0.5.0-development-plan.md)、
> [执行总控板 CTL-B01](2026-07-18-v0.5.0-execution-control-board.md)。

## 0. 决策摘要

1. Tutor 是用户显式启动的、可恢复的 **structured learning activity**；普通 Chat
   不做意图识别，不获得 checkpoint，也不改变现有 Agent turn 合同。
2. Tutor 使用独立的 `TutorGraphEngine` 和 `TutorGraphStateV1`，放在
   `hermes_core/agent/graph_engine/` 的新文件中；它复用现有 provider routing，
   但不嵌进 `TurnState`、不调用第二个 `AIAgent.run_conversation()`、不改变
   `build_agent_graph()` 的无 checkpointer 合同。
3. 持久化由 core 的 `LearningCheckpointStore` port 控制；v0.5 使用独立
   `tutor_runtime.db`（schema v1），不升级 v0.4 会打开的 `learning.db`。现有
   `learning_activities` 继续是不可恢复的追加事件；Tutor terminal event 通过 runtime DB
   outbox 幂等投影，不能反过来成为 checkpoint 真值。第三个小型
   `learning_coordination.db` 承载跨进程 operation fence；desk、Gateway、cron 等所有
   `LearningStore` 读写实例必须通过同一个 coordinator。
4. 身份键固定为 host 注入的
   `(owner_id, space_id, activity_kind, activity_id)`；chat session、gateway chat、
   browser tab、模型参数都不能提供或覆盖这些字段。
5. 生命周期增加可恢复的 `interrupted`：
   `created → running → waiting_for_learner → running`，`running` 可因进程中断进入
   `interrupted`，再由显式 recovery 回到 `running`；终态只有
   `completed | blocked | cancelled`。
6. learner check 与 tool approval 使用不同 type、ID namespace、store、API、UI 和
   timeout。learner answer 永不授予工具副作用权限。
7. 模型调用只走 `TutorProviderPort.execute_once()`：SDK retry=0、v0.5 provider fallback=0；
   每个物理 attempt 在调用前写入 durable reservation，crash 后 reservation 仍算消耗。
   node、attempt、token、wall-clock 和总活动检查次数超限原子落为 `blocked`。
8. v0.5 的 structured Tutor 入口只在 desktop host 开放。Gateway conversation 保持
   unbound；后续若开放 gateway Tutor，必须另做投递、身份、并发和隐私评审。
9. L-2 不判断自由文本“答对/答错”：第一次 check 是 host-defined choice control；学习者选择
   “继续”则以 `participation_only` 完成，选择“再解释一次”则只允许一次 bounded remediate，
   再发第二个 check 后完成。这个分支满足上游 L-2 的 completed/once-remediate 合同，但不写
   mastery。只有绑定受信 rubric 的 deterministic grader 才能在 L-3 返回 correct/incorrect。
10. 产品不支持从 v0.5 带 Tutor 数据原地降级到 v0.4。官方 installer/updater 必须阻止；
    降级前须用 v0.5 prepare-downgrade 导出并清除独立 runtime DB。已手工降级者须重装
    v0.5 清理，或在完全退出应用后删除三个精确 runtime DB 文件。

## 1. 当前代码事实与差距

### 1.1 可复用事实

- `hermes_core/agent/graph_engine/engine.py` 的通用 Agent graph 每个 user turn 调用一次，
  编译时无 checkpointer；`TurnState` 是普通 Agent loop 的兼容合同。
- `hermes_core/providers/chat_completions.py` 已集中 provider/model/auth/API-mode 解析；Tutor
  可以复用解析与 transport adapter，但不能复用包含内部 retry/fallback 的 `call_llm()`。
- `learning.db` 已使用 WAL、`BEGIN IMMEDIATE`、busy retry、`secure_delete=ON`，并由
  `LearningExecutionContext` 强制注入 owner/space。
- `python/src/learning_owner.py` 已建立 desktop owner；desk Study route 不接收请求中的
  `owner_id`。
- `tauri/src/study.rs` 已是 Study loopback command 的唯一 Rust bridge，具有 path-id
  校验和结构化错误映射。

### 1.2 不能直接复用的部分

- `learning_activities` 只有 `activity_type/detail_json/created_at`，是历史事件，不含
  revision、状态或 pending interrupt；把它原地改成 run 表会破坏现有 quiz/review 读者。
- `learning_schema_version` 当前为 v1，且 v0.4 不会拒绝未知高版本、owner delete 只枚举五张
  v1 表；因此不能把 Tutor 表加进 `learning.db` 后声称 downgrade fail closed。
- 通用 Agent graph 的 interrupt 是停止正在执行的 agent/tool，不是等待学习者回答的
  durable learner check。
- 当前 `/api/desk/study/activities` 是历史事件摘要接口，不能静默改成 resumable run API。

## 2. 分层与代码归属

```text
Web Study surface
  -> Tauri study commands                 tauri/src/study.rs
    -> loopback activity-run routes       python/src/desk_server/routes/study_activity_routes.py
      -> desktop owner/context injection  python/src/learning_owner.py
        -> TutorActivityService           hermes_core/learning/tutor_activity.py
          -> TutorGraphEngine             hermes_core/agent/graph_engine/tutor_*.py
          -> LearningCheckpointStore      hermes_core/learning/checkpoint_store.py
             -> tutor_runtime.db adapter  hermes_core/learning/tutor_runtime_store.py
          -> LearningOperationCoordinator hermes_core/learning/operation_coordinator.py
             -> learning_coordination.db  shared by desk/gateway/cron processes
          -> TutorProviderPort            resolved single provider / single physical attempt
```

边界规则：

- Core 拥有 graph、状态机、CAS、预算、错误 taxonomy 与 activity 语义。
- Python desk 只负责 host owner、loopback request/response、单进程 execution registry 和
  readiness；不复制 graph 或 scheduler。
- Rust 只做认证后的 loopback bridge 与 path/body 大小校验。
- Web 只渲染 snapshot/interrupt 并提交显式动作；不在 localStorage 保存 checkpoint。
- `learning.db` 继续由 `LearningStore` 管既有 Study 真值；`tutor_runtime.db` 只管
  resumable run/checkpoint/provider reservations/outbox。跨库操作由 desktop composite
  service 编排，不能假装 SQLite 跨文件原子提交。
- `learning_coordination.db` 不是业务数据仓库，只保存 operation fence/journal。所有进程中的
  `LearningStore` constructor 都必须取得同一路径的 coordinator；不能保留绕过 coordinator 的
  raw writer 或“仅 desk 检查 fence”的路径。
- v0.5 L-2 是 toolless Tutor graph。未来节点需要工具时只能走现有 tool registry 与
  approval backend；不能由 learner answer 绕开审批。

## 3. 身份、版本和公开 wire

### 3.1 ID 与大小规则

- `space_id/activity_id/interrupt_id/idempotency_key`：1–128 字符，只允许
  `[A-Za-z0-9._:-]`；activity/interrupt 默认由 host 生成随机 opaque id。
- JSON request 上限 32 KiB；`goal` 上限 4,000 Unicode code points；单个 learner
  answer 上限 8,000；`input_refs` 最多 16 个。
- 所有 schema 都显式携带整数 `schema_version=1`；未知版本 fail closed。
- public wire 不接受 `owner_id`。Core `LearningActivityRequestV1` 在 host 校验 space 后
  注入 owner；模型永远看不到可写 identity 字段。

Core/DB 唯一 key type 固定为：

```text
LearningActivityKeyV1 {
  owner_id: host-injected string
  space_id: opaque id
  activity_kind: "tutor" | "review" | "practice"
  activity_id: opaque id
}
```

repository、checkpoint、attempt、interrupt binding 与 public path adapter 都消费这个 type；
不存在接受三元 key 或裸 `activity_id` 的 overload。

### 3.2 Start

`POST /api/desk/study/activity-runs`

```text
LearningActivityStartV1 {
  schema_version: 1
  space_id: opaque id
  activity_kind: "tutor"                  # review/practice 保留，L-2 返回 not_ready
  idempotency_key: opaque id
  goal: string
  input_refs: [
    { kind: "artifact" | "item" | "source",
      id: opaque id,
      version?: integer,
      sha256?: lowercase hex }
  ]
}
```

Host 对验证/NFC 规范化后的 start body（排除 `idempotency_key`，包含 schema、space、kind、
goal 与保持原序的 refs）执行 RFC 8785 canonical JSON，再保存
`request_fingerprint = sha256(canonical_utf8)`。idempotency namespace 固定为
`(owner_id, space_id, activity_kind, idempotency_key)`：

- 同 namespace + 同 fingerprint：返回既有 snapshot，不创建第二个 activity；
- 同 namespace + 不同 fingerprint：409 `study_activity_conflict`，reason 固定
  `idempotency_payload_mismatch`，零模型调用；
- 不同 `activity_kind` 是不同 namespace，但所有 DB key、repository key 与 route 都必须显式
  携带 kind，不允许靠 activity id 猜 kind。

引用由 host 在进入 graph 前解析/授权；checkpoint 只保存 pinned ref metadata 和 bounded
excerpt，不保存 raw source document。

### 3.3 Resume / recover

`POST /api/desk/study/activity-runs/{activity_kind}/{activity_id}/resume`

```text
LearningActivityResumeV1 =
  | {
      schema_version: 1
      space_id: opaque id
      expected_revision: integer >= 0
      mode: "answer"
      interrupt_id: opaque id
      answer:
        | { type: "free_text", text: string }
        | { type: "choice", selected: [opaque option id] }
        | { type: "step", text: string }
    }
  | {
      schema_version: 1
      space_id: opaque id
      expected_revision: integer >= 0
      mode: "recover"
    }
```

- `answer` 只接受 `waiting_for_learner`；必须同时匹配 owner、space、activity、interrupt
  和 revision。
- `recover` 只接受 `interrupted`；不得附带新 learner answer。
- duplicate/stale/cross-owner/cross-space 请求在 graph 前失败；不产生模型调用。

### 3.4 Cancel、get 与 list

- `POST /api/desk/study/activity-runs/{activity_kind}/{activity_id}/cancel`：body 为
  `{schema_version, space_id, expected_revision}`。CAS 胜者决定 cancel/answer race；失败方
  收到 conflict 并刷新 snapshot。
- `GET /api/desk/study/activity-runs/{activity_kind}/{activity_id}?space_id=...`：只返回同
  owner/space/kind 的 snapshot；跨 scope 与不存在统一为 404。
- `GET /api/desk/study/activity-runs?space_id=...&activity_kind=...&status=...&limit=...`：
  kind 必填、bounded list，不返回 checkpoint content。

对应 Tauri commands 冻结为：

`cmd_study_activity_start/get/list/resume/cancel`。

### 3.5 Snapshot 与 learner interrupt

```text
LearningActivitySnapshotV1 {
  schema_version: 1
  activity_id: opaque id
  space_id: opaque id
  activity_kind: "tutor" | "review" | "practice"
  status: "created" | "running" | "waiting_for_learner" | "interrupted"
        | "completed" | "blocked" | "cancelled"
  revision: integer
  label: string
  latest_output?: { kind: "explanation" | "feedback", markdown: string }
  interrupt?: PublicLearningInterruptV1
  terminal?: {
    outcome: "completed" | "blocked" | "cancelled"
    completion_basis?: "participation_only" | "deterministic_correct"
    reason_code?: enum
    budget_summary: {
      nodes_used / attempts_used / reserved_input_tokens / reserved_output_tokens
      reserved_wall_ms / active_elapsed_ms: non-negative integers
    }
  }
  created_at: ISO-8601
  updated_at: ISO-8601
}
```

Internal `LearningInterruptV1` 保留 requirements 要求的 owner/space binding：

```text
LearningInterruptV1 {
  schema_version: 1
  interrupt_id: opaque id                 # namespace: lint_...
  kind: "learner_check"
  owner_id: host-injected owner
  space_id: opaque id
  activity_kind: "tutor" | "review" | "practice"
  activity_id: opaque id
  checkpoint_revision: integer
  prompt: {
    markdown: string
    options?: [{ id: opaque id, label: string }]
  }
  expected_input: "free_text" | "choice" | "step"
  created_at: ISO-8601
}
```

Public projection删除 `owner_id`，其他 binding 保留。Tool approval IDs 继续使用其现有
namespace/store；任何 API 都不能拿 learner interrupt id 调 approval endpoint，反之亦然。

## 4. 状态机与并发语义

```text
start: created(r0) -> running(r1)
running -> waiting_for_learner(r+1) -> running(r+1)
running -> interrupted(r+1) -> running(r+1)       # explicit recover only
created|running|waiting_for_learner|interrupted -> cancelled(r+1)
running -> completed|blocked(r+1)
```

`completed/blocked/cancelled` 为终态；只能通过新的 Start request 创建新 activity，不能
resume 原 id。

### 4.1 CAS 规则

- 每次状态/graph checkpoint 修改都在同一个 `BEGIN IMMEDIATE` transaction 中更新
  activity run 与 checkpoint，条件为 `revision = expected_revision`。
- 成功 transition 将 revision 精确加一；run revision 与 checkpoint revision 永远相等。
- answer claim 先把 bounded answer/control action 写入 checkpoint、清掉 pending interrupt、置
  `running`，再执行确定性 branch。L-2 的 `continue` 与第二次 acknowledgement 不调用模型；
  只有第一次 `explain_again` 分支可在 reservation 成功后调用一次模型生成 bounded remediate。
  进程若随后崩溃，answer 已被消费一次，recover 从该 state 继续，不要求学习者重答。
- worker 完成节点时仍用 claim 后 revision CAS。若 cancel 已先提交，worker save 失败且
  不得复活 activity。
- client timeout 不能推断请求失败；客户端必须 GET snapshot，再决定重放。Start 依靠
  idempotency key；resume 依靠 revision/interrupt 做安全重放。

### 4.2 Crash / restart

- desk child 持有内存 execution registry：
  `(space_id, activity_kind, activity_id) -> execution_id/cancel handle`。
- persisted `running` 但 registry 无相同 execution 的记录被惰性 reconciliation 为
  `interrupted`；只由验证过 desktop owner 的 desk service 执行，不扫描/修改 gateway owner。
- reconciliation 不调用模型、不投递消息、不自动 resume。用户点击“继续”后提交
  `mode=recover`。
- disconnect 发生在 `waiting_for_learner` 时保持原 checkpoint 与 revision，不变更状态。

### 4.3 Product ActivitySummary 映射

- `waiting_for_learner -> awaiting_user`
- `interrupted -> interrupted`
- `created/running -> running`
- `blocked/completed/cancelled` 同名

`ActivitySummary` 是 `tutor_activity_runs` 的 bounded read model，`source_kind` 固定
`learning_activity`、`source_id={activity_kind}:{activity_id}`；它不复制 checkpoint 或
learner content。

## 5. Graph 合同

### 5.1 独立状态

新增 `TutorGraphStateV1`，不扩展普通 Agent `TurnState`：

```text
TutorGraphStateV1 {
  schema_version: 1
  graph_schema_version: 1
  policy_version: "tutor-v1"
  owner_id / space_id / activity_id / activity_kind
  phase: "start" | "explain" | "handoff" | "acknowledge" | "remediate" | "terminal"
  goal: bounded string
  input_refs: bounded pinned refs
  turns: bounded [{role: "tutor"|"learner", kind, content}]
  learner_evidence: bounded structured values
  pending_interrupt?: LearningInterruptV1
  branch?: {decision, policy_version, reason_code}
  remediation_count: 0 | 1
  budget: TutorBudgetStateV1
  latest_output?: bounded markdown
  terminal?: structured outcome
}
```

禁止保存 client、callback、DB handle、API key、tool credential、raw source bytes、gateway id、
browser tab id、完整 provider response、chain-of-thought 或自由 exception。序列化前递归
JSON-safe 校验、禁止字段扫描、单字段 cap 和总 checkpoint 256 KiB cap；超限进入
`blocked(checkpoint_too_large)`。

### 5.2 L-2 最小图

```text
START -> validate_context -> explain_bounded_unit -> handoff_to_learner
handoff_to_learner -> learner_control_check_1 interrupt -> END(segment)

resume(continue) -> validate_answer_binding -> acknowledge_submission -> complete
resume(explain_again, remediation_count=0)
  -> validate_answer_binding -> remediate_once -> handoff_to_learner
  -> learner_control_check_2 interrupt -> END(segment)
resume(check_2 acknowledgement) -> validate_answer_binding -> acknowledge_submission -> complete
```

- `handoff_to_learner` 是结构边；explain 不能直接自由续写到另一个概念。
- L-2 第一次 check 固定为 host-defined `choice`，opaque option 只映射
  `continue | explain_again`，不接受模型创建/改写 option id。`continue` 直接记录 `submitted`；
  `explain_again` 只在 `remediation_count=0` 时进入一次 bounded remediate，并把 count 原子置 1。
- 第二次 check 仍是 `evaluation_mode=acknowledgement`，只接受 host-defined `continue`；通过
  type/size/binding validation 后记录 `submitted`，终态为
  `completed(completion_basis=participation_only)`。第二次不得再次 remediate。
- 这保留上游开发计划 §L-2 的“terminal completed 或一次 remediate → learner_check”，但
  remediate 是学习者显式控制分支，不是“答错”判断。UI 只能说“继续/再解释一次/本节已结束”，
  不得说“答对/已掌握”，也不写 correctness/mastery/weak-point。
- L-2 没有自由文本 grader、没有 `evaluate correctness` 节点；correct/incorrect 与基于错误的
  remediate 仍从 L-3 才开放，避免实现者为任意 Tutor goal 猜 rubric。
- graph 每次只运行一个 bounded segment。持久化由 service 在 segment 前后显式执行，
  不使用 LangGraph 内部 pickle/checkpoint schema。
- L-2 不暴露 tools。未来工具节点只接收受信 `TutorServices` port，并复用既有 approval。

### 5.3 Deterministic evaluation truth（L-3 consumer contract）

所有 learner check 都携带 host 冻结的 `TutorCheckSpecV1`：

```text
TutorCheckSpecV1 {
  schema_version: 1
  check_id: opaque id
  expected_input: "free_text" | "choice" | "step"
  evaluation_mode: "acknowledgement" | "deterministic"
  rubric_ref?: {
    source_kind: "activated_quiz_item" | "trusted_builtin_rubric"
    source_id: opaque id
    source_version: integer
    source_sha256: lowercase hex
  }
  normalization_policy: versioned enum
  control_policy?: "continue_or_explain_once" | "continue_only"
  correct_action?: "complete" | "advance"
  next_unit_ref?: { source_id: opaque id, source_version: integer, source_sha256: hex }
}
```

- L-2 强制 `acknowledgement`，并禁止 rubric/correct_action/next_unit_ref；第一 check 的
  `control_policy=continue_or_explain_once`，第二 check 为 `continue_only`。两者都是 host code
  常量，忽略/拒绝模型输出的 option/rubric/expected answer。
- `deterministic` 只允许引用当前 owner/space 下的 activated quiz item，或代码中受审计的
  builtin rubric；AI draft、Tutor explanation、模型生成 expected answer、learner 自报均不是
  可信 rubric，且必须有 host-supplied `correct_action`。rubric missing/stale/hash mismatch 进入
  `blocked(source_missing)`，不得回退 LLM 判分。
- grader input 是 `(owner/space/kind/activity/check/revision, normalized_answer,
  pinned_rubric)`；identity 由 host 注入。grader 是 pure function，无模型、网络、工具或写入。

```text
DeterministicEvaluationResultV1 {
  schema_version: 1
  evaluation_id: opaque id
  activity_id / activity_kind / check_id / checkpoint_revision
  mode: "acknowledgement" | "deterministic"
  outcome: "submitted" | "correct" | "incorrect" | "invalid"
  grader_id: string
  grader_version: string
  rubric_ref?: exact pinned ref
  answer_fingerprint: sha256(canonical normalized answer)
  evidence_codes: bounded enum[]
  evaluated_at: ISO-8601
}
```

结果保存 provenance/fingerprint/code，不在 run summary、日志或 telemetry 复制 learner answer。
`correct_action/next_unit_ref` 由 host 从 activated plan/builtin rubric 构造；模型不能填写。
`correct_action=advance` 必须同时有可验证 pinned `next_unit_ref`，否则 check spec 无效。

L-2 control branch 固定为 `tutor-control-v1`：

- first `continue -> complete(participation_only)`；
- first `explain_again + remediation_count=0 -> remediate_once -> check_2`；
- check_2 `continue -> complete(participation_only)`；
- invalid/未知 option（包括 check_2 上伪造 `explain_again`）-> 原 check、revision+1 继续等待且
  零模型调用；stale check/revision 在 claim 前返回 conflict。任何路径都不能生成第二次 remediate。

L-3 deterministic branch policy 固定为 `tutor-branch-v1`：

- `submitted -> complete(participation_only)`；
- `correct + correct_action=complete -> complete(deterministic_correct)`；
- `correct + correct_action=advance -> advance(next_unit_ref)`；
- `incorrect + remediation_count=0 -> remediate_once`；
- `incorrect + remediation_count>=1 -> blocked(remediation_exhausted)`；
- `invalid -> 原 check、revision+1 重新等待且零模型调用`；
- rubric/next-unit missing、stale、hash mismatch -> `blocked(source_missing)`。

L-3 可消费 correct/incorrect；L-2 只能产生 submitted/invalid 与 host-defined control action，
不能产生 correctness。

### 5.4 Model generation 与 prompt injection

`TutorProviderPort` 只生成 bounded explanation/remediate prose；L-2 learner-control prompt 与 option
由 host template 生成。Port 复用现有 provider/auth/
transport 解析，但禁止调用带内部 retry/fallback 的 `call_llm()`；逐 attempt 合同见 §7.1。
模型只输出 versioned JSON/typed result；adapter 负责 schema/size 验证，模型自由文本不能决定：

- owner/space/activity/revision；
- learner check 是否匹配；
- pass/fail 或 advance/remediate 最终分支；
- tool approval；
- checkpoint schema、budget 或 terminal transition。

learner answer、source excerpt、whiteboard text和模型结果全部按不可信内容处理。Prompt
明确以 data delimiters 包裹，但真正安全边界是 deterministic validation/CAS，而不是提示词。

## 6. 独立 Tutor runtime persistence v1

### 6.1 为什么必须是独立数据库

v0.4 的 `learning.db` reader 只 seed `learning_schema_version=1`，没有“发现未知高版本即拒绝”
逻辑；`delete_owner_data()` 也只枚举 v1 的 spaces/artifacts/items/activities/migrations。把 Tutor
表直接加进该文件会造成两个不可接受事实：旧 runtime 仍会写同一 DB，且“删除全部学习数据”
会遗漏 checkpoint。因此冻结：

- `learning.db` schema 和 v0.4 五表合同不因 L-1 改动；
- 新文件固定为 `%LOCALAPPDATA%\com.kabuqina.app\kabuqina-home\tutor_runtime.db`，
  使用独立 `tutor_runtime_schema_version=1`、WAL、busy retry、ACL 和 secure-delete；
- v0.5 Tutor runtime 对未知高于 1 的 runtime schema fail closed；这个保证只属于新 reader，
  不再声称 v0.4 会拒绝 `learning.db` v2。

### 6.2 表与一致 identity

```sql
CREATE TABLE tutor_runtime_schema_version (
  singleton INTEGER PRIMARY KEY CHECK (singleton = 1),
  version INTEGER NOT NULL
);

CREATE TABLE tutor_activity_runs (
  owner_id TEXT NOT NULL,
  space_id TEXT NOT NULL,
  activity_kind TEXT NOT NULL,
  activity_id TEXT NOT NULL,
  schema_version INTEGER NOT NULL DEFAULT 1,
  idempotency_key TEXT NOT NULL,
  request_fingerprint TEXT NOT NULL,
  label TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL,
  revision INTEGER NOT NULL DEFAULT 0,
  current_interrupt_id TEXT,
  execution_id TEXT,
  provider_plan_hash TEXT,
  policy_version TEXT NOT NULL DEFAULT 'tutor-v1',
  terminal_code TEXT,
  completion_basis TEXT,                  -- participation_only|deterministic_correct
  remediation_count INTEGER NOT NULL DEFAULT 0,
  budget_nodes_used INTEGER NOT NULL DEFAULT 0,
  budget_attempts_used INTEGER NOT NULL DEFAULT 0,
  budget_reserved_input_tokens INTEGER NOT NULL DEFAULT 0,
  budget_reserved_output_tokens INTEGER NOT NULL DEFAULT 0,
  budget_reserved_wall_ms INTEGER NOT NULL DEFAULT 0,
  budget_active_elapsed_ms INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  terminal_at TEXT,
  CHECK (completion_basis IS NULL OR completion_basis IN
         ('participation_only', 'deterministic_correct')),
  CHECK (remediation_count BETWEEN 0 AND 1),
  CHECK (budget_nodes_used >= 0 AND budget_attempts_used >= 0 AND
         budget_reserved_input_tokens >= 0 AND budget_reserved_output_tokens >= 0 AND
         budget_reserved_wall_ms >= 0 AND budget_active_elapsed_ms >= 0),
  PRIMARY KEY (owner_id, space_id, activity_kind, activity_id),
  UNIQUE (owner_id, space_id, activity_kind, idempotency_key)
);

CREATE TABLE tutor_checkpoints (
  owner_id TEXT NOT NULL,
  space_id TEXT NOT NULL,
  activity_kind TEXT NOT NULL,
  activity_id TEXT NOT NULL,
  revision INTEGER NOT NULL,
  graph_schema_version INTEGER NOT NULL DEFAULT 1,
  state_json TEXT NOT NULL,
  interrupt_json TEXT,
  state_sha256 TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  PRIMARY KEY (owner_id, space_id, activity_kind, activity_id)
);

CREATE TABLE tutor_provider_attempts (
  owner_id TEXT NOT NULL,
  space_id TEXT NOT NULL,
  activity_kind TEXT NOT NULL,
  activity_id TEXT NOT NULL,
  attempt_id TEXT NOT NULL,
  segment_id TEXT NOT NULL,
  ordinal INTEGER NOT NULL,
  provider_id TEXT NOT NULL,
  model_id TEXT NOT NULL,
  api_mode TEXT NOT NULL,
  status TEXT NOT NULL,                    -- reserved|succeeded|failed|unknown
  reserved_input_tokens INTEGER NOT NULL,
  reserved_output_tokens INTEGER NOT NULL,
  reserved_wall_ms INTEGER NOT NULL,
  actual_input_tokens INTEGER,
  actual_output_tokens INTEGER,
  actual_latency_ms INTEGER,
  reason_code TEXT,
  reserved_at TEXT NOT NULL,
  completed_at TEXT,
  PRIMARY KEY (owner_id, space_id, activity_kind, activity_id, attempt_id)
);

CREATE TABLE tutor_projection_outbox (
  event_id TEXT PRIMARY KEY,
  owner_id TEXT NOT NULL,
  space_id TEXT NOT NULL,
  activity_kind TEXT NOT NULL,
  activity_id TEXT NOT NULL,
  event_type TEXT NOT NULL,
  payload_json TEXT NOT NULL,
  created_at TEXT NOT NULL,
  delivered_at TEXT
);
```

`learning_coordination.db` 使用独立 schema v1、WAL/busy retry/secure-delete，并对未知高版本
fail closed；下面两表不放在 runtime DB：

```sql
CREATE TABLE learning_operation_fences (
  operation_id TEXT PRIMARY KEY,
  owner_id TEXT NOT NULL,
  space_id TEXT NOT NULL DEFAULT '',          -- empty means whole owner
  operation TEXT NOT NULL,                    -- delete|full_import|runtime_restore
  phase TEXT NOT NULL,
  bundle_sha256 TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  UNIQUE (owner_id, space_id)
);

CREATE TABLE learning_operation_journal (
  operation_id TEXT PRIMARY KEY,
  phase TEXT NOT NULL,
  rollback_manifest_json TEXT NOT NULL,       -- bounded IDs/fingerprints, no learner content
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

索引必须包含完整 identity：

- `(owner_id, space_id, activity_kind, status, updated_at DESC)`；
- `(owner_id, status, updated_at DESC)` 只用于 owner-level ActivitySummary；
- checkpoint/attempt 不提供按裸 activity id 的全局索引或 enumeration API。

### 6.3 Port 与跨库投影

```text
LearningCheckpointStore.load(key) -> Checkpoint | None
LearningCheckpointStore.save(key, checkpoint, expected_revision) -> Checkpoint
LearningCheckpointStore.clear(key, expected_revision) -> None

LearningActivityRepository extends LearningCheckpointStore:
  create_activity(request, request_fingerprint, initial_checkpoint) -> Snapshot
  claim_execution(key, expected_revision, operation) -> Checkpoint
  reserve_provider_attempt(key, expected_revision, reservation) -> Checkpoint
  settle_provider_attempt(key, attempt_id, result, expected_revision) -> Checkpoint
  commit_wait(key, checkpoint, interrupt, expected_revision) -> Snapshot
  commit_terminal(key, outcome, expected_revision) -> Snapshot
  cancel(key, expected_revision) -> Snapshot
  reconcile_abandoned(owner_id, live_execution_ids) -> count
  get/list scoped snapshots

LearningOperationCoordinator:
  begin_write(owner_id, space_id, operation_lease=None) -> WriteGuard
  begin_read(owner_id, space_id, operation_lease=None) -> ReadGuard
  begin_operation(owner_id, space_id, kind, bundle_sha256?) -> OperationLease
  advance_operation(lease, phase, rollback_manifest) -> OperationLease
  finish_operation(lease) -> None
  recover_operations() -> bounded operation list
```

`TutorRuntimeStore` 必须让 run/checkpoint/attempt/outbox 的复合更新在同一个 runtime DB
transaction 内原子化。Terminal transaction 清 checkpoint、保留 bounded run summary、写 outbox；
outbox projector 再幂等追加不含回答/正文的 `learning_activities` event。跨两个 DB 不宣称原子：
crash 留下 undelivered outbox，重启重放，event id 去重。Port contract tests 用 in-memory fake 与
SQLite adapter 同一套用例。

Terminal transaction 必须同时写 `terminal_code`、`completion_basis`、`remediation_count` 与六个
numeric budget columns；blocked/cancelled 的 `completion_basis` 为 NULL。只有这些汇总值写入并与
attempt rows 核对一致后，才允许按 §6.5 retention 删除 attempt rows；run summary 和 public
`terminal.budget_summary` 因而有同一持久真值。

`LearningOperationCoordinator` 是跨进程正确性的强制 port，而不是 desk overlay：

- coordinator 路径必须从 resolved `LearningStore.db_path` 派生为同目录
  `learning_coordination.db`，不能再单独按进程 HOME 猜路径；因此任何共享同一 `learning.db` 的
  desk child、Gateway child、cron 与 tests 都必然注入/派生同一个 coordinator。
- `LearningStore._execute_write` 增加 owner/space/optional `OperationLease`，并覆盖
  create/set-space、artifact/item/activity、quiz/review、migration、import/delete 等**全部**公开
  mutation；outbox projector 也走同一入口。禁止生产代码 raw-open `learning.db` 写入。
- `LearningActivityRepository` 的 start/resume/cancel/reconcile 与 runtime get/list 同样先取对应
  coordinator guard；否则 composite fence 不能阻止新的 Tutor runtime row，也不能通过 Gate。
- 普通 write 先在 coordination DB `BEGIN IMMEDIATE`，检查 owner-wide fence 与 exact-space
  fence均不存在，并保持该 transaction/guard，随后提交 `learning.db` write，最后才释放 guard。
  operation 安装 fence 也先取得同一 `BEGIN IMMEDIATE`，因此不存在“检查后、写入前”竞态。
- create-space、current-space、migration、owner import/delete 等不局限单个既有 space 的 mutation
  使用 `space_id=''` owner guard；exact-space writer 总是同时检查 empty owner fence。
- composite 内部写只接受 coordinator 签发且匹配 operation/owner/space 的 opaque lease；Gateway
  和普通 desk/cron writer 没有 lease，命中 fence 返回 retriable
  `learning_operation_in_progress`，不得缓存后绕过重放。
- get/list/export 也先在 coordination DB 取 `BEGIN IMMEDIATE` read guard，检查 fence，并保持到
  `learning.db` read transaction 完成；因此 operation 安装 fence 会等待已开始 read，fence 后的新
  read fail closed，不会观察跨库中间态。固定锁顺序为 coordination → learning → runtime，任何
  路径不得反向获取。v0.5 可先接受这个全局串行化的保守实现；优化并发必须保持同一互斥证明。
- owner fence 与其任一 space fence、同一 space 的两个 operation 互斥；不同 space 可有持久 fence，
  但实际 DB write 仍由 coordination transaction 串行。fence/journal 在两个 Python 进程 crash 后
  继续存在，由 recovery owner 按 phase 接管。

### 6.4 Delete、降级禁令与恢复流程

- v0.5 `CompositeLearningDataService` 是 owner/space delete/import/restore 的唯一入口。它先由
  coordinator 原子写入持久 `OperationLease + fence + journal`；这会阻止两个 Python 进程内
  matching owner/space 的所有 Study/Tutor 读写，而不只是 Tutor start/resume。
- delete 按 `fenced → learning_deleted → runtime_deleted → compacted` phase 推进，每步只使用
  matching lease；最后才清 journal/fence。crash 后 recovery 在 fence 仍存在时从已提交 phase
  继续。因为安装 fence 前会等待现有 writer guard 退出、安装后又不允许新 writer，cleanup
  不会遗漏 Gateway 新写入，也不会误删 operation 开始后的新数据。
- active/interrupted/waiting checkpoint 在 v0.5 不做时间 TTL；由显式 cancel、composite
  space delete、composite owner delete 处理。archive space 不等于 delete。
- **不支持带 Tutor 数据原地降级到 v0.4。** 仓库当前 `tauri.conf.json` 已声明
  `allowDowngrades=false`，L-1/Q-3 必须用真实 v0.5 candidate + 官方 v0.4 artifact 证明 NSIS/
  updater 均拒绝低版本；配置事实本身不算通过。
- 支持的降级流程是 v0.5 “准备降级”：导出并回读校验 `StudyOwnerBundleV2`，同时验证其中
  可独立恢复的 `TutorRuntimeBundleV1` section/hash → 用户确认 → 取得 owner operation fence →
  close runtime handles → secure-delete/删除 `tutor_runtime.db`、`-wal`、`-shm` → 保留 schema v1
  `learning.db` → 清 fence → 卸载 v0.5 后安装 v0.4。回到 v0.5 后使用 §6.5
  `mode=tutor_runtime_merge`，不要求删除 v0.4 期间新增的学习数据。
- 已手工覆盖安装 v0.4 而未 prepare 的恢复：停止应用；首选重装 v0.5，执行 composite
  export/delete；若隐私目标只是清除且无需恢复，则在确认进程退出后精确删除上述三个
  `tutor_runtime.db*` 文件。不得宣称 v0.4 的“删除全部”覆盖 Tutor 数据。

真实旧版本矩阵必须使用 `CTL-G00` 冻结的官方 v0.4 artifact hashes 与
`v0.4.0^{}`/`f6229d96` runtime；自行从当前 source 改版本号构建不算旧版本证据：

1. 先由 v0.5 创建 pending/running/terminal Tutor fixtures，记录 runtime DB/hash/mtime；
2. v0.4 启动、Study 读写、export、owner delete 后，证明它不打开/修改 runtime DB；owner
   delete 留下 runtime DB 是**预期 red characterization**，用于钉住降级禁令；
3. 官方 installer/updater downgrade 被阻止；
4. v0.5 composite delete 同时清两库，且 WAL/SHM 无私密残留；
5. prepare-downgrade 后 v0.4 可正常读取/新增 learning.db 数据；重装 v0.5 后以
   `tutor_runtime_merge` 恢复 Tutor backup，证明 v0.4 新数据 byte-for-byte/row-for-row 保留。

### 6.5 Export/import 总量合同

新 `StudyOwnerBundleV2` 是单个 canonical JSON object：`learning_v1` 保留现有五 sections，
`tutor_runtime` 含 runs/checkpoints/attempt metadata；import 强制写入当前 owner，导入的
`running` 改为 `interrupted`、清 execution id，terminal run 不含 checkpoint。

完整 `mode=replace_empty_owner` import 继续要求该 owner 在两库都为空。它先完整
parse/validate/canonical-hash，随后由 coordinator 建立 owner fence/journal；按 phase 写
入前在 fence 内重新检查两库 owner 仍为空，再按 phase 写 `learning.db`、runtime DB，全部成功后
才清 journal/fence。crash/restart 发现未完成 journal 时，
在 fence 内按 bounded rollback manifest 清理由该 operation 插入的 keys 并 compact；因为安装
fence 后所有 desk/Gateway/cron writer 都被拒绝，cleanup 不会删除并发新数据。schema/size/hash
错误是零写入，进程崩溃也不会留下对新 reader 可见的半导入 owner。

同一 BundleV2 还必须支持 `mode=tutor_runtime_merge`，这是 prepare-downgrade 的无损恢复路径：

- 只消费已由 outer manifest 绑定 hash/count 的 `TutorRuntimeBundleV1` section，绝不导入、覆盖、
  删除或重新编号当前 `learning.db` 行；当前 learning owner 可以非空。
- 取得 owner fence 后完整预检 owner/四元 identity/idempotency namespace。目标 runtime 中同 identity 且同 row/
  state fingerprint 视为幂等；同 identity 异 fingerprint，或同 idempotency namespace 异
  request fingerprint，整批返回 conflict 且零写入。
- 所有 runtime rows 在 owner operation fence 下用单个 runtime DB transaction merge；因此
  crash 是全有或全无。已有、非冲突 Tutor rows 保留，备份 rows 追加，不采用“先清空 owner”。
- 恢复时 pinned space/source 已不存在或 hash 不匹配，不丢弃 activity：保留原 identity/ref，
  原子转为 `blocked(source_missing)`，在 owner-level Recovery list 可导出/删除但不可 resume。
  这不会创建虚假 mastery，也不会覆盖 v0.4 新内容。
- outbox 保留稳定 `event_id`；projector 在 coordinator guard 下先按
  `detail_json.projection_event_id` 查 existing learning event，再插入或标 delivered，使恢复重放
  不复制 terminal history。
- 测试必须在 prepare 后用真实 v0.4 新建 space/artifact/item/activity，再回到 v0.5 合并有
  waiting/terminal/missing-source/conflicting-id fixtures 的 Tutor section；验证 v0.4 新行完全不变、
  无冲突数据成功恢复、冲突整批零写入、重复 restore 幂等。

所有层使用同一 UTF-8 serialized-byte 口径：

| 项 | 硬上限 |
|---|---:|
| `learning_v1` 子对象 | 16 MiB（保留现有 core limit） |
| 单 checkpoint | 256 KiB |
| owner nonterminal checkpoints | 12 个、合计 3 MiB；每 space 最多 6 个 |
| terminal run summaries | 每 owner 最近 1,000 条、每条 ≤1.5 KiB |
| pending outbox | 正常 ≤32 条；payload ≤512 B；existing runs terminal 时最多短暂增至 44 条 |
| `tutor_runtime` 子对象 | 6 MiB |
| manifest/structural overhead | 1 MiB |
| 整个 `StudyOwnerBundleV2` | 24 MiB |

- 第 13 个 owner resumable activity 或单 space 第 7 个返回
  `study_activity_conflict(activity_quota_exceeded)`；不通过删 checkpoint 腾 quota。
- terminal 超过 1,000 条时，仅在 outbox 已 delivered 后删除最旧 run summary；既有
  `learning_activities` terminal event 仍保存历史。该 retention 在 UI/backup 文档公开。
- provider attempt rows 在 terminal/outbox commit 后折叠成 run 的 numeric budget summary 并
  删除；delivered outbox 立即删除。undelivered outbox 达 32 时暂停新 start（已有 run 仍可
  terminal），使 runtime subsection 在最坏的 12 个 existing run 全部 terminal 后仍受 6 MiB cap。
- L-1 把 Rust file metadata/read cap 从当前 10 MiB 调到 24 MiB；loopback Content-Length、
  desk parser、core canonical re-serialization 都用 24 MiB。任一子 cap 或总 cap 超限时在任何
  DB write 前拒绝；不允许 Rust 接受、core 再以不同数字拒绝。
- export 生成后必须用同一 validator 回读并核对 section counts + SHA-256 才报告成功。
- 测试至少覆盖：12 个接近 aggregate 3 MiB checkpoint cap + 1,000 terminal summaries 的
  export→delete→import round-trip；24 MiB 边界前成功/后失败；v1 10–16 MiB fixture 经新 Rust
  bridge 可导入；payload/count/单 record 任一超限均零部分写入。

## 7. 预算、停止与失败

### 7.1 Tutor provider port 与 write-ahead budget

| 预算 | 每次 start/resume | 单 activity 总上限 |
|---|---:|---:|
| graph nodes | start 6 / 每次 resume 4 | 14 |
| physical provider attempts | start 1 / first `explain_again` resume 1 / 其他 resume 0 | 2 |
| provider fallback candidates | 0 | 0 |
| reserved input tokens | 16,384/attempt | 32,768 |
| reserved output tokens | 2,048/attempt | 4,096 |
| provider wall reservation | 35 s/attempt | 70 s |
| active wall clock | start/resume segment 各 ≤45 s | 120 s，不跨用户等待累计 |
| learner checks | — | 2 |

`TutorProviderPort` 冻结为以下 plan / reservation / execution / settlement 接口，不能包一层
现有 `call_llm()`：

```text
resolve_plan(main_runtime) -> TutorProviderPlanV1
  # 只做本地 config/credential resolution；得到一个固定 candidate，无网络调用

reserve_attempt(activity_key, expected_revision, segment_id,
                candidate, input_token_reserve, output_token_reserve,
                wall_ms_reserve <= 35000)
  -> AttemptReservation + new_checkpoint_revision

execute_once(reservation, typed_request, timeout_s <= 35)
  -> TutorProviderResult

settle_attempt(activity_key, attempt_id, result_code, numeric_usage,
               expected_revision) -> new_checkpoint_revision
```

- `TutorProviderPlanV1` 在 activity create 时固定并保存 hash，只含 provider/model/api_mode/
  endpoint identity，不含 key。`provider=auto` 只能在 create 时本地解析为一个 candidate；
  recover 不重新 auto-detect，不切 provider/model。
- `execute_once` 使用专用 single-attempt client factory：OpenAI/Anthropic SDK
  `max_retries=0`，transport 不循环，credential pool/fallback 禁用。v0.5 fallback candidate 数为
  **0**；以后开放必须改 schema/policy/test，不能配置暗开。
- 每次物理调用前，`reserve_attempt` 在 runtime DB 同一 CAS transaction 中：检查 segment/
  activity remaining → 增加 checkpoint reserved counters → 插入 `status=reserved` attempt row →
  commit；只有 commit 成功才允许发 HTTP。
- L-2 不做 provider retry：start explanation 只有一次 reservation；只有学习者第一次选择
  `explain_again` 才可取得 activity 的第二次 reservation。SDK/transport 若单次
  `execute_once` 被测试发现产生第二个 HTTP request，Gate 直接失败。
- crash 留下的 `reserved` 在 recovery 时改 `unknown`，**永久按已消耗计算，不退款**；这样
  crash-before-call 虽保守损失一次 attempt/token/35 s wall reservation，但 crash-after-call
  永远不能绕过总预算。
- input reservation 使用已注册 model tokenizer；未知 tokenizer 采用
  `reserved_input_tokens = UTF-8 byte length` 的保守上界。output 在 request 的 `max_tokens`
  写入前全额预留。实际 usage 只用于观测，不返还 reserved budget。
- 每个 active segment 建立 process-monotonic deadline：
  `min(now+45s, activity_remaining_active_wall)`。调用 provider 前必须保留至少 10 s 给 typed
  parsing、attempt settlement、checkpoint/outbox commit；实际 timeout 为
  `min(35s, segment_remaining-10s, activity_remaining-10s)`。结果返回后不得再发新 HTTP，且在
  deadline 内完成落库；remaining ≤10 s 时不 reservation，直接持久化 `blocked(budget_exhausted)`。
- `budget_active_elapsed_ms` 在每个 node/settlement CAS 后按 process-monotonic elapsed 单调持久化。
  任何 crash 遗留的 `running` segment 在 reconciliation 时保守计满该 segment 的 45 s（不超过
  activity remaining）；reserved attempt 同时改 unknown，但其 35 s 已包含在这 45 s 内，不双算。
  等待学习者的时间不计；系统时钟回拨或 restart 都不能把已开始 segment 归零。
- start 的唯一 attempt 失败，第一次 remediate attempt 失败，或任何 reservation 不足，原子进入
  `blocked(provider_attempt_exhausted|budget_exhausted)`。`continue` 和第二 check acknowledgement
  不调用模型。
- usage event/attempt row 只含 provider/model id、token/call/latency 数值、outcome/reason code，
  不含 goal、answer、prompt、output、source ref、endpoint secret 或 whiteboard text。

### 7.2 Terminal reason codes

`completed` 无 reason；`cancelled` 使用 `user_cancelled`；`blocked` 只允许：

`provider_unavailable | provider_timeout | provider_attempt_exhausted | invalid_model_output |
budget_exhausted | source_missing | remediation_exhausted | checkpoint_too_large |
policy_rejected | internal_error`。

message 在 API/Web 层由 code 映射；数据库和日志不保存原始 exception/response body。

### 7.3 HTTP error taxonomy

| HTTP | code | 语义 |
|---:|---|---|
| 400 | `study_activity_invalid_request` | schema/size/id/action 无效 |
| 404 | `study_activity_not_found` | 不存在或不属于当前 owner/space |
| 409 | `study_activity_conflict` | stale revision、interrupt mismatch、非法 transition、idempotency payload mismatch、quota |
| 503 | `study_activity_not_ready` | capability/provider/runtime 未就绪且尚未创建 run |
| 500 | `study_activity_internal_error` | 已 redacted 的非预期 host 错误 |

进入 graph 后的 provider/budget 失败优先返回持久化的 `blocked` snapshot，而不是只抛 HTTP
错误，确保 restart 后仍有诚实结果。

## 8. Threat model

| 威胁 | 强制控制 | 必测失败 |
|---|---|---|
| owner/space/kind 注入 | public body 无 owner；host context 注入；DB/API 全部使用四元 identity | cross-owner/space/kind 返回 404 且零模型调用 |
| idempotency payload swap | canonical request fingerprint 与 scoped key 一起持久化 | 同 key/异 payload 409 且零模型调用 |
| stale/duplicate answer | exact interrupt + revision CAS；claim 前验证 | duplicate、旧 tab、重放只一方推进 |
| cancel race | cancel/answer/worker commit 同一 revision CAS | 任一确定胜出，terminal 不复活 |
| crash mid-resume | answer write-ahead + running checkpoint + explicit recover | restart 不丢 answer、不重复消费 |
| learner/tool interrupt 混淆 | 不同 type/namespace/store/API/UI；learner answer不含 approval | cross-use 全拒绝、零副作用 |
| prompt injection | untrusted data delimiters + typed output + deterministic branch/identity | 内容伪造 owner/pass/tool call 无效 |
| checkpoint 泄密 | allowlist schema、禁止字段递归扫描、size cap、terminal clear | secret/raw source/gateway id 序列化失败 |
| enumeration | scoped composite key；跨 scope 与 missing 同 404 | timing/response shape 不泄露存在性 |
| provider retry/fallback 绕预算 | single-attempt client、write-ahead reservation、fallback=0、unknown 不退款 | crash/SDK retry/fallback 不能超过 2 次物理请求 |
| resource exhaustion | request/checkpoint/owner quota、24 MiB bundle、hard budgets | oversized body/state/bundle 在写入或调用前拒绝 |
| log/telemetry 泄漏 | code/id/numeric usage only；exception redaction | caplog 不含 answer/source/prompt |
| cross-process write race | shared coordination DB；所有 LearningStore writer 先 guard；operation lease/fence 持久化 | desk/Gateway/cron 并发写在 delete/import/restore 中 fail closed |
| delete orphan | cross-process fence + 两库 phased cleanup；未完成不报成功 | DB/WAL 无可读残留、crash 可继续且不误删新写入 |
| schema downgrade | 独立 runtime DB + 官方 downgrade block + prepare/recovery；不声称 v0.4 fail closed | 真实 v0.4 red characterization + installer/recovery matrix |

## 9. Capability 与降级

- 新 capability id：`student-tutor-runtime`。L-1 只有 persistence/API 时仍为
  `candidate/not_ready`；L-2 start→interrupt→resume/cancel/restart 自动与 Windows 证据关闭后
  才标 ready。
- provider 未配置时本地 Study 仍可使用；Tutor start 返回 `study_activity_not_ready`，
  不创建假 running activity。
- pending activity 在 provider 后续失效时仍可查看/取消；resume 后原子进入
  `blocked(provider_unavailable)`。
- v0.5 release notes/Settings 明示“不支持带 Tutor 数据原地降级”；只有 §6.4 prepare flow
  与 §6.5 runtime-only merge restore 都完成后才提供降级 runbook。v0.4 的 delete/export 不被
  描述为 Tutor-aware。
- visual/scene 未冻结时使用普通 accessible form/choice/step UI；不阻塞后端合同。
- Web/Gateway 不认识新 schema 时显示 unavailable/recovery，不回退普通 Chat 自动继续。

## 10. L-1 / L-2 JIT slices

### 10.1 `CTL-B02 / L-1` · checkpoint + activity lifecycle

**目标：** 不调用真实模型，先交付 wire validators、repository、独立 runtime DB v1、CAS、
composite delete、bundle v2、downgrade prepare、desktop route/Tauri bridge 与恢复合同。

**允许路径：**

- 新建 `hermes_core/learning/tutor_contract.py`、`checkpoint_store.py`、
  `tutor_activity.py`、`tutor_runtime_store.py`、`operation_coordinator.py`；
- `hermes_core/learning/learning_store.py` 允许在 constructor/central `_execute_write`/read wrapper
  接入强制 coordinator，并接 bundle/composite service；不改 v1 schema、不逐业务方法复制 fence
  分支；对应 `hermes_core/tests/learning/**` 与 gateway/cron focused tests；
- 新建 `python/src/desk_server/routes/study_activity_routes.py`、修改 route registration、
  `python/tests/test_study_activity_routes.py`；
- `tauri/src/study.rs`、`tauri/src/lib.rs` 及 focused Rust tests；
- capability registry/test 与 schema/export docs。

**禁止热区：** `run_agent.py`、通用 `graph_engine/{contracts,engine,builder,nodes}.py`、
gateway adapters、C 轨 profile/registry/supervisor、Web scene components。

**退出测试：** runtime/coordination DB fresh/unknown-newer、四元 identity、fingerprint mismatch、
CAS、two-tab、duplicate/stale/cross-scope、cancel race、running→interrupted、desk/Gateway/cron
cross-process writer 与 composite owner/space delete/import race、crash phase recovery、24 MiB bundle
round-trip/caps、真实 v0.4 downgrade characterization + runtime-only merge recovery、WAL/busy、HTTP/
Tauri 错误映射；无 LangGraph/LLM 调用。

### 10.2 `CTL-B03 / L-2` · minimal Tutor graph

**目标：** 接上独立 Tutor graph 与 single-attempt provider port，交付真实
start→explain→learner control check→resume→participation-only complete，或一次显式
`explain_again` remediate→第二 check→complete，以及 cancel/restart；不判自由文本正确性。

**允许路径：**

- 新建 `hermes_core/agent/graph_engine/tutor_{contracts,ports,nodes,builder,engine}.py`；
- L-1 core service/repository 的窄接口；
- provider resolver + single-attempt client、write-ahead reservation 与 usage accounting；
- Study activity desk route 的 service wiring、capability readiness；
- 新 `hermes_core/tests/agent/test_tutor_graph_*.py` 与 focused Python/Rust tests。

**禁止热区：** 普通 Agent `TurnState/build_agent_graph/GraphEngine` 行为、旧 loop 兼容、
P/S UI、gateway delivery、whiteboard、kq-kp v2/H4（各自后续 slice）。

**退出测试：** deterministic control/acknowledgement golden、continue direct complete、一次
explain-again 后第二 check、禁止第二次 remediate、真实 LangGraph contract、provider unavailable/
timeout/invalid output、SDK retry=0/fallback=0、reservation crash/unknown、35 s call/10 s finalize/
120 s active deadline、每种 node/attempt/token cap、restart recovery、learner/tool interrupt
isolation、ordinary Chat 零变化，以及 Windows bundle import/smoke。

### 10.3 Hot-file owner

| 热区 | 单一 owner | 消费方式 |
|---|---|---|
| `tutor_runtime_store.py` / operation coordinator / bundle/composite delete | L-1 DB owner | `learning_store.py` v1 schema 冻结；所有 writer 消费 coordinator；P/S 从固定 commit 消费 port |
| `agent/graph_engine/**` | L-2 Tutor runtime owner | 不与普通 graph maintenance 并行 |
| `python desk activity routes` | B integrator | Web/Tauri 消费 versioned wire |
| `tauri/src/study.rs` + command registration | B bridge owner | 与 C 轨 `lib.rs` 变更串行 rebase |

## 11. Test matrix 与验收命令

L-1 最低矩阵：

- contracts：schema version、size/ID、四元 identity、request fingerprint、forbidden fields；
- repository：create/load/save/clear、revision 单调、atomic run/checkpoint/attempt/outbox；
- concurrency：two connections/process shape、duplicate answer、cancel/answer/worker 三方 race；
- lifecycle：每条允许/禁止 transition、terminal immutable、explicit new；
- recovery：crash before/after answer claim、running reconciliation、restart/import；
- governance：owner/space/kind isolation、24 MiB export/import near-cap round-trip、desk/Gateway/
  cron writer fence race、composite space/owner secure delete、真实 v0.4 unsupported-downgrade +
  runtime-only merge recovery；
- bridge：desk HTTP + Tauri path/body/error mapping。

L-2 最低矩阵：

- topology：explain/remediate 必经 `handoff_to_learner`；L-2 只允许 host control 触发一次
  remediate，不存在 correctness evaluate；
- interrupt：exact binding、最多一个 unresolved、最多两个 checks、tool approval cross-use；
- evaluation：L-2 只有 control/acknowledgement/submitted，UI/DB 不产生 correct/mastery；L-3
  deterministic fixture 验证 trusted rubric provenance 与 branch-v1；
- budget：每一种 cap、deadline remaining、finalize reserve、reservation crash、unknown 不退款都能
  确定性进入 blocked；
- provider：no-provider、timeout、仅 explain-again 可第二 reservation、SDK physical retry=0、fallback=0、
  invalid typed output、usage redaction；
- compatibility：普通 Chat/Agent graph golden 与 import contract不变；
- bundle：bundled CPython 能 import new modules 和 `langgraph==1.2.6`。

JIT plan 应给出实际 test node；预期命令族：

```powershell
python -m pytest hermes_core/tests/learning -q
python -m pytest hermes_core/tests/agent/test_tutor_graph_*.py -q
cd python; python -m unittest tests.test_study_activity_routes -v; cd ..
cd tauri; cargo test study; cd ..
python python/tools/verify_bundle_site_packages.py
git diff --check
```

## 12. Reviewer sign-off checklist

只有下列角色逐项签字后 `CTL-B01` 才能从 REVIEW 变为 DONE：

- **Study owner：** 状态机、显式入口、terminal/recovery 行为；
- **DB/schema owner：** 独立 runtime DB、CAS/outbox、composite delete、bundle caps/WAL；
- **Core reviewer：** 独立 Tutor graph 不改变普通 Agent graph/loop；
- **Security/privacy reviewer：** identity、replay、interrupt isolation、retention/redaction；
- **P/S consumer：** Practice 与 Whiteboard 能只靠 versioned activity/revision/ref 合同接入，
  不猜 private state。

任何 reviewer 若要求改变 identity key、状态集合、revision 语义、terminal retention、public
wire 或 graph/Agent 边界，必须先更新本设计和 `DECISIONS.md`，不能在 L-1/L-2 代码中暗改。
