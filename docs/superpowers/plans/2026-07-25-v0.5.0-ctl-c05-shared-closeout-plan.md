# CTL-C05 shared single-truth closeout JIT plan

**Control:** CTL-C05 / C-4 shared closeout
**Base:** `4005b2c3` (`main`, 2026-07-25)
**Owner:** Codex, authorized by the product owner on 2026-07-25
**Worktree:** `.worktrees/c-track-c05-shared-closeout`
**Branch:** `codex/v050-c05-shared-closeout`

## Objective

Close the shared platform truth after all removed-platform leaves are `DONE`.
Python, Core, Rust, Web, dependency declarations, lock data, active docs,
license/advisory records, and the runtime-pruning verifier must agree on the
exact retained set:

- `mainland_cn`: `weixin`, `qqbot`, `dingtalk`
- `sea`: `telegram`, `whatsapp`, `email`

C05 does not claim that every retained transport is release-ready. Transport
repair and real-platform smoke remain CTL-C06; legacy credential/job/home
cleanup and upgrade semantics remain CTL-C07.

## Allowed paths

- `python/requirements-desktop.txt`, `python/build_bundle.ps1`,
  `python/tools/verify_runtime_*.py`, and focused `python/tests/`
- `hermes_core/pyproject.toml`, `hermes_core/uv.lock`, shared platform/profile
  registries, and focused Core contract tests
- shared Rust profile/supervisor and Web gateway registry contracts when a
  single-truth mismatch is demonstrated
- C0 manifest/audit records, active product documentation, `DECISIONS.md`,
  this plan, and one C05 evidence document

## Forbidden scope

- no retained-adapter redesign, DingTalk SDK migration, or real credential smoke
- no destructive deletion or migration of legacy credentials, profiles,
  sessions, jobs, or home targets
- no A/B/Study schema or product-experience changes
- no leaf-level sign-off or repeated leaf bundle
- Codex does not execute the owner-reserved bundle; the owner runs the single
  integrated `main` bundle after the source review stack is merged

## Implementation sequence

1. Audit exact retained sets, dependency declarations/lock/runtime inputs,
   license/advisory coverage, active docs, and runtime verifier coverage.
2. Fix demonstrated shared-truth mismatches and add negative/relationship
   regressions.
3. Refresh generated C0 ledgers once, run no-refresh audit, focused Core/Python/
   Rust/Web checks, source hygiene, and dependency consistency checks.
4. Submit one C05 source/evidence stack. After merge, hand the owner the single
   `.\python\build_bundle.ps1 -Verify` command and artifact checks for reuse by
   G01.

## Rollback and compatibility

Rollback is the C05 commit series only. Existing removed-platform data remains
untouched and unsupported targets remain fail-closed. A dependency may be
removed only when no retained runtime consumer remains; shared dependencies are
kept with their retained consumer documented.

## Exit evidence

- exact six-platform truth agrees across Python/Rust/Web/Core
- removed direct dependencies and exclusive runtime packages are absent
- retained direct dependencies required by the current desktop runtime are
  declared and runtime-verifiable, with C06 deferrals explicit
- active product docs do not advertise removed platforms
- license/advisory/dependency records match the resolved bundle inputs
- C0 no-refresh audit and focused non-bundle tests pass
- one owner-run fresh integrated bundle on merged `main` passes pruning/import
  checks; its metadata is recorded for G01 reuse
