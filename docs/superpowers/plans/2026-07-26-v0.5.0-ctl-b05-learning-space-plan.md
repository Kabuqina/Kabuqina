# CTL-B05 · Learning Space S-0 functional design and S-1 dependency plan

> **状态：** S-0 DONE · S-1 WAITING（缺 E2/X2）
>
> **固定 base：** `478a30178d7ef2e9cdc8472b8494fb555f31dddb`
>（B01-B04 + 2026-07-26 current `main` copy/review integration）
>
> **Branch / worktree：** `codex/v050-b05-learning-space` /
> `D:\project\Kabuqina\.worktrees\b-track-b03`
>
> **Accountable owner：** Codex / Study functional architecture

## 0. Dependency truth 与本轮退出点

总计划允许 S-0 与 E-0 并行，也允许 S-2 在 S-0 后、不等待 E/Tutor。可见 S-1 则明确要求
E-2 + X-2 + S-0。当前总控板仍记录 E2 未冻结、Context/Handoff owner 未分配，X2 也没有
可引用 Gate evidence；主仓 A-track 的原型/文案进展不能替代这两个 versioned contracts。

因此本轮只完成 S-0：

- 以当前 source、D-5 installed evidence 与 canonical prototype 为事实输入；
- 冻结 component/view-model tree、同构 semantic/flat layout 与完整状态矩阵；
- 给每个 scene object 填 source/action/empty/narrow/a11y mapping；
- 冻结 Companion 单状态边界；
- 冻结 whiteboard artifact/runtime/security/size/export/delete/recovery contract；
- 记录当前 chunk baseline 与 S-1/S-2 budgets；
- 产出可供 E/X/V/S-2 消费的 S-0 functional design。

S-0 完成后不把 CTL-B05 或 S-1 标为 DONE。总控板应显示 S-0 DONE、S-1 WAITING(E2/X2)，
随后 B 轨可拉取不依赖可见 shell 的 L-3/L-4/L-5/P-2 或 S-2；整轨 Review 仍统一在最后。

## 1. Current-source facts

- canonical route 固定 `/study/:spaceId/{flyleaf,plan,learn,practice,evaluate}`；非法 slug 与 unknown
  space 有显式恢复面；`/chat` 始终可达。
- `StudyRepository` 已是 Web 的 typed truth boundary；五页分别消费 student state、plan/items、
  M5 artifacts/knowledge points、flashcards/quizzes、evaluation/wrongbook/activity。
- Practice 当前由 `StudyDeskPage` 接管产品 chrome；其他四页仍由 `StudyTopBar +
  StudyLifecycleNav + StudyPageOutlet` 渲染。S-0 必须把这种 current-source 差异写入 mapping，
  不能假设 S-1 已经统一。
- route loading/error 支持 previous/stale 保留；Practice 有 dirty-leave guard；space switch 使用
  request generation，失败保留原 space。
- Web 已锁 `katex@0.17.0` 并用于 derivation；没有 production whiteboard/canvas dependency。
- current built output：initial 1,554,980 raw / 470,606 gzip；StudyRoute 122,224 / 32,539；
  CodePractice 358,630 / 121,591；Derivation 261,118 / 77,860 bytes。
- Learning envelope v1 上限 512 KiB，现有 kind 没有 whiteboard；owner/space artifact/item/activity、
  export/import/delete 与 operation fence 可复用，但 S-2 必须先增加 exact contract，不能塞 opaque JSON。

## 2. 允许路径与禁止热区

允许：

- 新增 S-0 functional design/spec 与本 JIT plan；
- 更新总控板/DECISIONS 的 dependency、scope 与 frozen contract；
- 为事实核验运行 read-only source/chunk/test commands。

本轮禁止：

- 修改 `web/src/`、prototype、locales、AppScaffold、route 或 repository；
- 自行补写 E2 context/return、X2 focus/keyboard/dirty contracts；
- 安装 whiteboard/canvas dependency 或修改 package lock/bundle；
- 修改 learning schema、增加 whiteboard runtime、Tutor graph 或 production scene art；
- 把现有 Practice desk 当成 S-1/P-3/V-5 已完成。

## 3. Tasks 与 commit 边界

### Task 1 · S-0 source evidence and dependency gate

- 对照 D-5、current route/repository/pages、B02-B04 runtime 与 current chunk；
- 列出 inherited truth、current deviations、E/X/V ownership 与 S-only decisions。

### Task 2 · Semantic/view-model/state mapping

- 冻结 DOM order、landmarks、container-query mapping 与 flat fallback triggers；
- 五页、shell objects、activity/interrupt/draft/whiteboard 的 source/action/state/a11y matrix；
- 所有 visual metaphor 必须有普通 DOM 等价物。

### Task 3 · Whiteboard and Companion gates

- exact whiteboard scene schema/caps/lineage/CAS/idempotency/export/delete/recovery；
- element allowlist 与 URL/HTML/JS/image/plugin denylist；
- DOM+KaTeX default，第三方 dependency 只有单独供应链 Gate 后才允许；
- 杯子只做 Chat anchor，不成为 runtime/notification/state owner。

### Task 4 · Handoff and scheduling

- S-0 记 DONE；S-1 保持 WAITING(E2/X2)；
- 明确 E/X/V/S-2/L/P consumer inputs 与不得猜测的 open fields；
- 更新总控板，后续拉取 independent runtime slice。

**Commits：**

1. `docs(b): plan CTL-B05 Learning Space dependencies`
2. `docs(study): freeze S0 Learning Space functional design`

## 4. Verification

```powershell
git diff --check
rg -n "S-1.*WAITING|E2|X2|whiteboard|Companion" <plan> <design> <control-board>
```

Review checklist：

- route/repository/five-page truth matches source；
- current Practice chrome difference disclosed；
- loading/empty/stale/error/offline/interrupt/dirty/recovery all mapped；
- flat fallback does not wait for or invent V art；
- whiteboard exact caps fit envelope/export/import and reject executable/external content；
- secure delete and crash recovery have a storage owner, not browser-only hope；
- S-1 is not started or declared done without E2/X2 evidence。

## 5. Stop rules

Stop and report if S-0 requires changing E/X product semantics, if current source has no owner/space-safe
storage primitive for the proposed S-2 design, or if an external canvas dependency is unavoidable before a
license/supply-chain/WebView2/size review. Otherwise prefer the existing DOM+KaTeX path and continue.
