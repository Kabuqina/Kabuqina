# v0.5.0 执行总控板：三流并行、Gate 驱动与派工规则

> **性质：** v0.5.0 的派工与集成控制面。本文不增加功能范围，不替代各专项计划，
> 只把既有路线压缩成可分配的工作包、WIP 上限、依赖门、共享热区和 release blockers。
>
> **状态：** Active v3（2026-07-25）。状态栏只按可核验交付物更新；“已有详细计划”、
> “进入设计”或“完成若干代码”均不自动等于 Gate 通过。
>
> **版本目标：** 交付一个成熟、可理解、可恢复、可升级的 Study-first 产品；不以
> “route 已存在”“平台从 UI 隐藏”或“hero 图很好看”代替产品完成。

## 0. 如何使用本文件

### 0.1 权威顺序

发生冲突时按以下顺序裁决：

1. owner 已冻结的产品/渠道决定；
2. [v0.5.0 开发计划](2026-07-17-v0.5.0-development-plan.md)的版本范围和 release 门；
3. E/C/X/V/L/P/S/R 等专项计划的领域合同；
4. 本文件的当前派工、WIP 和 merge train；
5. 每个运行时切片开工前单独编写的 JIT implementation plan。

本文可调整“谁先做、谁等待、谁合并”，但不能静默改变专项计划已经冻结的产品或数据
语义。范围或合同变化必须回写上游计划/`DECISIONS.md`，然后再更新本板。

本文派工卡统一使用 `CTL-A* / CTL-B* / CTL-C* / CTL-G*`；不带 `CTL-` 的 E/C/L/P/S/X/V/R/Q
编号始终指专项计划原始 slice。例如 `CTL-C04` 是总控板的长尾删除工作包，原计划 `C-4`
仍专指 credential/job/home-channel 兼容，二者不得混用。

### 0.2 状态词

| 状态 | 含义 |
|---|---|
| `READY` | 前置证据已具备，可立即分配；尚未开始 |
| `ACTIVE` | 已有唯一负责人、明确 base commit/worktree 和交付物 |
| `REVIEW` | 交付物与证据已提交，等待指定 Gate reviewer 验收 |
| `BLOCKED` | 缺少硬前置或出现 stop-rule；必须写明解除条件 |
| `WAITING` | 已知前序尚未完成，不应提前开工 |
| `DONE` | Gate reviewer 已按退出证据验收，不是作者自报完成 |
| `PARKED` | 不占 v0.5.0 当前活动槽；恢复时必须做等量 scope swap |

不使用模糊百分比。一个工作包只有“尚未满足退出证据”或“已满足并验收”。

## 1. 当前真实快照

| 面 | 当前事实（更新至 2026-07-25） | 结论 |
|---|---|---|
| v0.4 基线 | `v0.4.0^{}` = `f6229d96` 与官方 artifact hashes 已冻结；现有 Rust/Web/runtime/Windows 证据、provenance/legacy-upgrade accepted gaps 及 updater 密码学/保管边界已逐项核验 | [`CTL-G00` conditional-GO sign-off](../progress/2026-07-19-v0.5.0-ctl-g00-release-baseline-signoff.md) 已签字；runtime merge gate green，v0.5 发布仍受 `CTL-G01/Q-3 → Q-4` 约束 |
| 产品/UX | canonical 与 A06a durable commits 保持冻结；A06b pre-art 完整前端已在当前 worktree 实现并通过作者侧 Web/browser/实际 Tauri 路由、exact-return 与重启恢复验证，尚未提交。真实五题型写入、原生 200% 与故障注入仍待集中 Review/owner 验收；E3/E4/X4 仍无关闭证据 | `CTL-A01 DONE`；`CTL-A06a REVIEW · NOT DONE`；`CTL-A06b ACTIVE · IMPLEMENTED/UNCOMMITTED/REVIEW READY`。剩余人工项与独立裁决阻止 DONE 和 E-I2/V-5/A 轨完成宣告 |
| UI 美术 | V 为 Draft；Gate E4/X4 前只允许场景母版、现状、token/资产事实与实现债务整理 | V0A/V0B/V1 之后按相应 Gate 启动，不越权发明行为 |
| Study | v0.4 contracts 可消费；L-0、P-0、S-0 尚需各自 Gate 证据 | 当前只占用一个 Study 槽，优先 L-0 |
| 渠道裁剪 | C0 manifest、stop-producing、removed-platform convergence 与 C05 shared closeout 已完成并通过合并 Review/owner bundle | `CTL-C05 DONE`；C06 retained readiness 已启动 |
| profile 实现 | Python/Rust/Web 已收敛为 mainland_cn = Weixin/QQBot/DingTalk、sea = Telegram/WhatsApp/Email 的 exact retained set | 集合已稳定；真实 transport、Windows smoke 与 release readiness 仍由 C06 证明 |
| retained 渠道 | 钉钉仍有 SDK/`websockets<13`/bundle 风险；WhatsApp、Email 等还需真实 transport 与 Windows 证据 | 删除完成不等于 C 轨完成；retained readiness 是 release blocker |
| PPT | A-0/A-1 研究可保留；U-21/U-22 production baseline 已移至 v0.5.x，U-23 为后续候选 | 不占 v0.5.0 工程 WIP，不用 PPT 美化换掉产品脊柱/Study Must |

## 2. 只管理三条执行流

| 执行流 | 吸收的原轨 | 本版本必须交付 | 当前槽 |
|---|---|---|---|
| **A · 产品体验与设计** | E + X + V + E-I + Study UX Must | Study-first 书桌、Chat/Create/成果往返、完整任务/恢复合同、production-ready 视觉冻结 | `CTL-A01` DONE；`CTL-A06a` REVIEW；`CTL-A06b` ACTIVE（实现完成，未提交，集中 Review ready） |
| **B · Study 能力** | L + P + S | Tutor M7、Practice M8、Learning Space M9 的真实纵向闭环 | `CTL-B01/B02/B03/B04` DONE；`CTL-B05` S-0 DONE / S-1 WAITING(E2/X2)；`CTL-B06` backend phases ACTIVE |
| **C · 平台、升级与发布** | C + R；Q 为横向 Gate | 双 profile、平台物理裁剪、retained readiness、兼容/升级/bundle/release | `CTL-C01/C02/C03a/C03b/C03c/C04/C05/C06/C07` DONE；`CTL-G00` DONE；post-A 六平台 smoke 已转交 `CTL-G01/Q-2 → Q-3`；`CTL-G01` WAITING |

`U` 不是第四条执行流，而是 intake/停车场。功能只有在优先级、替换成本、上游合同和
独立 JIT plan 都明确后，才能进入 A/B/C 的一个槽。`Q` 是所有流共同经过的验收门，
不是持续增加功能的第四条线。

## 3. WIP 与派工硬规则

1. **全局最多 3 个产品工作包 ACTIVE：每条执行流最多 1 个。** 先完成或阻塞转交，
   才从同一流的 `NEXT` 拉取下一项。
2. `CTL-G00` 是 part-time release stewardship，不算第四个功能包；如果它需要实际代码修改，
   该修改占用 C 流的活动槽。
3. 每个工作包只有一个 accountable owner；协作者可以多人，但不能有两个最终裁决者。
4. 运行时代码开工前必须有独立 JIT plan、base commit、允许路径、禁止热区、测试命令、
   回滚/兼容说明和退出证据。
5. 一个共享热区同一时间只有一个 owner。消费者从已记录的固定 commit 起步，不从他人的
   dirty worktree 复制文件。
6. 机械删除、schema/行为语义变化、scene UI 与依赖/bundle 收口分 commit。C 轨
   removed-platform leaves 已结束；自 2026-07-25 起不再为子 leaf 建独立
   Gate/sign-off/bundle 循环，改为 C05/C06/C07 阶段工作包各一次合并 Review。
7. Gate 只认测试、截图/原型记录、schema/manifest、构建物和 reviewer sign-off；
   不认“应该可以”或“看起来完成”。用户于 2026-07-26 取消 B 轨切片间 Gate：B 子卡以
   durable author evidence 转 `DONE` 并继续，独立 reviewer sign-off 集中到整个 B 轨收口。
8. 发现 P0、安全/数据丢失风险、静默改投、隐式 course binding 或无法升级时立即停止该
   slice，不用临时 fallback 掩盖。

## 4. 第一批派工（现在）

### 4.1 总览

| ID | 状态 | 建议 accountable owner | 工作包 | 本轮退出点 |
|---|---|---|---|---|
| `CTL-A01` | `DONE` | Product/UX 设计 task | Study↔Chat 修订原型与 §13 可验证交接 | owner 已冻结 canonical 行为；`d529dc4e` 提交 canonical/legacy 拆分、复核 memo 与拆分后冒烟证据；不冒充 E3/E4 |
| `CTL-A06a` | `REVIEW` | Codex（Web/Study integration author） | frozen desk React rescue + production Practice integration | 独立复审关闭 P1/P2；durable commits `42a7eccf`、`1198915b`、`887078d6` 已形成并排除 C 轨 manifest。production fixture cleanup 已在当前 worktree 关闭并加入 bundle gate，待提交固化；真实 Tauri 人工矩阵延后到 pre-art 完整前端后一次执行，在此之前阻止 DONE；[review handoff](../handoffs/2026-07-23-v0.5.0-study-desk-production-review.md) |
| `CTL-A06b` | `ACTIVE` | Codex（Web/Study integration owner） | pre-art 完整前端：course Chat exact return、制作/成果工作夹、Activity/Recent、响应式/恢复状态 | 实现与作者验证完成，尚未形成 durable commit；Web `30/133`、lint/build、browser wide/720/360、runtime sync、实际 Tauri route/exact return/restart passed。真实五题型写入、原生 200% 与故障注入仍阻止 DONE；[JIT plan](2026-07-24-v0.5.0-ctl-a06b-pre-art-frontend-plan.md)；[集中 review handoff](../handoffs/2026-07-24-v0.5.0-pre-art-frontend-review.md) |
| `CTL-B01` | `DONE` | Codex（Study backend/learning runtime owner，用户于 2026-07-19 授权启动） | L-0 Tutor graph 实施设计 | 非作者 Gate reviewer 已验收 `fb20a04d`；[design v3](2026-07-19-v0.5.0-ctl-b01-tutor-runtime-design.md) + [sign-off evidence](../progress/2026-07-19-v0.5.0-ctl-b01-design-evidence.md) 冻结 cross-process fence、上游 L-2 once-remediate、runtime-only restore、terminal schema 与 deadline 合同；`CTL-B02` 转 `READY` |
| `CTL-B02` | `DONE` | Codex（Study backend/DB owner；用户于 2026-07-19 授权启动） | L-1 checkpoint/activity lifecycle | 非作者 reviewer 已关闭 R1–R5 并完成 [Gate sign-off](../progress/2026-07-23-v0.5.0-ctl-b02-tutor-lifecycle-signoff.md)；[实现证据](../progress/2026-07-19-v0.5.0-ctl-b02-tutor-lifecycle-evidence.md)记录 core 350、B02 Python 17、Rust study 13、真实 subprocess recovery、共享 Unicode fixture 与 forged history zero-write。2026-07-23 当前 1397.5 MB bundle 的标准 smoke、STT 与 B02 runtime import 通过，关键 bundled source hash 与当前源码一致；`CTL-B03` 前置条件满足 |
| `CTL-B03` | `DONE` | Codex（Study backend owner；用户于 2026-07-26 授权启动） | L-2 minimal Tutor graph | [JIT plan](2026-07-26-v0.5.0-ctl-b03-minimal-tutor-graph-plan.md) 与 [author evidence](../progress/2026-07-26-v0.5.0-ctl-b03-minimal-tutor-evidence.md)：独立 deterministic graph、single-attempt provider、B02 write-ahead/CAS/recovery、desktop start/resume 已闭环；Core focused 70、agent+learning 2312、ordinary graph 18、Python focused 27、Rust study 15 通过；Python full 仅余固定 base 的 C0/C07 manifest drift；不接 Web/Gateway Tutor，材料进入 B 轨整体 Review |
| `CTL-B04` | `DONE` | Codex（Study backend/Practice owner；B 轨切片连续开发） | P-0/P-1 Practice ④⑤ contracts/services | [JIT plan](2026-07-26-v0.5.0-ctl-b04-practice-contracts-plan.md) + [author evidence](../progress/2026-07-26-v0.5.0-ctl-b04-practice-evidence.md)：grader truth/provenance、activated hint ladder、幂等 zero-call hint、Level ⑤ pending/draft boundary 与 trusted desktop hint wire 已闭合；Core focused 161、Learning 383、Python 25+13、Rust 16 通过；production evaluator、L-3/P-2/P-3 与 Web 明确保留给后续卡，材料进入 B 轨整体 Review |
| `CTL-B05` | `WAITING` | Codex（Study functional architecture owner） | S-0 functional design；S-1 semantic shell | [JIT plan](2026-07-26-v0.5.0-ctl-b05-learning-space-plan.md) + [S-0 frozen design](../specs/2026-07-26-v0.5.0-learning-space-functional-design.md)：source/data/semantic/flat/state/object、Companion single-state、whiteboard schema/quota/export/delete/recovery/dependency gate 已冻结；S-0 DONE，S-1 仍严格等待 E2/X2，未修改 Web/A-track 热区 |
| `CTL-B06` | `ACTIVE` | Codex（Study backend runtime owner） | L3/L4/L5/P2 + S2/S3 backend closeout；P3/S1/S4 dependency-bound | [JIT plan](2026-07-26-v0.5.0-ctl-b06-study-runtime-closeout-plan.md)：L-3 deterministic Practice runtime、L-5 retention、L-4 zero-call backend post-node、S-2 owner-scoped whiteboard runtime 与 S-3 revision-bound constrained Tutor whiteboard port 已完成；正在汇总整轨 Review 证据。L-4 surface rollout/H1 retirement、Level ⑤ evaluator 及 S-1/P-3/S-4 仍分别等待 Web parity、独立预算与 E2/X2/V5，不接 Web/A 热区 |
| `CTL-C01` | `DONE` | Codex（本 task，owner 于 2026-07-18 授权） | C-0 全 surface + 依赖 + 持久化 machine manifest | evidence `45edb247` 的 contract、observed-source 与 23 项单测通过；五方 cross-layer review 与非作者 Gate reviewer 已签字 |
| `CTL-G00` | `DONE` | Codex（本 task，用户于 2026-07-19 授权核验） | v0.4 baseline 与 v0.5 merge gate 核验 | [sign-off](../progress/2026-07-19-v0.5.0-ctl-g00-release-baseline-signoff.md) 冻结 `f6229d96`、官方制品哈希、可复用证据、accepted gaps、updater trust 边界与 hotfix 回补规则；conditional GO 不冒充 v0.5 release approval |
| `CTL-C02` | `DONE` | Codex（C integrator，用户于 2026-07-19 授权实施） | C-1 stop-producing/autostart | [implementation evidence](../progress/2026-07-19-v0.5.0-ctl-c02-stop-producing-evidence.md) 与 [Gate sign-off](../progress/2026-07-19-v0.5.0-ctl-c02-stop-producing-signoff.md)；child profile、single-adapter config/core filter、Tauri producer guards 与 Web focused tests 经修复复审后通过，accepted deferrals 明确归入 C06/C07/G01 |
| `CTL-C03a` | `DONE` | Codex（C integrator，用户于 2026-07-19 授权实施） | Discord adapter removal leaf | [implementation evidence](../progress/2026-07-19-v0.5.0-ctl-c03a-discord-removal-evidence.md) 与 [Gate sign-off](../progress/2026-07-23-v0.5.0-ctl-c03a-discord-removal-signoff.md)：首次 Gate blocker 已关闭，复审基线 `3292a7af` 的 fresh-install、autostart、retained voice、runtime absence、current-main integration 与 C0 audit 全部通过；accepted deferrals 明确归入 C05/C07/G01 |
| `CTL-C03b` | `DONE` | Codex（C integrator，用户于 2026-07-23 授权继续开发） | Feishu/Lark removal leaf | [JIT plan](2026-07-23-v0.5.0-ctl-c03b-feishu-lark-removal-plan.md)、[implementation evidence](../progress/2026-07-23-v0.5.0-ctl-c03b-feishu-lark-removal-evidence.md) 与 [Gate sign-off](../progress/2026-07-23-v0.5.0-ctl-c03b-feishu-lark-removal-signoff.md)：复审基线 `b941c81e` 的 strict cleanup、verified-only marker、1,372.9 MB fresh runtime、Feishu/Lark absence、retained imports 与 C0 audit 全部通过；accepted deferrals 保持归属 C03c/C04/C05/C07/G01 |
| `CTL-C03c` | `DONE` | Codex（C integrator；Reviewer sign-off 2026-07-25） | WeCom bot/callback removal leaf | [JIT plan](2026-07-23-v0.5.0-ctl-c03c-wecom-removal-plan.md)、[implementation evidence](../progress/2026-07-23-v0.5.0-ctl-c03c-wecom-removal-evidence.md) 与 [C03c+C04 combined sign-off](../progress/2026-07-25-v0.5.0-ctl-c03c-c04-combined-signoff.md)：WeCom source/producers 已删除，Weixin hard boundary 保持，review blockers 已关闭 |
| `CTL-C04` | `DONE` | Codex（C integrator；Reviewer sign-off 2026-07-25） | long-tail families + IRC/Teams plugin removal | [combined plan](2026-07-24-v0.5.0-ctl-c04-long-tail-removal-plan.md)、[implementation evidence](../progress/2026-07-24-v0.5.0-ctl-c04-long-tail-removal-evidence.md) 与 [combined sign-off](../progress/2026-07-25-v0.5.0-ctl-c03c-c04-combined-signoff.md)：C04a-e source/tool/plugin/dependency 已删除；fixed allowlists、legacy fail-closed、Core 253、Python 18、Rust 21、Web contract 与 C0 audit 经复审通过 |
| `CTL-C05` | `DONE` | Codex（C integrator）+ owner reviewer sign-off 2026-07-26 | shared registry/profile/dependency/license/active-doc + one integrated bundle closeout | [JIT plan](2026-07-25-v0.5.0-ctl-c05-shared-closeout-plan.md) 与 [signed evidence](../progress/2026-07-25-v0.5.0-ctl-c05-shared-closeout-evidence.md)；merged-main bundle `verified=true`、1424.0 MB，runtime/import/STT/inventory 通过；ffmpeg digest provenance 显式转 G01/Q |
| `CTL-C06` | `DONE` | Codex（retained readiness reviewer） | retained mainland + SEA implementation/bundle readiness | [combined sign-off](../progress/2026-07-26-v0.5.0-ctl-c06-c07-combined-signoff.md) 已签；owner `main` bundle verified，六平台真实账号矩阵因依赖 A-track 完整产品 surface 延迟并转交 `CTL-G01/Q-2 → Q-3`，release 前必须补齐 |
| `CTL-C07` | `DONE` | Codex（upgrade/data compatibility reviewer） | credential/job/home-channel/upgrade | 首轮五项 finding 已全部关闭并由 [combined sign-off](../progress/2026-07-26-v0.5.0-ctl-c06-c07-combined-signoff.md) 验收；[JIT plan](2026-07-26-v0.5.0-ctl-c07-legacy-upgrade-plan.md)、[evidence](../progress/2026-07-26-v0.5.0-ctl-c07-legacy-upgrade-evidence.md)；installed-artifact upgrade 留 `CTL-G01/Q-3` |

### 4.2 `CTL-A01` · Study↔Chat 原型修订

**输入：**

- [设计修订 brief](../handoffs/2026-07-18-v0.5.0-study-chat-design-revision-brief.md)；
- [产品 UX/UI handoff §13](../handoffs/2026-07-18-v0.5.0-product-ux-ui-figma-handoff.md#13-本轮收尾记录2026-07-18)；
- [canonical Study ↔ Chat 灰阶交互原型](../prototypes/2026-07-18-v0.5.0-study-chat-low-fi.html)；
- E/X/V 计划与已确认书桌母版。

**本轮范围：** 只修原型与交接证据，不改 production React/Python/Rust。direct full Chat 是
可用 baseline/fallback；sidecar 继续为 `TO-TEST`，除非真实连续性、focus、窄窗和可用性证据
支持冻结。

**必须交付：**

- 修订后的 HTML 与 handoff 状态声明；
- Stop/partial answer、return focus、modal/inert/focus trap、窄窗/200%、unread/latest、
  fault/recovery 等修订 brief 验收记录；
- direct full Chat 与 sidecar 的对照结论、未解决 P1/P2 和归属；
- 明确说明该证据关闭了什么、仍未关闭 E3/E4/X4/V5 的什么。

**退出：** canonical 原型文件可读取；Product/UX reviewer 将该原型标为可进入 E3 的有效
输入；修订 brief 中没有未处理 P0；handoff 不再含互相矛盾的“已批准/仍待修订”声明。

#### 2026-07-18 plan-session 接收记录

**处置：`PLAN-SESSION ACCEPTED AS E3 SEED`；`CTL-A01 EXIT NOT YET MET`。** 这不是
E3/E4/X4/V5 或生产验收。本轮从真实交互路径
完成下列走查，浏览器控制台 `error/warning = 0`：

| 路径 | 本轮确认 | 仍未冻结 |
|---|---|---|
| `W01` | Study-first 首屏同时呈现课程/下一步、材料、复习、小娜杯、制作/成果与 Activity/Recent | 五秒 first-sight 仍需真实参与者 |
| `W03A/W03B` | HTML fixture 由单一 state 驱动多个 presentation；400px Chat 下 overlay/compress 均无横向 overflow，课程 context 一致 | 不证明 production state reuse；sidecar 价值、杯子 anchor、overlay/compress、卡片盒退让与 breakpoint 全部 `TO-TEST` |
| `W04 → 实际 W06 → 实际 W07` | Stop 后 partial、session/context、transcript 与 draft 保留；“跳到最新”到达末端；展开完整 Chat 后仍是同一会话，返回将焦点交还 `练习 3 · 第 2 步` | 静态 frame 下拉不能证明连续性；真实 backend pending、跨重排语义 scroll anchor 与跨窗口恢复仍为 `OPEN` |
| `M01` | `role=dialog`、`aria-modal`、背景 inert、Escape 与 invoker focus restore 成立 | 目标 Windows WebView 的原生 Tab loop、IME 与 screen reader 未验证 |
| `N01/Z01` | narrow 为非 modal full route，显式 return；`390×844` 与 `640×450 × 2` 等价 fixture 均无横向 overflow，transcript/composer 可达 | `Z01` 不是 Windows 系统 200%；真实 200%、reduced motion 与窄窗设备复验仍为 Gate |

进入 production slice 前保留以下 `P1 / OPEN` gates：

1. Send/Stop 互斥控件切换时必须显式保住键盘焦点，或用同一 focusable control 切换状态；
2. sidecar↔full 不得只保存绝对 `scrollTop`，应以 message/turn anchor 保住用户阅读位置；
3. streaming transcript 的 `role=log`/live-region 策略必须经 screen reader 验证，避免逐 chunk 播报；
4. 真实 backend 收起/展开连续性，以及 Windows 原生 Tab、IME、screen reader、系统 200% 和
   reduced motion 必须留在 E3/E-I6 验收门；
5. 本次指定链没有覆盖 `W05A/B/C`、`W07b`、`W08`、`G01`；blocked/network/missing-target、
   direct-full 独立路径与 ordinary/course Chat 区分不得从 handoff 文字反推为本轮已验证。

因此 `CTL-A01` 暂留 `ACTIVE`：内容已被 plan session 接收，但按本板规则，进入 `REVIEW`
还需要可引用 commit、Product/UX accountable-owner 复核，以及把 handoff §13 的状态措辞明确为
“修订完成、等待 E3/owner 验收”，不能把 `REVISION REQUIRED` 误读为仍有 P0，也不能误读为已批准。

#### 2026-07-23 `CTL-A01` 关闭与 production follow-on

上述 2026-07-18 的 `ACTIVE` 记录保留为历史。owner 随后冻结了 canonical 学生主流程与交互
合同；canonical/legacy 文件已拆分，拆分后宽屏主路径、K0、hint ladder、exact return、窄屏/
200% fixture、焦点和 console 冒烟通过，并由 commit `d529dc4e` 固化。关闭证据见
[canonical prototype review](../handoffs/2026-07-23-v0.5.0-canonical-prototype-review.md)。
因此 `CTL-A01` 现为 `DONE`。这只关闭原型行为与制品，不关闭 E3/E4/X4/V5 或 production。

后续实现以 `CTL-A06a` 占用 A 流唯一活动槽。该卡是在接管中断的前端 rescue、并经 owner
授权进入 Phase 2 后补登记的 retrospective control card；它没有事先建立独立 JIT plan/
worktree，也不豁免 E4/X4/V5。独立 reviewer 必须决定提交前是否补 JIT boundary、如何拆分
commit，以及哪些实现可作为正式 Gate 的输入。该包已经：

- 将 frozen canonical desk 迁入 `web/src/study/desk/`；
- 接入真实 `/study/:spaceId/practice`、既有 `StudyRepository` 与 quiz/activity/event truth；
- 为当前题检查增加 backward-compatible 的可选 `item_ids` bridge；
- 完成作者侧 Web/Core/Python/Rust 自动化、浏览器主路径与同态视觉对照。

当前为 `REVIEW · TAURI MATRIX DEFERRED TO PRE-ART GATE · NOT DONE`：两轮 P1/P2 findings 已关闭并完成作者侧回归，
且 durable commits `42a7eccf`、`1198915b`、`887078d6` 已形成；owner 尚未测试真实路由/功能。第二轮修复明确冻结
`completed → 清 recovery`、`incorrect/ungraded → 保留 recovery + needs_revision 离开保护`。
完整 findings 处理表、文件地图、必跑矩阵和退出格式见
[Study desk production review handoff](../handoffs/2026-07-23-v0.5.0-study-desk-production-review.md)。
`CTL-A06a` 是进入 M2 的首个生产纵切片候选；它不得被写成 `E-I1/E-I2/S-1/P-3/V-5`
或 A 轨整体完成。其当时延后的结构化 context、同一 session 连续性与 exact return 已由
`CTL-A06b` 在当前未提交 worktree 实现，并完成实际 Tauri route/exact-return/restart smoke。
A06b 的真实五题型写入、原生 200% 与故障注入仍待集中验收；在 durable commit、独立 Review
和 owner/Gate 裁决前，A06a/A06b 都不得标记 `DONE`。

### 4.3 `CTL-B01` · L-0 Tutor graph 实施设计

**范围：** 只冻结结构化辅导 runtime 的实现合同，不在此包修改 Tutor hot path。

**必须交付：**

- start → explain/check → interrupt → resume/cancel → terminal 状态机；
- owner/space scoped checkpoint、activity lifecycle、revision/CAS、delete/restart 行为；
- ordinary Chat 与显式 Tutor 的边界；
- deterministic grader、LLM draft、prompt injection、压缩/成本和失败降级边界；
- L-1/L-2 的 JIT slice 拆分、测试矩阵与 hot-file owner。

**退出：** Study owner + reviewer 能对 schema、状态转换、并发/崩溃恢复、成本上限和威胁模型
逐项签字；P/P2 与 S/S3 能消费合同而不猜接口。

### 4.4 `CTL-C01` · C-0 machine manifest

**范围：** 审计，不做物理删除。覆盖 producer、consumer、依赖、持久化、测试、文档和 bundle。

**machine manifest 每项至少包含：**

`surface`、`product_shells`、`gateway_platforms`、`profiles`、`decision`、`reason`、`owner`、
`source_paths`、`runtime_dependencies`、`persisted_data`、`network_hosts`、`credential_keys`、
`jobs/home_channel`、`removal_slice`、`verification`。

**必须交付：**

- mainland：Desktop shell + Weixin/QQ Bot/DingTalk；
- SEA：Desktop shell + Telegram/WhatsApp/Email；
- Discord、Feishu/Lark、WeCom、所有长尾及 IRC/Teams plugins 的完整退场图；
- 当前实现与目标的 diff，至少显式记录 SEA fallback、mainland Feishu/WeCom 残留、
  DingTalk 缺口；
- 四个共享边界审计：WeCom crypto↔Weixin、Discord media/voice↔WhatsApp、
  SMS/home-channel↔Email、Webhook/API server↔Desktop loopback；
- v0.4 runtime/site-packages/installer/startup/network/credential surface 基线；
- 可直接生成 C-1、C-2a/b/c、C-3 家族、retained readiness、C-4 的任务卡。

**退出：** Gateway、Python policy、Rust shell、Web Settings、bundle/release reviewer 对同一张
exact allowlist 签字；未知平台/动态 enum/import 失败关闭而不是落入通配保留。

### 4.5 `CTL-G00` · release baseline steward

**范围：** 只核验 v0.4 baseline 是否允许 v0.5 runtime slice 合入；不借机扩展 v0.4 或 v0.5。

**必须交付：** baseline commit、已有自动/手工/Windows 证据、accepted gaps、updater
trust-root/正式发布物状态，以及“哪些 v0.5 文档/审计可继续、哪些 runtime merge 仍阻塞”的
一页结论。

## 5. NEXT 队列与拉取条件

| 顺序 | ID | 工作包 | 拉取条件 | 退出证据摘要 |
|---|---|---|---|---|
| A1 | `CTL-A02` | E0-E2 evidence/contract gate audit | `CTL-A01` DONE；`CTL-A06a` 已进入 REVIEW，A 流活动槽释放 | surface facts、IA、context/source/deliverable/handoff/restart 有 owner、版本和 fallback |
| A2 | `CTL-A03` | J1-J5 + first-sight/keyboard/narrow E3 验证，关闭 E4 | `CTL-A02` DONE | P0=0；P1/P2 有归属；owner/Product/UX/FE/BE/UI sign-off |
| A3 | `CTL-A04` | X0-X4 Study task/state/focus/keyboard/recovery 冻结 | E2 可消费；L/P/S 状态事实可用 | Study 关键任务无 P0；X4 handoff 冻结 |
| A4 | `CTL-A05` | V-0A/V-1；随后 V-0B/V-2..5 | global 部分等 E4；Study detail 等 X4 | shell brief、tokens/a11y、状态/资产矩阵、V5 handoff |
| A5 | `CTL-A06` | E-I1/E-I2 JIT + production slices | E4 + E2；V 对应冻结；`CTL-A06a` 只提供可复用实现输入，不豁免 Gate | Desk shell/default/Activity-Recent 与 session context/return 真实纵切 |
| B1 | `CTL-B02` | L-1 checkpoint/activity lifecycle | `CTL-B01` DONE；v0.4 runtime merge gate green | CAS/owner/delete/crash/restart tests |
| B2 | `CTL-B03` | L-2 minimal Tutor graph | `CTL-B02` DONE | start/interrupt/resume/cancel/restart E2E |
| B3 | `CTL-B04` | P-0/P-1 Practice ④⑤ contracts/services | L-0 可消费；X 对应交互合同 | grader truth、hint/evaluation pending、prompt/cost tests |
| B4 | `CTL-B05` | S-0/S-1 semantic Learning Space shell | E2 + X2 + S0；不等待最终美术 | route/repository 不变、flat fallback、a11y/perf |
| B5 | `CTL-B06` | L3/P2/P3 + S2/S3/S4 收口 | 按开发计划 dependency chain；S4 等 V5 | Tutor/Practice/Whiteboard/scene 的成熟纵向闭环 |
| C1 | `CTL-C02` | C-1 stop-producing/autostart | `CTL-C01` DONE；v0.4 baseline 可引用 | stale credential 不启动/扩网/发送；新写入拒绝 removed target |
| C2 | `CTL-C03a/b/c` | Discord、Feishu/Lark、WeCom adapter 退场 | `DONE` | 历史 leaf 已验收；不再新增 leaf Gate |
| C3 | `CTL-C04` | 长尾家族 + IRC/Teams plugin 退场 | `DONE` | 与 C03c 合并 Review 已签字 |
| C4 | `CTL-C05` | shared registry/profile/dependency/bundle closeout | `CTL-C03c/C04 DONE` | Python/Rust/Web/requirements/license/active-doc 单一真值；一次 fresh integrated bundle |
| C5 | `CTL-C06` | retained mainland + SEA readiness | C05 `DONE` 后拉取 shared-spine 实现；此前只允许 adapter-local evidence preparation | SEA 不回落；钉钉可发布；六个外部渠道分 profile smoke |
| C6 | `CTL-C07` | C-4 credential/job/home-channel/upgrade | C05 `DONE` + C06 retained set/readiness 稳定 | 无静默改投；旧 job 可解释；显式清理；v0.4 upgrade |
| G1 | `CTL-G01` | Q-1..Q-4 integrated release train | C05/C06/C07 与对应 A/B 纵切完成 | automation、WebView2、复用 integrated bundle、upgrade、稳定窗口、release decision |

同一执行流严格按 WIP 拉取，不表示整张表完全串行。跨流 A/B/C 可并行；表内写明的依赖
必须满足。

## 6. C 轨后续管理方式（2026-07-25 生效）

removed-platform leaf 阶段已经结束。C03c 与 C04 以
[一份合并 Review sign-off](../progress/2026-07-25-v0.5.0-ctl-c03c-c04-combined-signoff.md)
同时关闭；历史上的“每个 leaf 独立 worktree、独立 Gate、独立 bundle”规则不再适用于
后续 C 轨。

```text
CTL-C01/C02/C03a/C03b/C03c/C04 DONE
                    ↓
CTL-C05 shared single-truth closeout + one integrated bundle
                    ↓
CTL-C06 retained mainland/SEA readiness
                    ↓
CTL-C07 compatibility cleanup + v0.4 upgrade
                    ↓
CTL-G01 integrated release train
```

后续规则：

- C05/C06/C07 各自是一个阶段工作包；子任务可拆 commit，但只交一份汇总 evidence、
  接受一次合并 Review，不建立子 leaf sign-off 队列。
- C 流仍保持同流 WIP=1。C06 可提前准备不改 shared spine 的 adapter-local evidence，
  但 shared profile、Rust supervisor、Web registry、requirements 与 bundle 由 C
  integrator 串行修改。
- fresh integrated bundle 只在 C05 生成一次；G01 验证并复用该制品。只有源码或
  dependency inputs 后续改变时才重建，不按子任务机械重复。
- source Review、runtime artifact proof 与 release approval 分层记录：C05 关闭单一真值，
  C06 关闭 retained transport readiness，C07 关闭兼容/升级，G01 才作 release decision。

## 7. 共享热区与唯一 owner

| 热区 | 冲突来源 | 唯一 owner / 合并规则 |
|---|---|---|
| Web shell：`web/src/main.tsx`、`AppScaffold`、`index.css`、locales | E-I1、S-1、Study UX Must、V integration、C Web 清理 | Web Shell owner；先合 C 的 route/Settings clean-base，再让 E/V 消费 |
| Chat/Study context、route、repository、API | E-I2/E-I3、X/U、Study frontend | Context/Handoff owner；其他分支只消费 E2 versioned contract |
| `state.db`/`learning.db`、migration、owner/space/CAS | E-I2/I4/I5、L/P、draft/review | DB/schema owner 先落公共 migration base；UI/service 再并行 |
| gateway registry/config/run/delivery/home channel | C-1、所有平台删除、retained repair | C integrator 独占 shared spine；平台 owner只碰 adapter/tests |
| `python/src/*profile*/*gateway*/*network*`、Rust supervisor/QR、Web Settings registry | C-1 / CTL-C05 / CTL-C06 | C integrator 串行收口，禁止各平台自行改 exact allowlist |
| requirements、bundle scripts、license/installer | 平台删除、retained SDK、release | C integrator + release captain；只在 leaf 稳定后集中修改 |
| document/PPT writers、typed output hook | E-I4、U-21/U-22 | v0.5 先 typed deliverable；PPT production 延期并与 E-I4 串行 |
| Study semantic shell 与 scene art | S-1、V/S-4 | semantic/flat 先行；production scene 等 V5，不同时重构同一组件 |

任一 task 若需要进入不归自己的热区，先交接口/patch 请求，不直接扩大 scope。

## 8. 版本里程碑

| Milestone | 必须关闭 | 允许进入下一阶段的判断 |
|---|---|---|
| **M0 · 可控开工** | `CTL-G00` 明确；`CTL-A01` 有效；`CTL-B01`、`CTL-C01` DONE | 有可信 baseline、三流合同/manifest，runtime 不靠猜测开工 |
| **M1 · 产品/平台合同冻结** | E2/E4、X4；C-1；L-1 | 产品 context/return、Study 状态、profile/autostart 与 checkpoint 真值冻结 |
| **M2 · 首个成熟纵切** | E-I1/I2；L-2；S-1；C removal leaf + retained profile 基础；V foundations | 用户可从书桌进入真实学习/Chat并返回；Tutor 可恢复；removed 渠道不再产生 |
| **M3 · v0.5 feature complete** | Study Must、E-I3..5、V5/S4、C-4、R compatibility | 产品主线完整，双 profile/升级/视觉/数据合同均有证据 |
| **M4 · Release candidate** | Q-1..Q-3 全绿 | clean install、v0.4 upgrade、WebView2、bundle、a11y/perf/security 全绿 |
| **M5 · Stable release** | Q-4 稳定窗口 + owner release sign-off | 窗口内无 schema/platform deletion/scene architecture 变化 |

## 9. Release blockers

以下任一未关闭，v0.5.0 不得宣称“成熟产品”或进入正式 release：

- E4、X4、V5 无可引用 sign-off，或 J1-J5/Study task 仍有 P0；
- Tutor M7、Practice M8、确定性 grader、interrupt/resume/restart 未形成真实闭环；
- `state.db`/`learning.db` migration、owner/space/CAS 或 v0.4 upgrade 不安全；
- SEA 仍回落 mainland，或 Python/Rust/Web/bundle retained set 不一致；
- 钉钉仍是“源码保留但 SDK/transport disabled”，或任一 retained 渠道没有 profile smoke；
- removed 平台仍有 live source、route、dynamic import、依赖、runtime package、credential
  autostart、network host 或 bundle/license surface；
- 旧 job/home target 被静默改投，或旧 credential 会自动启动/扩网；
- public/branded build、installer、Windows WebView2、a11y、性能、安全、隐私门未关闭；
- feature complete 后仍进行 schema migration、平台大删除或 scene architecture 变更。

## 10. PARKED / 不得偷跑

- U-18 Companion 新六态/合并；
- U-21/U-22 PPT production template baseline，除非 E4 与全部 Product spine/Study Must 已关闭
  且记录等量 scope swap；
- U-23 PPT 高阶语义/插画母版；
- 新 Dashboard、完整 Report Center/素材库、全局搜索、自由编辑器；
- Gateway Tutor UI、隐式 gateway→Study binding、跨设备同步、多人白板；
- 任何没有进入 U register、没有 displaced item、没有 JIT plan 的新增功能。

A-0/A-1 PPT moodboard/license/sample research 可作为非工程研究继续，但不能成为 v0.5.0
release claim，也不能占用 A/B/C 的活动实现槽。

## 11. 派工卡模板

给新 task 时复制以下模板；未填完整不得开工：

```markdown
你负责 v0.5.0 总控板工作包：<ID / 名称>。

权威输入：
- docs/superpowers/plans/2026-07-18-v0.5.0-execution-control-board.md
- <专项计划/交接文件>

Accountable owner：<一人>
Base commit：<hash>
Worktree/branch：<独立路径/分支>
允许修改：<精确目录/文件族>
禁止修改：<共享热区/其他用户文件>
前置 Gate：<证据链接>
必须交付：<文档/代码/schema/manifest/测试/截图>
验证命令：<明确命令>
退出证据：<总控板对应条目>
Stop rules：<P0/数据/安全/依赖条件>

先只读核验前置并写 JIT plan；发现前置不成立时标 BLOCKED，不自行扩 scope。
完成时回报：commit、changed files、测试数字、证据路径、未解决风险、建议下一卡。
```

## 12. 更新与回报节奏

- **分配时：** 填 owner、base commit、worktree、状态 `ACTIVE`；同时锁定热区 owner。
- **发生变化时：** 只更新状态、阻塞条件、证据链接和下一卡，不重写专项计划内容。
- **进入 REVIEW：** 必须附 commit、测试数字、构建/原型证据和已知风险。
- **验收后：** Gate reviewer 标 `DONE`，release captain 更新 merge train 的
  `base commit → consumer branches → integration result`。
- **每次拉 NEXT：** 先检查全局 WIP=3、同流 WIP=1、前置 Gate、热区冲突和 v0.4 merge gate。
- **稳定窗口：** 只接受 release blocker 修复；任何新功能回到 U/PARKED。

## 13. Owner 分配栏

| 职责 | 实际负责人 | 当前任务 | 备注 |
|---|---|---|---|
| Product/UX accountable owner | owner + 独立 Review session | `CTL-A06a REVIEW`；`CTL-A06b REVIEW READY/UNCOMMITTED` | 集中 Review handoff 已准备；真实五题型写入、原生 200% 与故障注入仍阻止 DONE；不替 E/X/V Gate 宣告整体冻结 |
| UI art owner | 待分配 | 等 `CTL-A03/CTL-A04` | E4/X4 前仅做允许的基础审计 |
| Study backend owner | Codex（本 task；B 轨连续开发） | `CTL-B01/B02/B03/B04 DONE`；`B05 S-0 DONE/S-1 WAITING`；`B06 ACTIVE` | B06 只推进 ready backend phases；visible S-1/P-3/S-4 继续等待 E2/X2/V5，整轨最终统一 Review |
| Gateway/platform integrator | Codex（C integrator；C06+C07 reviewer sign-off 2026-07-26） | `CTL-C01/C02/C03a/C03b/C03c/C04/C05/C06/C07 DONE` | 六平台真实账号 smoke 已转交 `CTL-G01/Q-2 → Q-3`；C-track sign-off 不代替 G01 installed-artifact upgrade 或最终 release decision |
| Web Shell owner | Codex（A06a/A06b implementation author） | `CTL-A06b IMPLEMENTED · AUTHOR VERIFIED · UNCOMMITTED` | Web/browser gates、runtime sync、实际 Tauri route/exact-return/restart 已完成；等待 durable commit 与独立 Review |
| Context/Handoff owner | 待分配 | WAITING | E2 versioned contract 的实现 owner |
| DB/schema owner | Codex（B02-B04 单一 owner） | `CTL-B02/B03/B04 DONE` | B04 只在现有 learning activity PK 上增加 coordinated bounded insert-once public seam；未迁 schema、未改 Tutor runtime DB |
| Release captain | Codex（CTL-G00 evidence reviewer） | `CTL-G00 DONE`；`CTL-G01` WAITING | 已签 v0.4 baseline；后续 accountable owner 仍须维护 merge train、Q gates、私钥恢复证明与稳定窗口 |

## 14. 当前派工动作

1. `CTL-A01` 已由 owner 冻结并以 `d529dc4e` 固化 canonical/legacy 拆分及复核证据，现为
   `DONE`。`CTL-A06a` 已由独立 reviewer 关闭 P1/P2、接受实现提交与 P3 follow-ups，当前为
   `REVIEW · NOT DONE`；
   [production review handoff](../handoffs/2026-07-23-v0.5.0-study-desk-production-review.md)
   记录完整裁决；durable commits `42a7eccf`、`1198915b`、`887078d6` 已形成并排除 C 轨
   manifest，production fixture cleanup 已关闭。`CTL-A06b` pre-art 前端现已实现并通过
   Web/browser、runtime sync 与实际 Tauri route/exact-return/restart 作者验证，当前未提交；
   [集中 review handoff](../handoffs/2026-07-24-v0.5.0-pre-art-frontend-review.md) 记录边界。
   durable commit、独立 Review、真实五题型写入、原生 200% 与故障注入仍阻止 `DONE`。该包
   不得跳过 `CTL-A02/A03/A04/A05` 的 E/X/V Gate。
2. `CTL-C01` 已在独立 worktree 完成 evidence commit `45edb247`，并由非作者 Gate reviewer
   完成 Gateway/core、Python policy、Rust shell、Web Settings、Bundle/release 五方复核后标记
   `DONE`：base `62fe63c72b88c750ac4d60c52d3a788941877fc4`，
   branch `codex/v050-c01-platform-manifest`，worktree
   `D:\project\Kabuqina\.worktrees\c-track-c01`。本切片只允许修改 C-0 manifest、审计报告、
   审计器/合同测试与本控制板；禁止修改 gateway registry/config/run/delivery、profile、Rust
   supervisor、Web Settings、requirements/bundle 等运行时 shared spine。环境键 evidence
   只从 owned runtime 的真实 access/声明结构发现 678 个 literal access 与 396 个结构化
   exact declaration 与 2 个 computed-key template，未知 exact key/namespace 失败关闭，
   形成 796 条 exact edge；32 个 wildcard namespace 独立建模，不冒充 exact key。计算型
   `{PLATFORM}_HOME_CHANNEL(_NAME)` 对 21 个 built-in 与 bundled IRC/Teams 展开，未分类
   plugin 失败关闭。验证命令为
   `python scripts/audit_v050_platform_manifest.py --check-observed`、
   `python -m unittest python.tests.test_v050_platform_manifest -v` 与 `git diff --check`；
   回滚只还原 evidence/signoff commit 与本板状态，不涉及数据迁移或运行时行为。`CTL-C01`
   的退出证据已经验收；`CTL-G00` 已按 [conditional-GO sign-off](../progress/2026-07-19-v0.5.0-ctl-g00-release-baseline-signoff.md)
   关闭后 `CTL-C02` 已按本板的 JIT plan、owner、热区和 review 规则实施，并经 reviewer
   签字转为 `DONE`。`CTL-C03a/C03b` 已分别验收；`CTL-C03c/C04` 已在
   `f7c72c04` 完成 [合并 Review sign-off](../progress/2026-07-25-v0.5.0-ctl-c03c-c04-combined-signoff.md)
   并同时转为 `DONE`，C05 前置已满足。
3. `CTL-B01` 已在独立 worktree 完成 [L-0 design v3](2026-07-19-v0.5.0-ctl-b01-tutor-runtime-design.md)
   与 [sign-off evidence](../progress/2026-07-19-v0.5.0-ctl-b01-design-evidence.md)，并由非作者
   Gate reviewer 验收 `fb20a04d` 后标记 `DONE`：shared coordination DB 强制
   desk/Gateway/cron writer fence；L-2 以 host control 保留一次 remediate；downgrade backup
   用 runtime-only merge 无损恢复；terminal budget 真值与 35 s call/120 s active deadline 已冻结。
   `CTL-B02` 已在 branch `codex/v050-b02-tutor-lifecycle`、worktree
   `D:\project\Kabuqina\.worktrees\b-track-b02` 从 sign-off base `2c34a8f2` 完成
   [独立 JIT plan](2026-07-19-v0.5.0-ctl-b02-tutor-lifecycle-jit-plan.md) 的 bundle 前实现与
   [evidence](../progress/2026-07-19-v0.5.0-ctl-b02-tutor-lifecycle-evidence.md)：独立 runtime、
   跨进程 fence、BundleV2/runtime-only restore、candidate desk/Tauri API 与安全备份均已落地；
   初始 core 334、Python 359、Rust study 11 全绿，真实 v0.4 source red characterization 证明旧版
   owner delete 不触碰两张新 DB。2026-07-23 非作者 review 提出的 R1–R4 已由
   `27908527/e64921b2` 修复：desktop-only dead-writer recovery、projection owner guard、
   post-merge aggregate quotas 与 Python/Rust shared canonical fixture；后续 R5 由 `d125a4c8`
   关闭伪造/复制 terminal outbox history，要求唯一 event id 与 run/type/time/payload 真值完全一致。
   fresh core 350、B02 Python 17、Rust study 13 及进程级 recovery matrix 全绿。用户于
   2026-07-23 重建 1397.5 MB bundle，标准 smoke、STT 与 B02 runtime import 全部通过；
   reviewer 复核关键 bundled source hash 与嵌入式 imports 后完成
   [Gate sign-off](../progress/2026-07-23-v0.5.0-ctl-b02-tutor-lifecycle-signoff.md)，
   本卡转 `DONE`。正式制品 install/upgrade/downgrade 仍归 G01/Q-3。用户于 2026-07-26
   授权启动 `CTL-B03`；B03 已在 current-main base `5ef27dbf` 的独立 worktree 建立
   [JIT plan](2026-07-26-v0.5.0-ctl-b03-minimal-tutor-graph-plan.md)，范围仍不含
   Web/Gateway Tutor。用户同日取消 B 轨切片间 Gate；B03 已完成 deterministic graph、
   single-attempt provider、B02 lifecycle 接线、desktop activation 与广域回归，并形成
   [author evidence](../progress/2026-07-26-v0.5.0-ctl-b03-minimal-tutor-evidence.md) 后转 `DONE`。
   Review 材料统一在 B 轨收口时审查。
4. `CTL-G00` 已完成并允许 v0.5 runtime merge；不得把 conditional GO 写成 v0.5 发布许可，
   updater recovery/E2E、final artifact install/upgrade 与 provenance 仍由 `CTL-G01/Q-3 → Q-4` 关闭。
5. C 流当前无新开发卡 ACTIVE：`CTL-C06/C07` 已合并签字为 `DONE`；`CTL-G01`
   等 A/B 纵切完成后拉取。C06 六平台真实账号完整矩阵转交 `Q-2`，最终 release
   candidate 的 clean-install/upgrade/profile confirmation 归 `Q-3`；不为每个平台重复 bundle。
6. 三张产品卡进入 `ACTIVE` 后停止继续开新功能；只有其中一张 `DONE/BLOCKED` 并完成
   handoff，才从相应流的 `NEXT` 拉取下一张。
